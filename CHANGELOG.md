# Changelog

## [1.1.0] - 2026-09-16

### Added

- 新增 `theme:update`：下载完整主题运行包，校验 SHA256、版本及提交，安装成功才写入 `THEME_REVISION`，解决升级必须人工比对模板和构建产物的问题。
- `dev`、`server`、`build` 自动同步主题，并校验输入和生成目录指纹；未变时跳过重建，避免每次启动重复构建。
- 新增升级集成测试，覆盖新版默认字段、数组缩减/清空、资源覆盖、旧文件清理及失败回滚。

### Changed

- 站点版本升级为 `1.1.0`，主题切换到 v1.9.0 发布包并锁定提交及 SHA256。

- 个人配置与资源分别维护在 `theme/config.yml` 和 `theme/assets/`，`themes/hexo/` 改为整体生成目录；移出 43 个与上游相同的默认资源副本，避免旧覆盖副本阻止新版默认资源生效。
- 个人配置改为对象逐层覆盖、数组整体替换，省略字段继承新版默认值；站点与主题默认配置混在同一文件导致的新字段遗漏不再需要手工摘取。
- Pages CI 按版本记录区分发布包和旧版源码模式，发布包模式无需 checkout 上游或安装 pnpm；README 与 AGENTS.md 同步更新操作约定。

### Fixed

- 命令行更新工具放入 `tools/`，避开 Hexo 对根 `scripts/` 的插件自动加载，修复生成时把 ESM 工具当插件执行产生的语法错误。
- 旧版源码构建补齐上游默认图片/音频，并验证 tag 与完整提交 SHA 一致；先准备完整目录再替换，配置或校验失败保留旧主题。
