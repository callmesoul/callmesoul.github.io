//#region ../../packages/core/dist/index.js
var e = "/*! tailwindcss v4.3.3 | MIT License | https://tailwindcss.com */\n@layer properties{@supports (((-webkit-hyphens:none)) and (not (margin-trim:inline))) or ((-moz-orient:inline) and (not (color:rgb(from red r g b)))){*,:before,:after,::backdrop{--tw-rotate-x:initial;--tw-rotate-y:initial;--tw-rotate-z:initial;--tw-skew-x:initial;--tw-skew-y:initial;--tw-border-style:solid;--tw-shadow:0 0 #0000;--tw-shadow-color:initial;--tw-shadow-alpha:100%;--tw-inset-shadow:0 0 #0000;--tw-inset-shadow-color:initial;--tw-inset-shadow-alpha:100%;--tw-ring-color:initial;--tw-ring-shadow:0 0 #0000;--tw-inset-ring-color:initial;--tw-inset-ring-shadow:0 0 #0000;--tw-ring-inset:initial;--tw-ring-offset-width:0px;--tw-ring-offset-color:#fff;--tw-ring-offset-shadow:0 0 #0000;--tw-outline-style:solid;--tw-blur:initial;--tw-brightness:initial;--tw-contrast:initial;--tw-grayscale:initial;--tw-hue-rotate:initial;--tw-invert:initial;--tw-opacity:initial;--tw-saturate:initial;--tw-sepia:initial;--tw-drop-shadow:initial;--tw-drop-shadow-color:initial;--tw-drop-shadow-alpha:100%;--tw-drop-shadow-size:initial;--tw-backdrop-blur:initial;--tw-backdrop-brightness:initial;--tw-backdrop-contrast:initial;--tw-backdrop-grayscale:initial;--tw-backdrop-hue-rotate:initial;--tw-backdrop-invert:initial;--tw-backdrop-opacity:initial;--tw-backdrop-saturate:initial;--tw-backdrop-sepia:initial;--tw-ease:initial}}}@layer theme{:root,:host{--font-sans:-apple-system, BlinkMacSystemFont, \"Segoe UI\", Roboto, \"Helvetica Neue\", \"PingFang SC\", \"Hiragino Sans GB\", \"Microsoft YaHei\", \"Noto Sans SC\", Arial, sans-serif;--font-mono:ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, \"Liberation Mono\", \"Courier New\", monospace;--ease-out:cubic-bezier(0, 0, .2, 1);--ease-in-out:cubic-bezier(.4, 0, .2, 1);--default-transition-duration:.15s;--default-transition-timing-function:cubic-bezier(.4, 0, .2, 1);--default-font-family:var(--font-sans);--default-mono-font-family:var(--font-mono);--color-body:#f2f2f2}}@layer base{*,:after,:before,::backdrop{box-sizing:border-box;border:0 solid;margin:0;padding:0}::file-selector-button{box-sizing:border-box;border:0 solid;margin:0;padding:0}html,:host{-webkit-text-size-adjust:100%;tab-size:4;line-height:1.5;font-family:var(--default-font-family,-apple-system, BlinkMacSystemFont, \"Segoe UI\", Roboto, \"Helvetica Neue\", \"Noto Sans\", Arial, sans-serif, \"Apple Color Emoji\", \"Segoe UI Emoji\", \"Segoe UI Symbol\", \"Noto Color Emoji\");font-feature-settings:var(--default-font-feature-settings,normal);font-variation-settings:var(--default-font-variation-settings,normal);-webkit-tap-highlight-color:transparent}hr{height:0;color:inherit;border-top-width:1px}abbr:where([title]){-webkit-text-decoration:underline dotted;text-decoration:underline dotted}h1,h2,h3,h4,h5,h6{font-size:inherit;font-weight:inherit}a{color:inherit;-webkit-text-decoration:inherit;-webkit-text-decoration:inherit;-webkit-text-decoration:inherit;-webkit-text-decoration:inherit;text-decoration:inherit}b,strong{font-weight:bolder}code,kbd,samp,pre{font-family:var(--default-mono-font-family,ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, \"Liberation Mono\", \"Courier New\", monospace);font-feature-settings:var(--default-mono-font-feature-settings,normal);font-variation-settings:var(--default-mono-font-variation-settings,normal);font-size:1em}small{font-size:80%}sub,sup{vertical-align:baseline;font-size:75%;line-height:0;position:relative}sub{bottom:-.25em}sup{top:-.5em}table{text-indent:0;border-color:inherit;border-collapse:collapse}:-moz-focusring:where(:not(iframe)){outline:auto}progress{vertical-align:baseline}summary{display:list-item}ol,ul,menu{list-style:none}img,svg,video,canvas,audio,iframe,embed,object{vertical-align:middle;display:block}img,video{max-width:100%;height:auto}button,input,select,optgroup,textarea{font:inherit;font-feature-settings:inherit;font-variation-settings:inherit;letter-spacing:inherit;color:inherit;opacity:1;background-color:#0000;border-radius:0}::file-selector-button{font:inherit;font-feature-settings:inherit;font-variation-settings:inherit;letter-spacing:inherit;color:inherit;opacity:1;background-color:#0000;border-radius:0}:where(select:is([multiple],[size])) optgroup{font-weight:bolder}:where(select:is([multiple],[size])) optgroup option{padding-inline-start:20px}::file-selector-button{margin-inline-end:4px}::placeholder{opacity:1}@supports (not ((-webkit-appearance:-apple-pay-button))) or (contain-intrinsic-size:1px){::placeholder{color:currentColor}@supports (color:color-mix(in lab, red, red)){::placeholder{color:color-mix(in oklab, currentcolor 50%, transparent)}}}textarea{resize:vertical}::-webkit-search-decoration{-webkit-appearance:none}::-webkit-date-and-time-value{min-height:1lh;text-align:inherit}::-webkit-datetime-edit{display:inline-flex}::-webkit-datetime-edit-fields-wrapper{padding:0}::-webkit-datetime-edit{padding-block:0}::-webkit-datetime-edit-year-field{padding-block:0}::-webkit-datetime-edit-month-field{padding-block:0}::-webkit-datetime-edit-day-field{padding-block:0}::-webkit-datetime-edit-hour-field{padding-block:0}::-webkit-datetime-edit-minute-field{padding-block:0}::-webkit-datetime-edit-second-field{padding-block:0}::-webkit-datetime-edit-millisecond-field{padding-block:0}::-webkit-datetime-edit-meridiem-field{padding-block:0}::-webkit-calendar-picker-indicator{line-height:1}:-moz-ui-invalid{box-shadow:none}button,input:where([type=button],[type=reset],[type=submit]){appearance:button}::file-selector-button{appearance:button}::-webkit-inner-spin-button{height:auto}::-webkit-outer-spin-button{height:auto}[hidden]:where(:not([hidden=until-found])){display:none!important}}@layer components;@layer utilities{.\\@container{container-type:inline-size}.static{position:static}.\\!container{width:100%!important}@media (width>=40rem){.\\!container{max-width:40rem!important}}@media (width>=48rem){.\\!container{max-width:48rem!important}}@media (width>=64rem){.\\!container{max-width:64rem!important}}@media (width>=80rem){.\\!container{max-width:80rem!important}}@media (width>=96rem){.\\!container{max-width:96rem!important}}.container{width:100%}@media (width>=40rem){.container{max-width:40rem}}@media (width>=48rem){.container{max-width:48rem}}@media (width>=64rem){.container{max-width:64rem}}@media (width>=80rem){.container{max-width:80rem}}@media (width>=96rem){.container{max-width:96rem}}.block{display:block}.flex{display:flex}.grid{display:grid}.hidden{display:none}.inline{display:inline}.table{display:table}.flex-shrink{flex-shrink:1}.border-collapse{border-collapse:collapse}.transform{transform:var(--tw-rotate-x,) var(--tw-rotate-y,) var(--tw-rotate-z,) var(--tw-skew-x,) var(--tw-skew-y,)}.resize{resize:both}.flex-wrap{flex-wrap:wrap}.border{border-style:var(--tw-border-style);border-width:1px}.text-wrap{text-wrap:wrap}.shadow{--tw-shadow:0 1px 3px 0 var(--tw-shadow-color,#0000001a), 0 1px 2px -1px var(--tw-shadow-color,#0000001a);box-shadow:var(--tw-inset-shadow), var(--tw-inset-ring-shadow), var(--tw-ring-offset-shadow), var(--tw-ring-shadow), var(--tw-shadow)}.outline{outline-style:var(--tw-outline-style);outline-width:1px}.filter{filter:var(--tw-blur,) var(--tw-brightness,) var(--tw-contrast,) var(--tw-grayscale,) var(--tw-hue-rotate,) var(--tw-invert,) var(--tw-saturate,) var(--tw-sepia,) var(--tw-drop-shadow,)}.backdrop-filter{-webkit-backdrop-filter:var(--tw-backdrop-blur,) var(--tw-backdrop-brightness,) var(--tw-backdrop-contrast,) var(--tw-backdrop-grayscale,) var(--tw-backdrop-hue-rotate,) var(--tw-backdrop-invert,) var(--tw-backdrop-opacity,) var(--tw-backdrop-saturate,) var(--tw-backdrop-sepia,);backdrop-filter:var(--tw-backdrop-blur,) var(--tw-backdrop-brightness,) var(--tw-backdrop-contrast,) var(--tw-backdrop-grayscale,) var(--tw-backdrop-hue-rotate,) var(--tw-backdrop-invert,) var(--tw-backdrop-opacity,) var(--tw-backdrop-saturate,) var(--tw-backdrop-sepia,)}.transition{transition-property:color,background-color,border-color,outline-color,text-decoration-color,fill,stroke,--tw-gradient-from,--tw-gradient-via,--tw-gradient-to,opacity,box-shadow,transform,translate,scale,rotate,filter,-webkit-backdrop-filter,backdrop-filter,display,content-visibility,overlay,pointer-events;transition-timing-function:var(--tw-ease,var(--default-transition-timing-function));transition-duration:var(--tw-duration,var(--default-transition-duration))}.ease-in-out{--tw-ease:var(--ease-in-out);transition-timing-function:var(--ease-in-out)}.ease-out{--tw-ease:var(--ease-out);transition-timing-function:var(--ease-out)}}:root{--brand-primary:#eb4f38;--brand-cta:#ee5b44;--brand-rgb:235, 79, 56;--brand-cta-rgb:238, 91, 68}:host{font-family:var(--font-sans,-apple-system, BlinkMacSystemFont, \"Segoe UI\", Roboto, \"Helvetica Neue\", \"PingFang SC\", \"Hiragino Sans GB\", \"Microsoft YaHei\", \"Noto Sans SC\", Arial, sans-serif);letter-spacing:.01em;color:var(--color-body,#f2f2f2);font-size:14px;line-height:1.6}@property --tw-rotate-x{syntax:\"*\";inherits:false}@property --tw-rotate-y{syntax:\"*\";inherits:false}@property --tw-rotate-z{syntax:\"*\";inherits:false}@property --tw-skew-x{syntax:\"*\";inherits:false}@property --tw-skew-y{syntax:\"*\";inherits:false}@property --tw-border-style{syntax:\"*\";inherits:false;initial-value:solid}@property --tw-shadow{syntax:\"*\";inherits:false;initial-value:0 0 #0000}@property --tw-shadow-color{syntax:\"*\";inherits:false}@property --tw-shadow-alpha{syntax:\"<percentage>\";inherits:false;initial-value:100%}@property --tw-inset-shadow{syntax:\"*\";inherits:false;initial-value:0 0 #0000}@property --tw-inset-shadow-color{syntax:\"*\";inherits:false}@property --tw-inset-shadow-alpha{syntax:\"<percentage>\";inherits:false;initial-value:100%}@property --tw-ring-color{syntax:\"*\";inherits:false}@property --tw-ring-shadow{syntax:\"*\";inherits:false;initial-value:0 0 #0000}@property --tw-inset-ring-color{syntax:\"*\";inherits:false}@property --tw-inset-ring-shadow{syntax:\"*\";inherits:false;initial-value:0 0 #0000}@property --tw-ring-inset{syntax:\"*\";inherits:false}@property --tw-ring-offset-width{syntax:\"<length>\";inherits:false;initial-value:0}@property --tw-ring-offset-color{syntax:\"*\";inherits:false;initial-value:#fff}@property --tw-ring-offset-shadow{syntax:\"*\";inherits:false;initial-value:0 0 #0000}@property --tw-outline-style{syntax:\"*\";inherits:false;initial-value:solid}@property --tw-blur{syntax:\"*\";inherits:false}@property --tw-brightness{syntax:\"*\";inherits:false}@property --tw-contrast{syntax:\"*\";inherits:false}@property --tw-grayscale{syntax:\"*\";inherits:false}@property --tw-hue-rotate{syntax:\"*\";inherits:false}@property --tw-invert{syntax:\"*\";inherits:false}@property --tw-opacity{syntax:\"*\";inherits:false}@property --tw-saturate{syntax:\"*\";inherits:false}@property --tw-sepia{syntax:\"*\";inherits:false}@property --tw-drop-shadow{syntax:\"*\";inherits:false}@property --tw-drop-shadow-color{syntax:\"*\";inherits:false}@property --tw-drop-shadow-alpha{syntax:\"<percentage>\";inherits:false;initial-value:100%}@property --tw-drop-shadow-size{syntax:\"*\";inherits:false}@property --tw-backdrop-blur{syntax:\"*\";inherits:false}@property --tw-backdrop-brightness{syntax:\"*\";inherits:false}@property --tw-backdrop-contrast{syntax:\"*\";inherits:false}@property --tw-backdrop-grayscale{syntax:\"*\";inherits:false}@property --tw-backdrop-hue-rotate{syntax:\"*\";inherits:false}@property --tw-backdrop-invert{syntax:\"*\";inherits:false}@property --tw-backdrop-opacity{syntax:\"*\";inherits:false}@property --tw-backdrop-saturate{syntax:\"*\";inherits:false}@property --tw-backdrop-sepia{syntax:\"*\";inherits:false}@property --tw-ease{syntax:\"*\";inherits:false}", t = null;
function n() {
	if (t) return t;
	try {
		let n = new CSSStyleSheet();
		return n.replaceSync(e), t = n, n;
	} catch {
		return null;
	}
}
var r = class extends HTMLElement {
	shadow;
	constructor() {
		super(), this.shadow = this.attachShadow({ mode: "open" });
	}
	mounted() {}
	$(e) {
		return this.shadow.querySelector(e);
	}
	$$(e) {
		return this.shadow.querySelectorAll(e);
	}
	emit(e, t, n) {
		let r = new CustomEvent(e, {
			detail: t,
			bubbles: !0,
			composed: !0,
			cancelable: n?.cancelable === !0
		});
		return this.dispatchEvent(r), r;
	}
	connectedCallback() {
		if (this.shadow.children.length > 0) return;
		let t = n();
		if (t) try {
			this.shadow.adoptedStyleSheets = [t];
		} catch {
			let t = document.createElement("style");
			t.textContent = e, this.shadow.appendChild(t);
		}
		else {
			let t = document.createElement("style");
			t.textContent = e, this.shadow.appendChild(t);
		}
		this.shadow.innerHTML += this.render(), this.mounted();
	}
	attributeChangedCallback(e, t, n) {
		if (t === n) return;
		let r = this.constructor.observedAttributes;
		r && r.indexOf(e) === -1 || (this.shadow.innerHTML = "", this.shadow.innerHTML = this.render(), this.mounted());
	}
}, i = class extends r {
	static get observedAttributes() {
		return ["image"];
	}
	render() {
		return `
      <style>
        :host {
          display: block;
          position: fixed;
          inset: 0;
          z-index: 0;
          overflow: hidden;
          background-color: #080808;
        }
        img {
          position: absolute;
          inset: 0;
          width: 100%;
          height: 100%;
          object-fit: cover;
          object-position: center;
        }
        .overlay {
          pointer-events: none;
          position: absolute;
          inset: 0;
          background:
            radial-gradient(ellipse at 30% 35%, rgba(50,48,45,0.35) 0%, transparent 55%),
            radial-gradient(ellipse at 65% 20%, rgba(40,38,35,0.3) 0%, transparent 45%),
            linear-gradient(180deg, rgba(8,8,8,0.65) 0%, rgba(8,8,8,0.3) 40%, rgba(8,8,8,0.9) 100%);
        }
      </style>
      <img src="${this.getAttribute("image") || ""}" alt="" part="bg-image">
      <div class="overlay" part="overlay"></div>
    `;
	}
};
customElements.get("site-background") || customElements.define("site-background", i);
function a(e) {
	return String(e).replace(/[&<>"']/g, (e) => ({
		"&": "&amp;",
		"<": "&lt;",
		">": "&gt;",
		"\"": "&quot;",
		"'": "&#39;"
	})[e]);
}
var o = class e extends r {
	static get observedAttributes() {
		return ["active-cat", "active-tag"];
	}
	_hlEl = null;
	_directoryOpen = !1;
	static navigationItemsTemplate(e, t) {
		return e.map((e, n) => `
      <a class="nav-item${e.cat === t ? " active" : ""}"
         href="./#cat=${encodeURIComponent(e.cat)}"
         data-cat="${a(e.cat)}"
         title="${a(e.zh)} · ${a(e.en)}">
        <span class="nav-icon-slot" aria-hidden="true">
          <img class="nav-icon" src="${a(e.icon)}" alt=""
               style="width:${Number(e.w)}px;height:${Number(e.h)}px;">
        </span>
        <span class="nav-copy">
          <span class="nav-zh">${a(e.zh)}</span>
          <span class="nav-en">${a(e.en)}</span>
        </span>
        <span class="nav-index" aria-hidden="true">${String(n + 1).padStart(2, "0")}</span>
      </a>
    `).join("");
	}
	render() {
		let t = this.getAttribute("active-cat") || "all", n = this.getAttribute("active-tag") || "", r = this._categories, i = this._social, o = this._tags, s = this._icp, c = this._archiveUrl, l = this._aboutUrl, u = this._friendsUrl, d = this._siteName || "CallMeSoul", f = (r || []).map((e) => ({
			cat: e.id,
			zh: e.name,
			en: e.en,
			icon: e.icon,
			w: e.w,
			h: e.h,
			count: e.count
		})), p = ![
			"all",
			"archives",
			"about",
			"friends"
		].includes(t), m = this._directoryOpen || p, h = typeof window < "u" && window.matchMedia("(max-width: 1199px)").matches && !m, g = t === "all" && !m, _ = m || p, v = t === "archives", y = t === "about", b = t === "friends", x = e.navigationItemsTemplate(f, n ? "" : t), S = (o || []).map((e) => `<a class="tag-item${e.name === n ? " active" : ""}"
                 href="./#tag=${encodeURIComponent(e.name)}"
                 data-tag="${a(e.name)}">
                <span class="tag-name">${a(e.name)}</span>
                <span class="tag-count">${Number(e.count)}</span>
              </a>`).join(""), C = (i || []).map((e) => {
			let t = a(e.name || ""), n = `<img${e.mono ? " class=\"tone-mono\"" : ""}
                         src="${a(e.icon)}" alt="${t}"
                         style="width:${e.width || 22}px;height:${e.height || 18}px;object-fit:contain;--hue:${e.hue ?? 0}deg;">`, r = e.href && e.href !== "#" ? a(e.href) : "", i = e.qr && e.qr !== "#" ? a(e.qr) : "";
			return r ? `<a class="social-icon has-link" href="${r}" target="_blank"
                   rel="noopener noreferrer" aria-label="${t}" title="${t}">${n}</a>` : i ? `<span class="social-icon has-qr" role="img" aria-label="${t}二维码" title="扫码关注${t}">
          ${n}
          <span class="social-qr">
            <img class="qr-image" src="${i}" alt="${t}二维码">
            <em>扫一扫关注</em><b>${t}</b>
          </span>
        </span>` : `<span class="social-icon is-static" aria-label="${t}" title="${t}">${n}</span>`;
		}).join(""), w = c ? `<a class="primary-item${v ? " active" : ""}"
            href="${a(c)}" data-cat="archives"
            aria-label="归档" title="归档 · Archives">
          <svg viewBox="0 0 24 24" aria-hidden="true">
            <path d="M4.5 8h15v10.5a1.5 1.5 0 0 1-1.5 1.5H6a1.5 1.5 0 0 1-1.5-1.5z"></path>
            <path d="M3.5 4h17v4h-17zM9 12h6"></path>
          </svg>
          <span class="primary-tooltip">归档</span>
        </a>` : "", T = l ? `<a class="primary-item${y ? " active" : ""}"
            href="${a(l)}" data-cat="about"
            aria-label="关于" title="关于我 · About">
          <svg viewBox="0 0 24 24" aria-hidden="true">
            <circle cx="12" cy="8" r="3"></circle>
            <path d="M6 19c.8-4 3-6 6-6s5.2 2 6 6"></path>
          </svg>
          <span class="primary-tooltip">关于</span>
        </a>` : "", E = u ? `<a class="primary-item${b ? " active" : ""}"
            href="${a(u)}" data-cat="friends"
            aria-label="友链" title="友链 · Friends">
          <svg viewBox="0 0 24 24" aria-hidden="true">
            <circle cx="8" cy="12" r="3.5"></circle>
            <circle cx="16" cy="12" r="3.5"></circle>
            <path d="M11.5 12h1M5.2 9.5 3.8 8M18.8 9.5 20.2 8"></path>
          </svg>
          <span class="primary-tooltip">友链</span>
        </a>` : "";
		return `
      <style>
        :host {
          display: contents;
        }
        * {
          box-sizing: border-box;
        }
        .sidebar {
          position: relative;
          z-index: 20;
          display: flex;
          width: 320px;
          min-width: 320px;
          max-width: 320px;
          height: 100%;
          color: #f2f2f2;
          transition: width 0.32s cubic-bezier(0.22, 1, 0.36, 1),
                      min-width 0.32s cubic-bezier(0.22, 1, 0.36, 1),
                      max-width 0.32s cubic-bezier(0.22, 1, 0.36, 1);
        }
        .sidebar.directory-open {
          width: 320px;
          min-width: 320px;
          max-width: 320px;
        }

        /* 一级图标轨道 */
        .primary-rail {
          position: relative;
          z-index: 2;
          display: flex;
          width: 68px;
          min-width: 68px;
          height: 100%;
          flex-direction: column;
          align-items: center;
          border-right: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(9, 9, 9, 0.97);
          backdrop-filter: blur(18px) saturate(120%);
          -webkit-backdrop-filter: blur(18px) saturate(120%);
        }
        .primary-logo {
          display: grid;
          width: 38px;
          height: 38px;
          margin: 22px auto 26px;
          place-items: center;
          border-radius: 11px;
          background: var(--brand-primary);
          box-shadow: 0 8px 22px rgba(var(--brand-rgb), 0.24);
        }
        .primary-logo img {
          display: block;
          width: 21px;
          height: auto;
          object-fit: contain;
          filter: brightness(0) invert(1);
        }
        .primary-nav {
          display: flex;
          width: 100%;
          flex-direction: column;
          align-items: center;
          gap: 10px;
        }
        .primary-item {
          position: relative;
          display: grid;
          width: 42px;
          height: 42px;
          padding: 0;
          place-items: center;
          border: 0;
          border-radius: 12px;
          color: #76716b;
          background: transparent;
          cursor: pointer;
          text-decoration: none;
          -webkit-tap-highlight-color: transparent;
          transition: color 0.2s ease, background 0.2s ease, transform 0.2s ease;
        }
        .primary-item::before {
          content: '';
          position: absolute;
          top: 8px;
          bottom: 8px;
          left: -13px;
          width: 3px;
          border-radius: 0 3px 3px 0;
          background: var(--brand-primary);
          opacity: 0;
          transition: opacity 0.2s ease;
        }
        .primary-item:hover,
        .primary-item:focus-visible {
          color: #ffffff;
          background: rgba(255, 255, 255, 0.055);
        }
        .primary-item.active {
          color: var(--brand-primary);
          background: rgba(var(--brand-rgb), 0.14);
        }
        .primary-item.active::before {
          opacity: 1;
        }
        .primary-item:active {
          transform: scale(0.94);
        }
        .primary-item img {
          display: block;
          width: 20px;
          height: 20px;
          object-fit: contain;
          filter: brightness(0) invert(0.46);
          transition: filter 0.2s ease;
        }
        .primary-item:hover img,
        .primary-item:focus-visible img {
          filter: brightness(0) invert(1);
        }
        .primary-item.active img {
          filter: brightness(0) saturate(100%) invert(46%) sepia(82%) saturate(2207%) hue-rotate(336deg) brightness(100%) contrast(87%);
        }
        .primary-item svg {
          width: 19px;
          height: 19px;
          fill: none;
          stroke: currentColor;
          stroke-linecap: round;
          stroke-linejoin: round;
          stroke-width: 1.8;
        }
        .primary-tooltip {
          position: absolute;
          top: 50%;
          left: calc(100% + 12px);
          z-index: 80;
          padding: 5px 9px;
          border: 1px solid #3c3936;
          border-radius: 6px;
          color: #ffffff;
          background: rgba(20, 19, 18, 0.96);
          box-shadow: 0 8px 18px rgba(0, 0, 0, 0.3);
          font-family: var(--font-sans);
          font-size: 11px;
          line-height: 1.3;
          white-space: nowrap;
          opacity: 0;
          visibility: hidden;
          pointer-events: none;
          transform: translate(-4px, -50%);
          transition: opacity 0.18s ease, transform 0.18s ease, visibility 0.18s;
        }
        .primary-item:hover .primary-tooltip,
        .primary-item:focus-visible .primary-tooltip {
          opacity: 1;
          visibility: visible;
          transform: translate(0, -50%);
        }
        .directory-open .primary-tooltip {
          display: none;
        }
        .rail-caption {
          margin-top: auto;
          margin-bottom: 18px;
          color: #494541;
          font-family: var(--font-sans);
          font-size: 8px;
          letter-spacing: 0.12em;
          text-transform: uppercase;
          writing-mode: vertical-rl;
          text-decoration: none;
          cursor: pointer;
          transition: color 0.2s ease;
        }
        .rail-caption:hover { color: #9e9d99; }

        /* 二级目录面板 */
        .secondary-panel {
          position: absolute;
          top: 0;
          bottom: 0;
          left: 68px;
          z-index: 1;
          display: flex;
          width: 252px;
          flex-direction: column;
          border-right: 1px solid rgba(255, 255, 255, 0.12);
          color: #f2f2f2;
          background: rgba(20, 18, 16, 0.94);
          box-shadow: 18px 0 48px rgba(0, 0, 0, 0.15);
          backdrop-filter: blur(18px) saturate(120%);
          -webkit-backdrop-filter: blur(18px) saturate(120%);
          opacity: 1;
          visibility: visible;
          pointer-events: auto;
          transform: translateX(0);
          transition: opacity 0.24s ease,
                      transform 0.32s cubic-bezier(0.22, 1, 0.36, 1),
                      visibility 0.24s;
        }
        .secondary-header {
          display: flex;
          min-height: 104px;
          padding: 22px 16px;
          align-items: center;
          justify-content: space-between;
          border-bottom: 1px solid rgba(255, 255, 255, 0.06);
        }
        .secondary-title {
          min-width: 0;
        }
        .context-title {
          display: none;
        }
        .sidebar:not(.directory-open):not(.archive-context):not(.about-context):not(.friends-context) .context-home,
        .sidebar.directory-open .context-directory,
        .sidebar.archive-context:not(.directory-open) .context-archive,
        .sidebar.about-context:not(.directory-open) .context-about,
        .sidebar.friends-context:not(.directory-open) .context-friends {
          display: block;
        }
        .secondary-title small,
        .secondary-title strong,
        .secondary-title span {
          display: block;
        }
        .secondary-title small {
          overflow: hidden;
          margin-bottom: 5px;
          color: #706a64;
          font-family: var(--font-sans);
          font-size: 9px;
          line-height: 1.2;
          letter-spacing: 0.05em;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
        .secondary-title strong {
          color: #ffffff;
          font-family: var(--font-sans);
          font-size: 17px;
          font-weight: 500;
          line-height: 1.25;
          letter-spacing: 0.04em;
        }
        .secondary-title span {
          margin-top: 3px;
          color: #625d57;
          font-family: var(--font-sans);
          font-size: 8px;
          line-height: 1.2;
          letter-spacing: 0.14em;
          text-transform: uppercase;
        }
        .panel-close {
          display: none;
          width: 32px;
          height: 32px;
          flex: 0 0 auto;
          padding: 0;
          place-items: center;
          border: 0;
          border-radius: 8px;
          color: #746e68;
          background: rgba(255, 255, 255, 0.04);
          cursor: pointer;
        }
        .panel-close:hover,
        .panel-close:focus-visible {
          color: #ffffff;
          background: rgba(255, 255, 255, 0.08);
        }
        .panel-close svg {
          width: 15px;
          height: 15px;
          fill: none;
          stroke: currentColor;
          stroke-linecap: round;
          stroke-width: 1.8;
        }
        .site-nav {
          position: relative;
          min-height: 0;
          flex: 1;
          padding: 18px 0 10px;
          overflow-x: hidden;
          overflow-y: auto;
          scrollbar-color: #45403b transparent;
          scrollbar-width: thin;
        }
        .categories-section {
          display: none;
        }
        .directory-open .categories-section {
          display: block;
        }
        .site-nav::-webkit-scrollbar {
          width: 4px;
        }
        .site-nav::-webkit-scrollbar-track {
          background: transparent;
        }
        .site-nav::-webkit-scrollbar-thumb {
          border-radius: 3px;
          background: #45403b;
        }
        .section-heading {
          display: flex;
          margin: 0 14px 9px;
          align-items: center;
          justify-content: space-between;
          color: #6d6761;
          font-family: var(--font-sans);
          font-size: 10px;
          font-weight: 500;
          line-height: 1.2;
          letter-spacing: 0.11em;
        }
        .section-heading span:last-child {
          color: #4e4944;
          font-size: 9px;
          letter-spacing: 0;
        }
        .nav-item {
          position: relative;
          z-index: 1;
          display: grid;
          grid-template-columns: 38px minmax(0, 1fr) 24px;
          min-height: 52px;
          margin: 1px 0;
          padding: 0 14px;
          align-items: center;
          color: #9e9d99;
          cursor: pointer;
          text-decoration: none;
          -webkit-tap-highlight-color: transparent;
          transition: color 0.2s ease;
        }
        .nav-item:hover,
        .nav-item.active {
          color: #ffffff;
        }
        .nav-icon-slot {
          display: grid;
          width: 32px;
          height: 32px;
          place-items: center;
          border-radius: 9px;
          background: rgba(255, 255, 255, 0.035);
        }
        .nav-icon {
          max-width: 18px;
          max-height: 18px;
          object-fit: contain;
          filter: brightness(0) invert(0.52);
          transition: filter 0.2s ease, transform 0.2s ease;
        }
        .nav-item:hover .nav-icon,
        .nav-item.active .nav-icon {
          filter: brightness(0) saturate(100%) invert(46%) sepia(82%) saturate(2207%) hue-rotate(336deg) brightness(100%) contrast(87%);
          transform: scale(1.05);
        }
        .nav-copy,
        .nav-zh,
        .nav-en {
          display: block;
          min-width: 0;
        }
        .nav-copy {
          padding-left: 7px;
        }
        .nav-zh,
        .nav-en {
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
        .nav-zh {
          font-family: var(--font-sans);
          font-size: 13px;
          font-weight: 500;
          line-height: 1.25;
          letter-spacing: 0.02em;
        }
        .nav-en {
          margin-top: 3px;
          color: #69635d;
          font-family: var(--font-sans);
          font-size: 8px;
          line-height: 1.2;
          letter-spacing: 0.08em;
          text-transform: uppercase;
        }
        .nav-item.active .nav-en {
          color: #8d8580;
        }
        .nav-index {
          color: #514c47;
          font-family: var(--font-sans);
          font-size: 9px;
          line-height: 1;
          text-align: right;
        }
        .nav-item.active .nav-index {
          color: var(--brand-primary);
        }
        .nav-highlight {
          position: absolute;
          top: 0;
          left: 0;
          z-index: 0;
          width: 100%;
          opacity: 0;
          pointer-events: none;
          will-change: transform;
          transition: transform 0.5s cubic-bezier(0.22, 1, 0.36, 1), opacity 0.2s ease;
        }
        .nav-highlight.is-on {
          opacity: 1;
        }
        .nav-highlight.no-anim {
          transition: none;
        }
        .nav-highlight::before {
          content: '';
          position: absolute;
          inset: 4px 10px;
          border-radius: 10px;
          background: linear-gradient(90deg,
            rgba(var(--brand-rgb), 0.15),
            rgba(var(--brand-rgb), 0.055));
        }
        .nav-highlight::after {
          content: '';
          position: absolute;
          top: 10px;
          bottom: 10px;
          left: 0;
          width: 3px;
          border-radius: 0 3px 3px 0;
          background: var(--brand-primary);
        }

        /* 标签 */
        .tags-section {
          margin: 16px 14px 0;
          padding-top: 15px;
          border-top: 1px solid rgba(255, 255, 255, 0.075);
        }
        /* 首页/归档（非目录浏览）场景：标签区作为面板最后一块，不显示上边框 */
        .sidebar:not(.directory-open) .tags-section {
          border-top: none;
        }
        .tags-section .section-heading {
          margin-right: 0;
          margin-left: 0;
        }
        .tags-list {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 6px;
        }
        .tag-item {
          display: flex;
          min-width: 0;
          min-height: 30px;
          padding: 5px 7px 5px 10px;
          align-items: center;
          justify-content: space-between;
          gap: 6px;
          border-radius: 7px;
          color: #9e9d99;
          background: rgba(255, 255, 255, 0.045);
          font-family: var(--font-sans);
          font-size: 11px;
          line-height: 1.25;
          text-decoration: none;
          transition: color 0.18s ease, background 0.18s ease, box-shadow 0.18s ease;
        }
        .tag-name {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
        .tag-count {
          flex: 0 0 auto;
          color: #625d57;
          font-size: 9px;
          font-weight: 400;
        }
        .tag-item:hover,
        .tag-item:focus-visible {
          color: #ffffff;
          background: rgba(var(--brand-rgb), 0.1);
          box-shadow: inset 2px 0 0 var(--brand-primary);
        }
        .tag-item.active {
          color: #ffffff;
          font-weight: 500;
          background: linear-gradient(90deg,
            rgba(var(--brand-rgb), 0.2),
            rgba(var(--brand-rgb), 0.1));
          box-shadow:
            inset 0 0 0 1px rgba(var(--brand-rgb), 0.38),
            inset 3px 0 0 var(--brand-primary);
        }
        .tag-item:hover .tag-count,
        .tag-item:focus-visible .tag-count {
          color: var(--brand-primary);
        }
        .tag-item.active .tag-count {
          padding: 1px 4px;
          border-radius: 4px;
          color: #ffffff;
          background: rgba(var(--brand-rgb), 0.22);
        }

        /* 二级面板底部 */
        .secondary-footer {
          margin-top: auto;
          border-top: 1px solid rgba(255, 255, 255, 0.065);
        }
        .icp-text {
          padding: 13px 16px 11px;
          overflow: hidden;
          color: #5c5751;
          font-family: var(--font-sans);
          font-size: 9px;
          line-height: 1.4;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
        .social-bar {
          display: grid;
          grid-template-columns: repeat(6, minmax(0, 1fr));
          height: 44px;
          border-top: 1px solid rgba(255, 255, 255, 0.065);
        }
        .social-icon {
          position: relative;
          display: flex;
          min-width: 0;
          align-items: center;
          justify-content: center;
          border-right: 1px solid rgba(255, 255, 255, 0.065);
          color: #8b857e;
          cursor: pointer;
          text-decoration: none;
        }
        .social-icon:last-child {
          border-right: 0;
        }
        .social-icon img {
          --hue: 0deg;
          opacity: 0.92;
          filter: invert(64%) sepia(79%) saturate(467%) hue-rotate(var(--hue)) brightness(97%);
          transition: opacity 0.2s ease, transform 0.2s ease, filter 0.2s ease;
        }
        .social-icon:hover img,
        .social-icon:focus-visible img {
          opacity: 1;
          transform: translateY(-1px) scale(1.06);
          filter: invert(54%) sepia(88%) saturate(740%) hue-rotate(var(--hue)) brightness(112%);
        }
        .social-icon img.tone-mono {
          filter: brightness(0) invert(0.62);
        }
        .social-icon:hover img.tone-mono,
        .social-icon:focus-visible img.tone-mono {
          filter: brightness(0) invert(0.92);
        }
        .social-icon::after {
          content: attr(aria-label);
          position: absolute;
          bottom: calc(100% + 9px);
          left: 50%;
          z-index: 70;
          padding: 4px 8px;
          border: 1px solid #3c3936;
          border-radius: 5px;
          color: #ffffff;
          background: rgba(20, 19, 18, 0.96);
          font-family: var(--font-sans);
          font-size: 11px;
          line-height: 1.3;
          white-space: nowrap;
          opacity: 0;
          visibility: hidden;
          pointer-events: none;
          transform: translate(-50%, 4px);
          transition: opacity 0.18s ease, transform 0.18s ease, visibility 0.18s;
        }
        .social-icon:hover::after,
        .social-icon:focus-visible::after {
          opacity: 1;
          visibility: visible;
          transform: translate(-50%, 0);
        }
        .social-icon.has-qr::after {
          display: none;
        }
        .social-qr {
          position: absolute;
          bottom: calc(100% + 9px);
          left: 50%;
          z-index: 80;
          display: flex;
          width: 146px;
          padding: 9px;
          flex-direction: column;
          align-items: center;
          gap: 4px;
          border: 1px solid #3c3936;
          border-radius: 8px;
          color: #ffffff;
          background: rgba(22, 21, 20, 0.98);
          box-shadow: 0 12px 28px rgba(0, 0, 0, 0.5);
          opacity: 0;
          visibility: hidden;
          pointer-events: none;
          transform: translate(-50%, 6px);
          transition: opacity 0.18s ease, transform 0.18s ease, visibility 0.18s;
        }
        .social-icon.has-qr:hover .social-qr,
        .social-icon.has-qr:focus-visible .social-qr {
          opacity: 1;
          visibility: visible;
          transform: translate(-50%, 0);
        }
        .social-bar .social-qr .qr-image {
          display: block;
          width: 120px !important;
          height: 120px !important;
          padding: 5px;
          border-radius: 6px;
          object-fit: contain;
          background: #ffffff;
          filter: none !important;
          opacity: 1 !important;
          transform: none !important;
        }
        .social-qr em {
          color: #9e9d99;
          font-family: var(--font-sans);
          font-size: 10px;
          font-style: normal;
          line-height: 1.3;
        }
        .social-qr b {
          font-family: var(--font-sans);
          font-size: 11px;
          font-weight: 500;
          line-height: 1.3;
        }
        .social-icon:first-child .social-qr {
          left: 5px;
          transform: translateY(6px);
        }
        .social-icon:first-child.has-qr:hover .social-qr,
        .social-icon:first-child.has-qr:focus-visible .social-qr {
          transform: translateY(0);
        }
        .social-icon:last-child .social-qr {
          right: 5px;
          left: auto;
          transform: translateY(6px);
        }
        .social-icon:last-child.has-qr:hover .social-qr,
        .social-icon:last-child.has-qr:focus-visible .social-qr {
          transform: translateY(0);
        }

        @media (max-width: 1199px) {
          .sidebar,
          .sidebar.directory-open {
            width: 68px;
            min-width: 68px;
            max-width: 68px;
          }
          .secondary-panel {
            opacity: 0;
            visibility: hidden;
            pointer-events: none;
            transform: translateX(-14px);
          }
          .directory-open .secondary-panel {
            opacity: 1;
            visibility: visible;
            pointer-events: auto;
            transform: translateX(0);
          }
          .directory-open .secondary-panel {
            box-shadow: 22px 0 48px rgba(0, 0, 0, 0.36);
          }
          .panel-close {
            display: grid;
          }
        }
        @media (max-width: 380px) {
          .secondary-panel {
            width: calc(100vw - 68px);
          }
          .tags-list {
            grid-template-columns: minmax(0, 1fr);
          }
        }
        @media (prefers-reduced-motion: reduce) {
          .sidebar,
          .secondary-panel,
          .nav-highlight {
            transition-duration: 0.01ms;
          }
        }
      </style>

      <aside class="sidebar${m ? " directory-open" : ""}${v ? " archive-context" : ""}${y ? " about-context" : ""}${b ? " friends-context" : ""}">
        <div class="primary-rail">
          <div class="primary-logo" title="${a(d)}">
            <img src="/images/extracted/login/图形@2x.png" alt="${a(d)}">
          </div>

          <nav class="primary-nav" aria-label="一级导航">
            <a class="primary-item${g ? " active" : ""}"
               href="./#cat=all" data-cat="all" aria-label="首页"
               ${g ? "aria-current=\"page\"" : ""} title="首页 · Home">
              <img src="/images/extracted/home/iconfont-shouye@2x.png" alt="" aria-hidden="true">
              <span class="primary-tooltip">首页</span>
            </a>

            <button class="primary-item directory-toggle${_ ? " active" : ""}"
                    type="button" aria-label="目录" aria-controls="category-panel"
                    aria-expanded="${m ? "true" : "false"}" title="目录 · Categories">
              <svg viewBox="0 0 24 24" aria-hidden="true">
                <path d="M3.5 6.5h6l1.8 2h9.2v9.5a2 2 0 0 1-2 2h-13a2 2 0 0 1-2-2z"></path>
                <path d="M3.5 9h17"></path>
              </svg>
              <span class="primary-tooltip">目录</span>
            </button>

            ${w}
            ${E}
            ${T}
          </nav>

          <a class="rail-caption" href="https://github.com/callmesoul/soul-blog-theme"
             target="_blank" rel="noopener noreferrer"
             title="Soul Blog Theme · 本项目 GitHub" aria-label="Soul Blog Theme · 本项目 GitHub">Soul&nbsp;Blog</a>
        </div>

        <section class="secondary-panel" id="category-panel"
                 aria-label="二级导航" aria-hidden="${h ? "true" : "false"}">
          <header class="secondary-header">
            <div class="secondary-title context-title context-home">
              <small>${a(d)}</small>
              <strong>首页</strong>
              <span>Home</span>
            </div>
            <div class="secondary-title context-title context-directory">
              <small>${a(d)}</small>
              <strong>内容目录</strong>
              <span>Categories</span>
            </div>
            <div class="secondary-title context-title context-archive">
              <small>${a(d)}</small>
              <strong>归档</strong>
              <span>Archives</span>
            </div>
            <div class="secondary-title context-title context-about">
              <small>${a(d)}</small>
              <strong>关于我</strong>
              <span>About</span>
            </div>
            <div class="secondary-title context-title context-friends">
              <small>${a(d)}</small>
              <strong>友链</strong>
              <span>Friends</span>
            </div>
            <button class="panel-close" type="button" aria-label="收起目录" title="收起目录">
              <svg viewBox="0 0 24 24" aria-hidden="true">
                <path d="m15 6-6 6 6 6"></path>
              </svg>
            </button>
          </header>

          <nav class="site-nav" aria-label="目录列表">
            <section class="categories-section" aria-label="分类列表">
              <div class="section-heading">
                <span>分类</span><span>${String(f.length).padStart(2, "0")}</span>
              </div>
              ${x || "<p class=\"section-heading\"><span>暂无分类</span></p>"}
              <span class="nav-highlight" aria-hidden="true"></span>
            </section>

            ${S ? `<section class="tags-section" aria-label="标签列表">
              <h3 class="section-heading">
                <span>标签</span><span>${String(o?.length || 0).padStart(2, "0")}</span>
              </h3>
              <div class="tags-list">${S}</div>
            </section>` : ""}
          </nav>

          <footer class="secondary-footer">
            <div class="icp-text">${s ? a(s) : "@CallMeSoul 粤ICP备15053557"}</div>
            <div class="social-bar" aria-label="社交媒体">${C}</div>
          </footer>
        </section>
      </aside>
    `;
	}
	mounted() {
		this._hlEl = this.$(".nav-highlight"), this._layoutHighlight(), this.$(".directory-toggle")?.addEventListener("click", () => {
			this._setDirectoryOpen(!0);
		}), this.$(".panel-close")?.addEventListener("click", () => {
			this._setDirectoryOpen(!1);
		}), this.$(".primary-item[data-cat=\"all\"]")?.addEventListener("click", (e) => {
			e.preventDefault(), this._setDirectoryOpen(!1), this.emit("navigate", { cat: "all" });
		}), this.$(".site-nav")?.addEventListener("click", (e) => {
			let t = e.target, n = t.closest(".tag-item");
			if (n) {
				let t = n.dataset.tag;
				t && (e.preventDefault(), this.emit("navigate", {
					cat: "all",
					tag: t
				}), this._closeOverlayAfterNavigation());
				return;
			}
			let r = t.closest(".nav-item"), i = r && r.dataset.cat;
			i && (e.preventDefault(), this.emit("navigate", { cat: i }), this._closeOverlayAfterNavigation());
		});
	}
	attributeChangedCallback(e, t, n) {
		if (t !== n) {
			if (e === "active-cat") {
				let e = n || "all", t = this.getAttribute("active-tag") || "";
				this._syncActiveClass(t ? "" : e), [
					"all",
					"archives",
					"about",
					"friends"
				].includes(e) || this._setDirectoryOpen(!0), [
					"all",
					"archives",
					"about",
					"friends"
				].includes(e) && !t && this._setDirectoryOpen(!1);
			}
			if (e === "active-tag") {
				let e = n || "", t = this.getAttribute("active-cat") || "all";
				this._syncTagActiveClass(e), this._syncActiveClass(e ? "" : t), !e && t === "all" && this._setDirectoryOpen(!1);
			}
			this._syncPrimaryState(), this._layoutHighlight();
		}
	}
	set categories(e) {
		this._categories = e, this._reRender();
	}
	set social(e) {
		this._social = e, this._reRender();
	}
	set siteName(e) {
		this._siteName = e, this._reRender();
	}
	set icp(e) {
		this._icp = e, this._reRender();
	}
	set tags(e) {
		this._tags = e, this._reRender();
	}
	set archiveUrl(e) {
		this._archiveUrl = e || "", this._reRender();
	}
	set aboutUrl(e) {
		this._aboutUrl = e || "", this._reRender();
	}
	set friendsUrl(e) {
		this._friendsUrl = e || "", this._reRender();
	}
	_reRender() {
		this.shadow.innerHTML = this.render(), this.mounted();
	}
	_isDirectoryOpen() {
		return !!this.$(".sidebar")?.classList.contains("directory-open");
	}
	_setDirectoryOpen(e) {
		this._directoryOpen = e;
		let t = this.$(".sidebar"), n = this.$(".secondary-panel"), r = this.$(".directory-toggle");
		t?.classList.toggle("directory-open", e);
		let i = window.matchMedia("(max-width: 1199px)").matches && !e;
		n?.setAttribute("aria-hidden", i ? "true" : "false"), r?.setAttribute("aria-expanded", e ? "true" : "false"), this._syncPrimaryState(), e && requestAnimationFrame(() => this._layoutHighlight());
	}
	_syncPrimaryState() {
		let e = this.getAttribute("active-cat") || "all", t = this.getAttribute("active-tag") || "", n = this._isDirectoryOpen(), r = this.$(".primary-item[data-cat=\"all\"]"), i = this.$(".directory-toggle"), a = this.$(".primary-item[data-cat=\"archives\"]"), o = this.$(".primary-item[data-cat=\"about\"]"), s = this.$(".primary-item[data-cat=\"friends\"]");
		r?.classList.toggle("active", e === "all" && !n), i?.classList.toggle("active", n || ![
			"all",
			"archives",
			"about",
			"friends"
		].includes(e)), a?.classList.toggle("active", e === "archives" && !n), o?.classList.toggle("active", e === "about" && !n), s?.classList.toggle("active", e === "friends" && !n);
		let c = this.$(".sidebar");
		if (c) {
			let n = ![
				"all",
				"archives",
				"about",
				"friends"
			].includes(e), r = this._directoryOpen || n && !t;
			c.classList.toggle("archive-context", e === "archives" && !t), c.classList.toggle("about-context", e === "about" && !t), c.classList.toggle("friends-context", e === "friends" && !t), c.classList.toggle("directory-open", r);
		}
	}
	_syncActiveClass(e) {
		this.shadow.querySelectorAll(".nav-item").forEach((t) => {
			t.classList.toggle("active", t.dataset.cat === e);
		});
	}
	_syncTagActiveClass(e) {
		this.shadow.querySelectorAll(".tag-item").forEach((t) => {
			t.classList.toggle("active", t.dataset.tag === e);
		});
	}
	_layoutHighlight() {
		let e = this._hlEl;
		if (!e || !this._isDirectoryOpen() || this.getAttribute("active-tag")) {
			e?.classList.remove("is-on");
			return;
		}
		let t = this.getAttribute("active-cat") || "all", n = Array.from(this.shadow.querySelectorAll(".nav-item")).find((e) => e.dataset.cat === t);
		if (!n) {
			e.classList.remove("is-on");
			return;
		}
		let r = !e.classList.contains("is-on");
		r && e.classList.add("no-anim"), e.style.height = n.offsetHeight + "px", e.style.transform = "translateY(" + n.offsetTop + "px)", e.classList.add("is-on"), r && requestAnimationFrame(() => e.classList.remove("no-anim"));
	}
	_closeOverlayAfterNavigation() {
		window.matchMedia("(max-width: 1199px)").matches && this._setDirectoryOpen(!1);
	}
};
customElements.get("site-sidebar") || customElements.define("site-sidebar", o);
function s(e, t) {
	let n = e.toLowerCase(), r = String(t || "").toLowerCase();
	if (!n || !r) return 0;
	if (r === n) return 120;
	if (r.startsWith(n)) return 100;
	if (r.indexOf(n) >= 0) return 80 + Math.min(n.length, 10);
	let i = 0, a = 0, o = 0;
	for (let e = 0; e < r.length && i < n.length; e++) r[e] === n[i] ? (i++, o++, a += 1 + o) : o = 0;
	return i < n.length ? 0 : a;
}
function c(e, t, n, r) {
	let i = (e || "").trim();
	if (!i) return [];
	let a = [];
	for (let e of t) {
		let t = n[e.cat] || e.cat, r = [
			{
				text: e.title,
				w: 5
			},
			{
				text: e.summary,
				w: 3
			},
			{
				text: t,
				w: 3
			},
			{
				text: (e.paragraphs || []).join(" "),
				w: 1
			}
		], o = 0;
		for (let e of r) {
			let t = s(i, e.text) * e.w;
			t > o && (o = t);
		}
		o > 0 && a.push({
			article: e,
			catName: t,
			score: o
		});
	}
	return a.sort((e, t) => t.score - e.score), r ? a.slice(0, r) : a;
}
function l(e, t) {
	let n = String(e), r = String(t || "").trim().toLowerCase();
	if (!r) return a(n);
	let i = n.toLowerCase().indexOf(r);
	if (i >= 0) return a(n.slice(0, i)) + "<b class=\"search-hl\">" + a(n.slice(i, i + r.length)) + "</b>" + a(n.slice(i + r.length));
	let o = /* @__PURE__ */ new Set(), s = 0;
	for (let e = 0; e < n.length && s < r.length; e++) n[e].toLowerCase() === r[s] && (o.add(e), s++);
	if (s < r.length) return a(n);
	let c = "";
	for (let e = 0; e < n.length; e++) {
		let t = a(n[e]);
		c += o.has(e) ? "<b class=\"search-hl\">" + t + "</b>" : t;
	}
	return c;
}
var u = class extends r {
	static get observedAttributes() {
		return [];
	}
	_articles = [];
	_results = [];
	_activeIndex = 0;
	_catNames = {};
	set articles(e) {
		this._articles = e, this._catNames = {};
	}
	set catNames(e) {
		this._catNames = e;
	}
	render() {
		return "\n      <style>\n        :host {\n          display: none;\n          position: fixed;\n          inset: 0;\n          z-index: 1000;\n        }\n        :host(.is-open) {\n          display: block;\n        }\n        .search-backdrop {\n          position: absolute;\n          inset: 0;\n          background: rgba(0,0,0,0.6);\n        }\n        .search-dialog {\n          position: absolute;\n          top: 18%;\n          left: 50%;\n          transform: translateX(-50%);\n          width: 600px;\n          max-width: 90vw;\n          background: #0f0e0d;\n          border: 1px solid #333;\n          border-radius: 8px;\n          overflow: hidden;\n          box-shadow: 0 16px 48px rgba(0,0,0,0.5);\n        }\n        .search-box {\n          display: flex;\n          align-items: center;\n          gap: 12px;\n          padding: 16px 20px;\n          border-bottom: 1px solid #2a2a2a;\n        }\n        .search-box svg {\n          flex-shrink: 0;\n          color: #6b6b6b;\n        }\n        .search-input {\n          flex: 1;\n          min-width: 0;\n          background: none;\n          border: none;\n          outline: none;\n          font-size: 15px;\n          line-height: 1.6;\n          color: #f2f2f2;\n          font-family: inherit;\n        }\n        .search-input::placeholder {\n          color: #6b6b6b;\n        }\n        .search-box kbd {\n          font-size: 11px;\n          color: #6b6b6b;\n          background: #1d1d1d;\n          padding: 2px 6px;\n          border-radius: 3px;\n          font-family: inherit;\n        }\n        .search-results {\n          max-height: 400px;\n          overflow-y: auto;\n        }\n        .search-result {\n          display: flex;\n          align-items: center;\n          gap: 12px;\n          width: 100%;\n          padding: 10px 20px;\n          border: none;\n          background: none;\n          cursor: pointer;\n          text-align: left;\n          font-family: inherit;\n          font-size: 14px;\n          color: #f2f2f2;\n          transition: background 0.15s;\n        }\n        .search-result:hover,\n        .search-result.active {\n          background: rgba(255,255,255,0.05);\n        }\n        .search-result-cover {\n          width: 40px;\n          height: 40px;\n          object-fit: cover;\n          border-radius: 3px;\n          flex-shrink: 0;\n        }\n        .search-result-main {\n          flex: 1;\n          min-width: 0;\n        }\n        .search-result-title {\n          font-size: 14px;\n          line-height: 1.5;\n          color: #f2f2f2;\n          white-space: nowrap;\n          overflow: hidden;\n          text-overflow: ellipsis;\n        }\n        .search-hl {\n          color: #eb4f38;\n          font-weight: 500;\n        }\n        .search-result-desc {\n          font-size: 12px;\n          color: #6b6b6b;\n          line-height: 1.4;\n          white-space: nowrap;\n          overflow: hidden;\n          text-overflow: ellipsis;\n        }\n        .search-result-side {\n          flex-shrink: 0;\n          text-align: right;\n        }\n        .search-result-cat {\n          display: block;\n          font-size: 11px;\n          color: #9e9d99;\n        }\n        .search-result-date {\n          display: block;\n          font-size: 11px;\n          color: #6b6b6b;\n          margin-top: 2px;\n        }\n        .search-empty {\n          padding: 32px 20px;\n          text-align: center;\n          color: #6b6b6b;\n          font-size: 14px;\n        }\n        .search-foot {\n          display: flex;\n          gap: 16px;\n          padding: 10px 20px;\n          border-top: 1px solid #2a2a2a;\n          font-size: 12px;\n          color: #6b6b6b;\n        }\n        .search-foot kbd {\n          font-size: 11px;\n          color: #9e9d99;\n          background: #1d1d1d;\n          padding: 1px 5px;\n          border-radius: 2px;\n          font-family: inherit;\n        }\n      </style>\n      <div class=\"search-backdrop\" data-part=\"backdrop\"></div>\n      <div class=\"search-dialog\" role=\"dialog\" aria-modal=\"true\" aria-label=\"站内搜索\">\n        <div class=\"search-box\">\n          <svg width=\"17\" height=\"17\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.8\" stroke-linecap=\"round\" stroke-linejoin=\"round\">\n            <circle cx=\"11\" cy=\"11\" r=\"7\"></circle><path d=\"m21 21-4.3-4.3\"></path>\n          </svg>\n          <input class=\"search-input\" type=\"text\" placeholder=\"搜索文章、目录、正文关键词…\" autocomplete=\"off\" spellcheck=\"false\">\n          <kbd>Esc</kbd>\n        </div>\n        <div class=\"search-results\" data-part=\"results\"></div>\n        <div class=\"search-foot\">\n          <span><kbd>↑</kbd><kbd>↓</kbd> 选择</span>\n          <span><kbd>Enter</kbd> 打开</span>\n          <span><kbd>Esc</kbd> 关闭</span>\n        </div>\n      </div>\n    ";
	}
	mounted() {
		let e = this.$(".search-input"), t = this.$("[data-part=\"results\"]"), n = this.$("[data-part=\"backdrop\"]");
		e && t && (e.addEventListener("input", () => {
			this._results = c(e.value, this._articles, this._catNames, 12), this._activeIndex = 0, this._renderResults(e.value);
		}), e.addEventListener("keydown", (t) => {
			if (t.key === "Escape") {
				this._close();
				return;
			}
			if (t.key === "ArrowDown" || t.key === "ArrowUp") {
				if (!this._results.length) return;
				t.preventDefault();
				let e = t.key === "ArrowDown" ? 1 : -1;
				this._activeIndex = (this._activeIndex + e + this._results.length) % this._results.length, this._syncActive();
				return;
			}
			if (t.key === "Enter") {
				t.preventDefault();
				let n = this._results[this._activeIndex];
				if (n) this._openResult(n);
				else {
					let t = e.value.trim();
					this._close(), t && (window.location.href = "search.html?q=" + encodeURIComponent(t));
				}
			}
		}), t.addEventListener("click", (e) => {
			let t = e.target.closest(".search-result");
			if (!t) return;
			let n = t.dataset.id, r = t.dataset.cat;
			n && (this._close(), this.emit("search-select", {
				id: n,
				cat: r
			}));
		}), t.addEventListener("mousemove", (e) => {
			let n = e.target.closest(".search-result");
			if (!n) return;
			let r = Array.from(t.querySelectorAll(".search-result")).indexOf(n);
			r >= 0 && r !== this._activeIndex && (this._activeIndex = r, this._syncActive());
		}), n?.addEventListener("click", (e) => {
			e.target === n && this._close();
		}), document.addEventListener("keydown", (e) => {
			(e.ctrlKey || e.metaKey) && e.shiftKey && (e.key === "F" || e.key === "f") && (e.preventDefault(), this._open());
		}), document.addEventListener("click", (e) => {
			(e.composedPath()[0] || e.target).closest("[data-act=\"search\"]") && this._open();
		}));
	}
	_renderResults(e) {
		let t = this.$("[data-part=\"results\"]");
		if (!t) return;
		let n = e.trim();
		if (!n) {
			t.innerHTML = "<div class=\"search-empty\">输入关键词，模糊搜索文章 / 目录 / 正文内容</div>";
			return;
		}
		if (!this._results.length) {
			t.innerHTML = "<div class=\"search-empty\">没有找到与「" + a(n) + "」相关的内容</div>";
			return;
		}
		t.innerHTML = this._results.map((e) => {
			let t = e.article;
			return `
        <button class="search-result${this._results.indexOf(e) === this._activeIndex ? " active" : ""}" type="button"
                data-id="${a(t.id)}" data-cat="${a(t.cat)}">
          <img class="search-result-cover" src="${a(t.cover)}" alt="">
          <div class="search-result-main">
            <p class="search-result-title">${l(t.title, n)}</p>
            <p class="search-result-desc">${a(t.summary)}</p>
          </div>
          <div class="search-result-side">
            <span class="search-result-cat">${a(e.catName || t.cat)}</span>
            <span class="search-result-date">${a(t.date)}</span>
          </div>
        </button>
      `;
		}).join("");
	}
	_syncActive() {
		let e = this.$("[data-part=\"results\"]");
		if (!e) return;
		let t = Array.from(e.querySelectorAll(".search-result"));
		t.forEach((e, t) => e.classList.toggle("active", t === this._activeIndex));
		let n = t[this._activeIndex];
		n && n.scrollIntoView && n.scrollIntoView({ block: "nearest" });
	}
	_open() {
		this.classList.add("is-open");
		let e = this.$(".search-input");
		e && (e.value = "", this._results = [], this._activeIndex = 0, this._renderResults(""), requestAnimationFrame(() => e.focus()));
	}
	_close() {
		this.classList.remove("is-open");
	}
	_openResult(e) {
		this._close(), this.emit("search-select", {
			id: e.article.id,
			cat: e.article.cat
		});
	}
};
customElements.get("search-panel") || customElements.define("search-panel", u);
var d = (() => {
	let e = {};
	try {
		let e = "__soul_blog_test__";
		return localStorage.setItem(e, "1"), localStorage.removeItem(e), {
			get(e, t) {
				try {
					let n = localStorage.getItem(e);
					return n == null ? t : JSON.parse(n);
				} catch {
					return t;
				}
			},
			set(e, t) {
				try {
					localStorage.setItem(e, JSON.stringify(t));
				} catch {}
			}
		};
	} catch {
		return {
			get(t, n) {
				return t in e ? e[t] : n;
			},
			set(t, n) {
				e[t] = n;
			}
		};
	}
})(), f = class extends r {
	static get observedAttributes() {
		return [];
	}
	render() {
		return "\n      <style>\n        :host {\n          display: flex;\n          flex-direction: column;\n          align-items: center;\n          justify-content: center;\n          min-height: 100%;\n          padding: 40px 20px;\n        }\n        .login-icon {\n          position: relative;\n          z-index: 1;\n          margin-bottom: -30px;\n          display: block;\n          width: 88px;\n          height: 81px;\n          object-fit: contain;\n        }\n        .login-card {\n          width: 357px;\n          overflow: hidden;\n          background: #211e1c;\n        }\n        .login-form {\n          padding: 48px 36px 44px;\n        }\n        .input-row {\n          display: flex;\n          align-items: center;\n          gap: 14px;\n          border-bottom: 1px solid #2d2927;\n          padding: 8px 0;\n          margin-bottom: 16px;\n          transition: border-color 0.2s;\n        }\n        .input-row:focus-within {\n          border-bottom-color: var(--brand-primary, #eb4f38);\n        }\n        .input-row img {\n          width: 13px;\n          height: 13px;\n          object-fit: contain;\n          flex-shrink: 0;\n          filter: brightness(0) invert(0.62);\n          transition: filter 0.2s;\n        }\n        .input-row:focus-within img {\n          filter: brightness(0) invert(1);\n        }\n        .input-row input {\n          min-width: 0;\n          flex: 1;\n          background: transparent;\n          border: none;\n          outline: none;\n          font-size: 14px;\n          line-height: 1.6;\n          letter-spacing: 0.01em;\n          color: #f2f2f2;\n          font-family: inherit;\n        }\n        .input-row input::placeholder {\n          color: #6b6b6b;\n        }\n        .input-row input:focus::placeholder {\n          color: #9e9d99;\n        }\n        .input-row input[type=\"password\"]:not(:placeholder-shown) {\n          letter-spacing: 0.25em;\n        }\n        .options-row {\n          display: flex;\n          align-items: center;\n          justify-content: space-between;\n          padding: 16px 0;\n        }\n        .checkbox-wrap {\n          display: flex;\n          align-items: center;\n          gap: 8px;\n          cursor: pointer;\n          user-select: none;\n        }\n        .checkbox-box {\n          display: flex;\n          width: 18px;\n          height: 18px;\n          flex-shrink: 0;\n          align-items: center;\n          justify-content: center;\n          border-radius: 1px;\n          border: 1px solid #4a4542;\n          background: transparent;\n          transition: background 0.2s, border-color 0.2s;\n        }\n        .checkbox-box[data-checked=\"1\"] {\n          border-color: var(--brand-primary, #eb4f38);\n          background: var(--brand-primary, #eb4f38);\n        }\n        .checkbox-label {\n          font-size: 12px;\n          line-height: 1.6;\n          letter-spacing: 0.03em;\n          color: #9e9d99;\n        }\n        .forgot-link {\n          font-size: 12px;\n          line-height: 1.6;\n          letter-spacing: 0.03em;\n          color: #6b6b6b;\n          text-decoration: none;\n          transition: color 0.2s;\n        }\n        .forgot-link:hover {\n          color: #ffffff;\n        }\n        .btn-login {\n          display: block;\n          width: 100%;\n          height: 44px;\n          margin-bottom: 10px;\n          cursor: pointer;\n          border: none;\n          border-radius: 3px;\n          background: var(--brand-cta, #ee5b44);\n          color: #f2f2f2;\n          font-size: 14px;\n          font-weight: 600;\n          letter-spacing: 0.15em;\n          font-family: inherit;\n          transition: all 0.2s ease-out;\n        }\n        .btn-login:hover {\n          transform: translateY(-1px);\n          filter: brightness(1.1);\n          box-shadow: 0 6px 16px rgba(238, 91, 68, 0.25);\n        }\n        .btn-login:disabled {\n          cursor: not-allowed;\n          opacity: 0.7;\n        }\n        .btn-register {\n          display: block;\n          width: 100%;\n          height: 44px;\n          cursor: pointer;\n          border: 1px solid #363230;\n          border-radius: 3px;\n          background: #292623;\n          color: #8a8784;\n          font-size: 14px;\n          font-weight: 500;\n          letter-spacing: 0.15em;\n          font-family: inherit;\n          transition: all 0.2s;\n        }\n        .btn-register:hover {\n          transform: translateY(-1px);\n          background: #322e2a;\n          color: #9e9d99;\n        }\n        .form-tip {\n          text-align: center;\n          color: #eb4f38;\n          font-size: 13px;\n          margin-top: 12px;\n          opacity: 0;\n          transition: opacity 0.3s;\n        }\n        .form-tip.show {\n          opacity: 1;\n        }\n        .pwd-toggle {\n          cursor: pointer;\n          display: flex;\n          align-items: center;\n          padding: 4px;\n          opacity: 0.5;\n          transition: opacity 0.2s;\n        }\n        .pwd-toggle:hover {\n          opacity: 1;\n        }\n      </style>\n      <img src=\"/images/extracted/login/图形@2x.png\" alt=\"\" class=\"login-icon\">\n\n      <div class=\"login-card\">\n        <form class=\"login-form\" novalidate>\n          <div class=\"input-row\">\n            <img src=\"/images/extracted/login/iconfont-denglu@2x.png\" alt=\"\">\n            <input type=\"text\" class=\"username-input\" placeholder=\"请输入用户名\" autocomplete=\"username\">\n          </div>\n\n          <div class=\"input-row\">\n            <img src=\"/images/extracted/login/iconfont-iconyaochi@2x.png\" alt=\"\">\n            <input type=\"password\" class=\"password-input\" placeholder=\"请输入密码\" autocomplete=\"current-password\">\n          </div>\n\n          <div class=\"options-row\">\n            <label class=\"checkbox-wrap\">\n              <input type=\"checkbox\" class=\"remember-input\" checked style=\"position:absolute;opacity:0;width:1px;height:1px;\">\n              <span class=\"checkbox-box\" data-checked=\"1\">\n                <svg width=\"12\" height=\"12\" viewBox=\"0 0 12 12\" fill=\"none\"><path d=\"M2.5 6L5 8.5L9.5 3.5\" stroke=\"#FFFFFF\" stroke-width=\"1.6\" stroke-linecap=\"round\" stroke-linejoin=\"round\" fill=\"none\"/></svg>\n              </span>\n              <span class=\"checkbox-label\">自动登录</span>\n            </label>\n            <a href=\"#\" class=\"forgot-link\">忘记密码</a>\n          </div>\n\n          <button type=\"submit\" class=\"btn-login\">登录</button>\n          <button type=\"button\" class=\"btn-register\" data-act=\"register\">注册</button>\n        </form>\n        <p class=\"form-tip\"></p>\n      </div>\n    ";
	}
	mounted() {
		this._setupForm(), this._setupPasswordToggle(), this._setupCheckbox(), this._setupForgotPassword(), this._setupRegister(), this._fillRememberedUsername();
	}
	_setupForm() {
		let e = this.$(".login-form");
		e && e.addEventListener("submit", (t) => {
			t.preventDefault();
			let n = this.$(".username-input")?.value.trim() || "", r = this.$(".password-input")?.value || "", i = this.$(".remember-input")?.checked || !1;
			if (!n || !r) {
				this._flashError("请填写用户名和密码");
				return;
			}
			if (r.length < 4) {
				this._flashError("密码长度至少 4 位");
				return;
			}
			i && d.set("auth:remembered-username", n), d.set("auth:last-user", n);
			let a = e.querySelector(".btn-login");
			a && (a.disabled = !0, a.textContent = "登录中…"), this.emit("login-submit", {
				username: n,
				password: r,
				remember: i
			});
		});
	}
	_setupPasswordToggle() {
		let e = this.$(".password-input");
		if (!e) return;
		let t = e.closest(".input-row");
		if (!t) return;
		let n = document.createElement("span");
		n.className = "pwd-toggle", n.title = "显示/隐藏密码", n.innerHTML = "<svg viewBox=\"0 0 16 16\" width=\"14\" height=\"14\" fill=\"none\"><path d=\"M1 8s2.5-5 7-5 7 5 7 5-2.5 5-7 5S1 8 1 8z\" stroke=\"currentColor\" stroke-width=\"1.2\"/><circle cx=\"8\" cy=\"8\" r=\"2\" stroke=\"currentColor\" stroke-width=\"1.2\"/></svg>", n.addEventListener("click", () => {
			e.type = e.type === "password" ? "text" : "password", n.classList.toggle("show", e.type === "text");
		}), t.appendChild(n);
	}
	_setupCheckbox() {
		let e = this.$(".remember-input"), t = this.$(".checkbox-box");
		e && t && (e.checked = d.get("auth:remember-login", !0), t.dataset.checked = e.checked ? "1" : "0", e.addEventListener("change", () => {
			t.dataset.checked = e.checked ? "1" : "0", d.set("auth:remember-login", e.checked);
		}));
	}
	_setupForgotPassword() {
		let e = this.$(".forgot-link");
		e && e.addEventListener("click", (e) => {
			e.preventDefault(), this.emit("forgot-password"), this._flashError("请联系管理员重置密码", 2400);
		});
	}
	_setupRegister() {
		let e = this.$("[data-act=\"register\"]");
		e && e.addEventListener("click", () => {
			this.emit("register"), this._flashError("注册功能即将上线，敬请期待", 2400);
		});
	}
	_fillRememberedUsername() {
		let e = this.$(".username-input");
		if (!e) return;
		let t = d.get("auth:remembered-username", null);
		t && !e.value && (e.value = t);
	}
	_flashError(e, t = 1800) {
		let n = this.$(".form-tip");
		n && (n.textContent = e, n.classList.add("show"), clearTimeout(n._t), n._t = setTimeout(() => n.classList.remove("show"), t));
	}
};
customElements.get("login-panel") || customElements.define("login-panel", f);
var p = "https://events.vercount.one/api/v2/log", m = "vercount-page-view", h = 5e3, g = /* @__PURE__ */ new Map(), _ = !1, v = 0, y = null;
function b(e) {
	let t = Number(e);
	return Number.isFinite(t) && t >= 0 ? Math.floor(t) : 0;
}
function x(e) {
	if (e.status === "error") return null;
	let t = e.data || e;
	return {
		sitePv: b(t.site_pv),
		siteUv: b(t.site_uv),
		pagePv: b(t.page_pv)
	};
}
function S() {
	return `vercount_uv_${window.location.host.replace(/[^a-zA-Z0-9_-]/g, "_")}`;
}
function C() {
	let e = S(), t = document.cookie.split("; ").some((t) => t.startsWith(`${e}=`));
	return t || (document.cookie = `${e}=1; path=/; max-age=31536000; samesite=lax`), !t;
}
async function w(e, t) {
	let n = new AbortController(), r = window.setTimeout(() => n.abort(), h);
	try {
		let r = await fetch(t ? p : `${p}?url=${encodeURIComponent(e)}`, t ? {
			method: "POST",
			headers: { "Content-Type": "application/json" },
			body: JSON.stringify({
				url: e,
				isNewUv: C()
			}),
			signal: n.signal
		} : {
			method: "GET",
			signal: n.signal
		});
		return r.ok ? x(await r.json()) : null;
	} catch {
		return null;
	} finally {
		window.clearTimeout(r);
	}
}
function T(e, t) {
	let n = document.getElementById(e);
	n && (n.textContent = String(t), n.classList.remove("is-loading"), n.removeAttribute("aria-busy"));
}
function E(e) {
	let t = document.getElementById(e);
	t?.classList.contains("is-loading") && (t.textContent = "—", t.classList.remove("is-loading"), t.removeAttribute("aria-busy"));
}
function D() {
	E("vercount_value_site_pv"), E("vercount_value_site_uv");
}
function O(e) {
	T("vercount_value_site_pv", e.sitePv), T("vercount_value_site_uv", e.siteUv);
}
function ee(e) {
	O(e), T("vercount_value_page_pv", e.pagePv);
}
function k(e) {
	return new URL(`/articles/${encodeURIComponent(e)}`, window.location.origin).href;
}
async function te(e) {
	_ = !0;
	let t = ++v;
	y !== null && (window.clearTimeout(y), y = null);
	let n = k(e), r = await w(n, !0);
	return r ? (t === v && O(r), g.set(n, Promise.resolve(r.pagePv)), window.dispatchEvent(new CustomEvent(m, { detail: {
		articleId: e,
		views: r.pagePv
	} })), r.pagePv) : (D(), null);
}
function ne(e) {
	let t = k(e), n = g.get(t);
	if (n) return n;
	let r = w(t, !1).then((e) => (e || g.delete(t), e?.pagePv ?? null));
	return g.set(t, r), r;
}
function re() {
	if (typeof window > "u") return;
	let e = () => {
		/(?:^|[&#])art=/.test(window.location.hash) || (y = window.setTimeout(async () => {
			if (y = null, _) return;
			let e = await w(window.location.href, !0);
			_ || (e ? ee(e) : D());
		}, 150));
	};
	document.readyState === "loading" ? document.addEventListener("DOMContentLoaded", e, { once: !0 }) : e();
}
var ie = class extends r {
	static get observedAttributes() {
		return ["active-cat", "active-tag"];
	}
	_articles = [];
	_icons = {};
	_pageSize = 6;
	_shown = 0;
	_done = !1;
	_loading = !1;
	_observer = null;
	_catName = "全部文章";
	_totalCount = 0;
	_activeTag = "";
	_remoteViews = /* @__PURE__ */ new Map();
	_vercountListenerBound = !1;
	set articles(e) {
		this._articles = e, this._resetList(), this._renderPage(), this._loadMore();
	}
	set icons(e) {
		this._icons = e;
	}
	set catName(e) {
		this._catName = e;
	}
	set totalCount(e) {
		this._totalCount = e;
	}
	set activeTag(e) {
		this._activeTag = e, this.setAttribute("active-tag", e);
	}
	static articleCardTemplate(e, { href: t, icons: n }) {
		let r = (e.tags || []).map((e) => `<span class="card-tag" data-tag="${a(e)}">${a(e)}</span>`).join("");
		return n.tag, `
      <a class="article-card" href="${a(t)}" data-id="${a(e.id)}" data-cat="${a(e.cat)}">
        <img class="card-thumb" src="${a(e.cover)}" alt="" loading="lazy">
        <div class="card-body">
          <h3 class="card-title">${a(e.title)}</h3>
          <p class="card-desc">${a(e.summary)}</p>
          ${r ? `<div class="card-tags">${r}</div>` : ""}
          <div class="card-meta">
            <span class="meta-item"><img class="meta-icon" src="${a(n.eye)}" alt=""><span class="is-loading" data-part="article-views" aria-busy="true">${a(e.views)}</span></span>
            <span class="meta-item"><img class="meta-icon" src="${a(n.comment)}" alt="">${a(e.commentCount)}</span>
            <span class="meta-item" style="margin-left:auto;"><img class="meta-icon" src="${a(n.calendar)}" alt="">${a(e.date)}</span>
          </div>
        </div>
      </a>
    `;
	}
	render() {
		let e = this._icons.home || "/images/extracted/home/iconfont-shouye@2x.png";
		return `
      <style>
        :host {
          display: contents;
        }
        .list-header {
          display: flex;
          align-items: center;
          padding: 40px 59px 20px;
        }
        @media (max-width: 700px) {
          .list-header { padding: 40px 20px 20px; }
        }
        @media (max-width: 480px) {
          .list-header { padding: 40px 14px 20px; }
        }
        .home-icon {
          display: inline-block;
          width: 14px;
          height: 14px;
          flex-shrink: 0;
          background: var(--brand-primary, #eb4f38);
          mask: url(${e}) center/contain no-repeat;
          -webkit-mask: url(${e}) center/contain no-repeat;
        }
        .home-link {
          margin-left: 8px;
          font-size: 14px;
          letter-spacing: 0.02em;
          color: #c9c6c2;
          text-decoration: none;
          transition: color 0.2s;
        }
        .home-link:hover {
          color: #ffffff;
        }
        .crumb-sep {
          margin: 0 8px;
          font-size: 12px;
          color: #5d5a59;
        }
        .crumb-current {
          font-size: 14px;
          font-weight: 500;
          letter-spacing: 0.02em;
          color: #ffffff;
        }
        .header-divider {
          margin-left: 36px;
          height: 1px;
          min-width: 20px;
          flex: 1;
          background: linear-gradient(90deg, #3c3936, transparent);
        }
        .search-trigger {
          display: inline-flex;
          align-items: center;
          gap: 7px;
          margin-left: 14px;
          padding: 7px 11px;
          border: 1px solid #2A2A2A;
          border-radius: 8px;
          background: rgba(255, 255, 255, 0.03);
          color: #9E9D99;
          cursor: pointer;
          font-family: inherit;
          font-size: 12px;
          letter-spacing: 0.02em;
          white-space: nowrap;
          flex-shrink: 0;
          transition: background 0.2s ease, color 0.2s ease, border-color 0.2s ease;
        }
        .search-trigger:hover {
          background: rgba(255, 255, 255, 0.06);
          color: #FFFFFF;
          border-color: rgba(var(--brand-rgb), 0.5);
        }
        .search-trigger:active { transform: scale(0.97); }
        .search-trigger svg { display: block; color: #6B6B6B; transition: color 0.2s ease; }
        .search-trigger:hover svg { color: var(--brand-primary); }
        .search-trigger-keys {
          display: inline-flex;
          gap: 3px;
          margin-left: 2px;
        }
        .search-trigger kbd {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          min-width: 18px;
          height: 18px;
          padding: 0 5px;
          border: 1px solid #3C3936;
          border-bottom-width: 2px;
          border-radius: 4px;
          background: #1d1d1d;
          color: #9E9D99;
          font-family: inherit;
          font-size: 10px;
          line-height: 1;
          letter-spacing: 0.05em;
        }
        .article-area {
          flex: 1;
          display: flex;
          flex-direction: column;
          overflow-y: auto;
          scrollbar-width: thin;
          scrollbar-color: #444 transparent;
          padding: 54px 59px 48px;
        }
        .article-area::-webkit-scrollbar {
          width: 6px;
        }
        .article-area::-webkit-scrollbar-track {
          background: transparent;
        }
        .article-area::-webkit-scrollbar-thumb {
          background: #444;
          border-radius: 3px;
        }
        .article-area::-webkit-scrollbar-thumb:hover {
          background: #555;
        }
        @media (max-width: 700px) {
          .article-area { padding: 54px 20px 48px; }
        }
        @media (max-width: 480px) {
          .article-area { padding: 54px 14px 48px; }
        }
        .article-grid {
	          flex: 1;
	          display: grid;
	          grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
	          gap: 16px;
	          align-content: start;
	        }
        .article-card {
          display: flex;
          flex-direction: column;
          background: #0f0e0d;
          border: 1px solid #2a2a2a;
          border-radius: 4px;
          overflow: hidden;
          text-decoration: none;
          transition: border-color 0.2s, transform 0.2s;
          cursor: pointer;
        }
        .article-card:hover {
          border-color: #eb4f38;
          transform: translateY(-2px);
        }
        .card-thumb {
          width: 100%;
          height: 160px;
          object-fit: cover;
        }
        .card-body {
          padding: 14px 16px 16px;
        }
        .card-title {
          font-size: 15px;
          font-weight: 500;
          line-height: 1.5;
          color: #ffffff;
          margin: 0 0 8px;
          display: -webkit-box;
          -webkit-line-clamp: 2;
          -webkit-box-orient: vertical;
          overflow: hidden;
        }
        .card-desc {
          font-size: 13px;
          line-height: 1.5;
          color: #9e9d99;
          margin: 0 0 12px;
          display: -webkit-box;
          -webkit-line-clamp: 2;
          -webkit-box-orient: vertical;
          overflow: hidden;
        }
        .card-tags {
          display: flex;
          flex-wrap: wrap;
          gap: 6px;
          margin-bottom: 10px;
        }
        .card-tag {
          display: inline-block;
          padding: 2px 8px;
          font-size: 11px;
          line-height: 1.5;
          color: #9e9d99;
          background: rgba(255,255,255,0.05);
          border: 1px solid #2a2a2a;
          border-radius: 3px;
          cursor: pointer;
          transition: color 0.2s, border-color 0.2s, background 0.2s;
        }
        .card-tag:hover {
          color: #eb4f38;
          border-color: #eb4f38;
          background: rgba(235,79,56,0.08);
        }
        .card-meta {
          display: flex;
          align-items: center;
          gap: 14px;
          font-size: 12px;
          color: #9e9d99;
        }
        .meta-item {
          display: flex;
          align-items: center;
          gap: 4px;
        }
        [data-part="article-views"] {
          display: inline-flex;
          min-width: 1.2em;
          align-items: center;
          justify-content: center;
        }
        [data-part="article-views"].is-loading {
          width: 12px;
          height: 12px;
          min-width: 12px;
          color: transparent;
          font-size: 0;
        }
        [data-part="article-views"].is-loading::after {
          content: '';
          width: 8px;
          height: 8px;
          border: 1.5px solid rgba(158,157,153,0.28);
          border-top-color: var(--brand-primary, #eb4f38);
          border-radius: 50%;
          animation: viewCountSpin 0.72s linear infinite;
        }
        .meta-icon {
          display: block;
          width: 16px;
          height: 12px;
          object-fit: contain;
          filter: brightness(0) invert(0.62);
          transition: filter 0.2s ease;
        }
        .article-card:hover .card-meta .meta-icon {
          filter: brightness(0) invert(0.78);
        }
        @keyframes viewCountSpin {
          to { transform: rotate(360deg); }
        }
        @media (prefers-reduced-motion: reduce) {
          [data-part="article-views"].is-loading::after { animation: none; }
        }
        .list-status {
          text-align: center;
          padding: 16px;
          font-size: 13px;
          color: #6b6b6b;
        }
        .list-status[data-state="hidden"] {
          display: none;
        }
        .grid-empty {
          text-align: center;
          padding: 80px 20px;
          color: #6b6b6b;
          font-size: 14px;
          animation: fadeInUp 0.5s ease both;
          grid-column: 1 / -1;
          margin: auto;
        }
        .empty-icon {
          width: 90px;
          height: 90px;
          margin: 0 auto 24px;
          border-radius: 50%;
          background: rgba(255, 255, 255, 0.05);
          display: flex;
          align-items: center;
          justify-content: center;
          border: 1px solid rgba(255, 255, 255, 0.08);
        }
        .empty-icon svg {
          width: 45px;
          height: 45px;
          color: #6b6b6b;
        }
        .empty-title {
          font-size: 18px;
          color: #9e9d99;
          margin-bottom: 8px;
          font-weight: 500;
        }
        .empty-desc {
          font-size: 14px;
          color: #6b6b6b;
          margin-bottom: 24px;
        }
        .empty-btn {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          padding: 10px 20px;
          border: 1px solid #2a2a2a;
          border-radius: 8px;
          background: rgba(235, 79, 56, 0.08);
          color: #eb4f38;
          font-size: 14px;
          cursor: pointer;
          transition: all 0.2s ease;
        }
        .empty-btn:hover {
          background: rgba(235, 79, 56, 0.15);
          border-color: #eb4f38;
          transform: translateY(-1px);
        }
        .empty-btn:active {
          transform: translateY(0);
        }
        @keyframes fadeInUp {
          from {
            opacity: 0;
            transform: translateY(10px);
          }
          to {
            opacity: 1;
            transform: translateY(0);
          }
        }
      </style>
      <div class="list-header">
        <span class="home-icon"></span>
        <a href="./" class="home-link">首页</a>
        <span class="crumb-sep">&gt;</span>
        <span class="crumb-current" data-part="crumb">${a(this._catName)}${this._totalCount ? " · 共 " + this._totalCount + " 篇" : ""}</span>
        <div class="header-divider"></div>
        <button class="search-trigger" type="button" data-act="search" aria-label="搜索（Ctrl+Shift+F）">
          <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="11" cy="11" r="7"></circle><path d="m21 21-4.3-4.3"></path>
          </svg>
          <span>搜索</span>
          <span class="search-trigger-keys"><kbd>Ctrl</kbd><kbd>Shift</kbd><kbd>F</kbd></span>
        </button>
      </div>

      <div class="article-area" data-part="area">
        <div class="article-grid" data-part="grid" aria-live="polite"></div>
        <div class="list-status" data-part="status" data-state="ready"></div>
      </div>
    `;
	}
	mounted() {
		this._setupIntersectionObserver(), this._setupCardClick(), this._setupVercountUpdates(), this._resetList(), this._renderPage(), this._loadMore();
	}
	_setupIntersectionObserver() {
		this._observer && this._observer.disconnect();
		let e = this.$("[data-part=\"area\"]"), t = this.$("[data-part=\"status\"]");
		t && (this._observer = new IntersectionObserver((e) => {
			e.forEach((e) => {
				e.isIntersecting && this._loadMore();
			});
		}, {
			root: e,
			rootMargin: "0px"
		}), this._observer.observe(t));
	}
	_setupCardClick() {
		let e = this.$("[data-part=\"grid\"]");
		e && e.addEventListener("click", (e) => {
			if (e.defaultPrevented) return;
			let t = e.target;
			if (t.closest("[data-act=\"empty-browse\"]")) {
				this.emit("article-select", {
					id: "",
					cat: "all"
				});
				return;
			}
			let n = t.closest(".card-tag");
			if (n) {
				e.preventDefault();
				let t = n.dataset.tag;
				t && this.emit("tag-select", { tag: t });
				return;
			}
			let r = t.closest(".article-card");
			if (!r) return;
			let i = r.dataset.id;
			i && (e.preventDefault(), this.emit("article-select", { id: i }));
		});
	}
	_setupVercountUpdates() {
		this._vercountListenerBound || (this._vercountListenerBound = !0, window.addEventListener("vercount-page-view", ((e) => {
			let t = String(e.detail?.articleId || ""), n = Number(e.detail?.views);
			t && Number.isFinite(n) && this._applyRemoteViews(t, n);
		})));
	}
	_applyRemoteViews(e, t) {
		this._remoteViews.set(e, t), this.$$(".article-card").forEach((n) => {
			if (n.dataset.id !== e) return;
			let r = n.querySelector("[data-part=\"article-views\"]");
			r && (r.textContent = String(t), r.classList.remove("is-loading"), r.removeAttribute("aria-busy"));
		});
	}
	_finishRemoteViewLoading(e) {
		this.$$(".article-card").forEach((t) => {
			if (t.dataset.id !== e) return;
			let n = t.querySelector("[data-part=\"article-views\"]");
			n?.classList.remove("is-loading"), n?.removeAttribute("aria-busy");
		});
	}
	_hydrateVisibleViews() {
		this.$$(".article-card").forEach((e) => {
			let t = e.dataset.id || "";
			t && !this._remoteViews.has(t) && ne(t).then((e) => {
				this._remoteViews.has(t) || (e === null ? this._finishRemoteViewLoading(t) : this._applyRemoteViews(t, e));
			});
		});
	}
	_viewsFor(e) {
		return this._remoteViews.get(e.id) ?? e.views;
	}
	_resetList() {
		this._shown = 0, this._loading = !1, this._done = !1;
	}
	_renderPage() {
		let e = this.$("[data-part=\"grid\"]"), t = this.$("[data-part=\"status\"]");
		if (!e) return;
		let n = this.getAttribute("active-cat") || "all", r = this.getAttribute("active-tag") || "", i = n === "all" ? this._articles.slice() : this._articles.filter((e) => e.cat === n);
		r && (i = i.filter((e) => e.tags && e.tags.includes(r)));
		let o = i.sort((e, t) => t.date.localeCompare(e.date));
		if (!o.length) {
			e.innerHTML = "<div class=\"grid-empty\"><div class=\"empty-icon\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><path d=\"M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z\"/><polyline points=\"14 2 14 8 20 8\"/><line x1=\"16\" y1=\"13\" x2=\"8\" y2=\"13\"/><line x1=\"16\" y1=\"17\" x2=\"8\" y2=\"17\"/></svg></div><div class=\"empty-title\">空空如也</div><div class=\"empty-desc\">该分类下还没有文章，去其他目录逛逛吧～</div><button class=\"empty-btn\" type=\"button\" data-act=\"empty-browse\">看看其他分类 &rarr;</button></div>", t && (t.dataset.state = "hidden");
			return;
		}
		e.innerHTML = o.slice(0, this._pageSize).map((e) => {
			let t = this._icons, n = (e.tags || []).map((e) => `<span class="card-tag" data-tag="${a(e)}">${a(e)}</span>`).join("");
			return `
        <a class="article-card" href="#" data-id="${a(e.id)}" data-cat="${a(e.cat)}">
          <img class="card-thumb" src="${a(e.cover)}" alt="" loading="lazy">
          <div class="card-body">
            <h3 class="card-title">${a(e.title)}</h3>
            <p class="card-desc">${a(e.summary)}</p>
            ${n ? `<div class="card-tags">${n}</div>` : ""}
            <div class="card-meta">
              <span class="meta-item"><img class="meta-icon" src="${a(t.eye)}" alt=""><span class="${this._remoteViews.has(e.id) ? "" : "is-loading"}" data-part="article-views" ${this._remoteViews.has(e.id) ? "" : "aria-busy=\"true\""}>${a(this._viewsFor(e))}</span></span>
              <span class="meta-item"><img class="meta-icon" src="${a(t.comment)}" alt="">${a(e.commentCount)}</span>
              <span class="meta-item" style="margin-left:auto;"><img class="meta-icon" src="${a(t.calendar)}" alt="">${a(e.date)}</span>
            </div>
          </div>
        </a>
      `;
		}).join(""), this._hydrateVisibleViews(), this._shown = Math.min(this._pageSize, o.length), this._done = this._shown >= o.length;
		let s = this.$("[data-part=\"crumb\"]");
		s && (s.textContent = this._catName + " · 共 " + o.length + " 篇"), t && (this._done ? (t.dataset.state = "done", t.textContent = "已经到底啦 · 共 " + o.length + " 篇") : (t.dataset.state = "ready", t.textContent = ""));
	}
	_loadMore() {
		if (this._loading || this._done) return;
		let e = this.getAttribute("active-cat") || "all", t = this.getAttribute("active-tag") || "", n = e === "all" ? this._articles.slice() : this._articles.filter((t) => t.cat === e);
		t && (n = n.filter((e) => e.tags && e.tags.includes(t)));
		let r = n.sort((e, t) => t.date.localeCompare(e.date)), i = r.slice(this._shown);
		if (!i.length) {
			this._done = !0;
			let e = this.$("[data-part=\"status\"]");
			e && (e.dataset.state = "done", e.textContent = "已经到底啦 · 共 " + r.length + " 篇");
			return;
		}
		this._loading = !0;
		let o = this.$("[data-part=\"status\"]");
		o && (o.dataset.state = "loading", o.innerHTML = "<span class=\"list-spinner\"></span>正在加载更多…");
		let s = this.$("[data-part=\"grid\"]");
		setTimeout(() => {
			let e = i.slice(0, this._pageSize), t = this._icons, n = e.map((e) => {
				let n = (e.tags || []).map((e) => `<span class="card-tag" data-tag="${a(e)}">${a(e)}</span>`).join("");
				return `
        <a class="article-card" href="#" data-id="${a(e.id)}" data-cat="${a(e.cat)}">
          <img class="card-thumb" src="${a(e.cover)}" alt="" loading="lazy">
          <div class="card-body">
            <h3 class="card-title">${a(e.title)}</h3>
            <p class="card-desc">${a(e.summary)}</p>
            ${n ? `<div class="card-tags">${n}</div>` : ""}
            <div class="card-meta">
              <span class="meta-item"><img class="meta-icon" src="${a(t.eye)}" alt=""><span class="${this._remoteViews.has(e.id) ? "" : "is-loading"}" data-part="article-views" ${this._remoteViews.has(e.id) ? "" : "aria-busy=\"true\""}>${a(this._viewsFor(e))}</span></span>
              <span class="meta-item"><img class="meta-icon" src="${a(t.comment)}" alt="">${a(e.commentCount)}</span>
              <span class="meta-item" style="margin-left:auto;"><img class="meta-icon" src="${a(t.calendar)}" alt="">${a(e.date)}</span>
            </div>
          </div>
        </a>
      `;
			}).join("");
			if (s && s.insertAdjacentHTML("beforeend", n), this._hydrateVisibleViews(), this._shown += e.length, this._loading = !1, this._shown >= r.length) this._done = !0, o && (o.dataset.state = "done", o.textContent = "已经到底啦 · 共 " + r.length + " 篇");
			else {
				o && (o.dataset.state = "ready", o.textContent = "");
				let e = this.$("[data-part=\"area\"]");
				e && o && o.getBoundingClientRect().bottom <= e.getBoundingClientRect().bottom && this._loadMore();
			}
		}, 250);
	}
};
customElements.get("article-list") || customElements.define("article-list", ie);
var ae = 4, oe = 2, se = class e extends r {
	_articles = [];
	_catNames = {};
	_years = [];
	_revealed = 0;
	_loading = !1;
	_done = !1;
	_observer = null;
	_activeYear = "";
	_scrollRaf = 0;
	set articles(t) {
		this._articles = t || [], this._years = e.groupByYearMonth(this._articles), this._revealed = 0, this._loading = !1, this._done = !1, this._scheduleRender();
	}
	set catNames(e) {
		this._catNames = e || {}, this._years.length && this._scheduleRender();
	}
	static groupByYearMonth(e) {
		let t = e.filter((e) => e && e.date).slice().sort((e, t) => String(t.date).localeCompare(String(e.date))), n = /* @__PURE__ */ new Map();
		t.forEach((e) => {
			let t = /^(\d{4})[-\/](\d{1,2})/.exec(String(e.date));
			if (!t) return;
			let r = t[1], i = t[2].padStart(2, "0"), a = n.get(r);
			a || (a = /* @__PURE__ */ new Map(), n.set(r, a));
			let o = a.get(i);
			o || (o = [], a.set(i, o)), o.push(e);
		});
		let r = [];
		return n.forEach((e, t) => {
			let n = [];
			e.forEach((e, r) => {
				n.push({
					key: `${t}-${r}`,
					month: r,
					posts: e
				});
			}), n.sort((e, t) => t.key.localeCompare(e.key)), r.push({
				year: t,
				months: n
			});
		}), r.sort((e, t) => t.year.localeCompare(e.year)), r;
	}
	_yearHTML(e) {
		let t = e.months.reduce((e, t) => e + t.posts.length, 0), n = e.months.map((t) => this._monthHTML(e, t)).join("");
		return `
      <section class="tl-year" data-year="${a(e.year)}">
        <header class="tl-year-bar">
          <h2 class="tl-year-num">${a(e.year)}</h2>
          <span class="tl-year-cnt">${t} 篇</span>
          <span class="tl-year-line"></span>
          <span class="tl-year-mons">${e.months.length} 个月</span>
        </header>
        <div class="tl-months">${n}</div>
      </section>
    `;
	}
	_monthHTML(e, t) {
		let n = t.posts.map((e) => this._postHTML(e)).join(""), r = Number(t.month);
		return `
      <section class="tl-mon" data-month="${a(t.key)}">
        <h3 class="tl-mon-h" role="button" tabindex="0" aria-expanded="true" data-act="mon-toggle">
          <span class="tl-mon-caret" aria-hidden="true"></span>
          <span class="tl-mon-name">${e.year} 年 ${r} 月</span>
          <span class="tl-mon-cnt">${t.posts.length} 篇</span>
        </h3>
        <div class="tl-mon-body">${n}</div>
      </section>
    `;
	}
	_postHTML(e) {
		let t = /^(\d{4})[-\/](\d{1,2})[-\/](\d{1,2})/.exec(String(e.date)), n = t ? `${t[2].padStart(2, "0")}/${t[3].padStart(2, "0")}` : String(e.date || ""), r = this._catNames && this._catNames[e.cat] || "";
		return `
      <a class="tl-post" href="#" data-id="${a(e.id)}" data-cat="${a(e.cat)}">
        <time class="tl-date">${a(n)}</time>
        <span class="tl-title">${a(e.title)}</span>
        ${r ? `<span class="tl-cat">${a(r)}</span>` : ""}
      </a>
    `;
	}
	_yearBtnsHTML() {
		return this._years.map((e) => this._yearBtnHTML(e, e.year === this._activeYear)).join("");
	}
	_yearBtnHTML(e, t) {
		let n = e.months.reduce((e, t) => e + t.posts.length, 0);
		return `<button type="button" class="year-btn${t ? " active" : ""}" data-year-btn="${a(e.year)}">${a(e.year)}<em>${n}</em></button>`;
	}
	_totalStats() {
		return this._years.length ? `归档 · 共 ${this._years.reduce((e, t) => e + t.months.reduce((e, t) => e + t.posts.length, 0), 0)} 篇 · ${this._years.length} 年` : "暂无文章";
	}
	render() {
		let e = "/images/extracted/home/iconfont-shouye@2x.png";
		return `
      <style>
        :host { display: contents; }
        .arch-top {
          display: flex;
          align-items: center;
          padding: 40px 59px 16px;
        }
        @media (max-width: 700px) { .arch-top { padding: 32px 20px 12px; } }
        @media (max-width: 480px) { .arch-top { padding: 28px 14px 10px; } }
        .arch-home-icon {
          display: inline-block;
          width: 14px; height: 14px; flex-shrink: 0;
          background: var(--brand-primary, #eb4f38);
          mask: url(${e}) center/contain no-repeat;
          -webkit-mask: url(${e}) center/contain no-repeat;
        }
        .arch-home-link {
          margin-left: 8px; font-size: 14px; letter-spacing: 0.02em;
          color: #c9c6c2; text-decoration: none; transition: color 0.2s;
        }
        .arch-home-link:hover { color: #ffffff; }
        .arch-sep { margin: 0 8px; font-size: 12px; color: #5d5a59; }
        .arch-crumb {
          font-size: 14px; font-weight: 500; letter-spacing: 0.02em;
          color: #ffffff; white-space: nowrap;
        }
        .arch-crumb small { margin-left: 8px; font-size: 12px; font-weight: 400; color: #9e9d99; letter-spacing: 0.04em; }
        .arch-divider {
          margin-left: 24px; height: 1px; min-width: 20px; flex: 1;
          background: linear-gradient(90deg, #3c3936, transparent);
        }
        .arch-area {
          flex: 1; min-height: 0; overflow-y: auto;
          scrollbar-width: thin; scrollbar-color: #444 transparent;
        }
        .arch-area::-webkit-scrollbar { width: 6px; }
        .arch-area::-webkit-scrollbar-track { background: transparent; }
        .arch-area::-webkit-scrollbar-thumb { background: #444; border-radius: 3px; }
        .arch-area::-webkit-scrollbar-thumb:hover { background: #555; }
        .arch-frame {
          margin: 0 auto;
          padding: 30px 48px 26px;
          container-type: inline-size;
        }
        @media (max-width: 700px) { .arch-frame { padding: 20px 20px 18px; } }
        @media (max-width: 480px) { .arch-frame { padding: 16px 14px 14px; } }

        .arch-main { display: flex; gap: 44px; align-items: flex-start; }

        /* 时间线 */
        .tl { flex: 1; min-width: 0; }
        .tl-year {
          margin-bottom: 18px;
          background: rgba(15, 14, 13, 0.55);
          border: 1px solid #2a2a2a;
          border-radius: 6px;
          overflow: hidden;
        }
        .tl-year-bar {
          display: flex; align-items: baseline; gap: 10px;
          padding: 13px 16px 11px;
          border-bottom: 1px solid #1f1e1d;
          background: rgba(255, 255, 255, 0.02);
        }
        .tl-year-num {
          font-size: 19px; font-weight: 600; line-height: 1;
          color: var(--brand-primary, #eb4f38);
          font-variant-numeric: tabular-nums;
          letter-spacing: 0.02em;
        }
        .tl-year-cnt { font-size: 12px; color: #9e9d99; white-space: nowrap; }
        .tl-year-line {
          flex: 1; height: 1px; min-width: 16px;
          background: linear-gradient(90deg, rgba(255,255,255,0.10), transparent);
        }
        .tl-year-mons { font-size: 11px; color: #5d5a59; letter-spacing: 0.06em; white-space: nowrap; }

        /* 月份 */
        .tl-mon + .tl-mon { border-top: 1px solid #1c1b1a; }
        .tl-mon-h {
          display: flex; align-items: center; gap: 8px;
          margin: 0; padding: 10px 16px;
          cursor: pointer; user-select: none;
          font-size: 13px; font-weight: 500; color: #c9c6c2;
          letter-spacing: 0.04em;
          -webkit-tap-highlight-color: transparent;
          transition: background 0.2s ease, color 0.2s ease;
        }
        .tl-mon-h:hover, .tl-mon-h:focus-visible {
          background: rgba(255, 255, 255, 0.03);
          color: #ffffff;
          outline: none;
        }
        .tl-mon-caret {
          width: 0; height: 0;
          border-left: 4px solid transparent;
          border-right: 4px solid transparent;
          border-top: 5px solid #5d5a59;
          transition: transform 0.22s ease;
          flex-shrink: 0;
        }
        .tl-mon-h[aria-expanded="false"] .tl-mon-caret { transform: rotate(-90deg); }
        .tl-mon-cnt {
          margin-left: auto; font-size: 11px; font-weight: 400;
          color: #6b6b6b; white-space: nowrap;
        }
        .tl-mon-body { padding: 0 8px 8px; }
        .tl-mon-h[aria-expanded="false"] + .tl-mon-body { display: none; }

        /* 文章行 */
        .tl-post {
          display: flex; align-items: baseline; gap: 12px;
          padding: 7px 8px; border-radius: 4px;
          color: #d8d6d3; text-decoration: none;
          transition: background 0.18s ease, color 0.18s ease;
          -webkit-tap-highlight-color: transparent;
        }
        .tl-post:hover { background: rgba(235, 79, 56, 0.07); }
        .tl-date {
          flex-shrink: 0; min-width: 44px;
          font-size: 12px; color: #777672;
          font-variant-numeric: tabular-nums;
          letter-spacing: 0.03em;
          transition: color 0.18s ease;
        }
        .tl-title {
          flex: 1; min-width: 0;
          font-size: 14px; line-height: 1.55;
          color: #f2f2f2;
          overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
          transition: color 0.18s ease;
        }
        .tl-post:hover .tl-title { color: var(--brand-primary, #eb4f38); }
        .tl-cat {
          flex-shrink: 0;
          max-width: 30%;
          padding: 1px 7px;
          font-size: 11px; line-height: 1.6;
          color: #9e9d99;
          background: rgba(255, 255, 255, 0.05);
          border: 1px solid #2a2a2a; border-radius: 3px;
          overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
        }
        .tl-post:hover .tl-cat {
          color: #c9c6c2; border-color: rgba(235, 79, 56, 0.4);
        }
        @media (max-width: 480px) {
          .tl-cat { display: none; }
        }

        .arch-empty {
          padding: 70px 20px; text-align: center;
          color: #6b6b6b; font-size: 14px;
        }

        /* 年份快捷导航：桌面右侧竖排 */
        .year-nav { display: none; width: 132px; flex-shrink: 0; }
        .year-nav-title {
          display: block; padding: 2px 6px 10px;
          font-size: 11px; letter-spacing: 0.18em; text-transform: uppercase;
          color: #5d5a59;
        }
        .year-nav-list {
          position: sticky; top: 6px;
          display: flex; flex-direction: column; gap: 4px;
          max-height: calc(100vh - 220px); overflow-y: auto;
          scrollbar-width: thin; scrollbar-color: #444 transparent;
        }
        .year-nav .year-btn {
          display: flex; align-items: center; gap: 8px;
          padding: 5px 10px;
          border: 1px solid transparent; border-radius: 5px;
          background: transparent;
          font-family: inherit; font-size: 13px; color: #9e9d99;
          cursor: pointer; text-align: left;
          transition: color 0.2s, background 0.2s, border-color 0.2s;
        }
        .year-btn em {
          font-style: normal; margin-left: auto;
          min-width: 18px; padding: 0 4px;
          font-size: 10px; line-height: 1.7; text-align: center;
          color: #5d5a59; background: rgba(255,255,255,0.05);
          border-radius: 3px;
        }
        .year-nav .year-btn:hover { color: #ffffff; background: rgba(255,255,255,0.04); }
        .year-nav .year-btn.active {
          color: var(--brand-primary, #eb4f38);
          background: rgba(235, 79, 56, 0.10);
          border-color: rgba(235, 79, 56, 0.35);
        }
        .year-nav .year-btn.active em { color: #eb4f38; background: rgba(235, 79, 56, 0.12); }

        /* 年份快捷导航：窄屏顶部胶囊 */
        .top-chips { display: none; }
        .top-chips .year-btn {
          display: inline-flex; align-items: center; gap: 6px;
          padding: 4px 10px;
          border: 1px solid #2a2a2a; border-radius: 999px;
          background: rgba(255,255,255,0.03);
          font-family: inherit; font-size: 12px; color: #9e9d99;
          cursor: pointer; white-space: nowrap;
          transition: color 0.2s, background 0.2s, border-color 0.2s;
        }
        .top-chips .year-btn em {
          min-width: 0; padding: 0; background: transparent;
          font-size: 10px; color: #6b6b6b;
        }
        .top-chips .year-btn:hover { color: #ffffff; border-color: #3c3936; }
        .top-chips .year-btn.active {
          color: #eb4f38;
          background: rgba(235, 79, 56, 0.12);
          border-color: rgba(235, 79, 56, 0.45);
        }
        .top-chips .year-btn.active em { color: #eb4f38; }

        @container (min-width: 760px) {
          .year-nav { display: block; }
          .top-chips { display: none; }
        }
        @container (max-width: 759px) {
          .top-chips { display: flex; flex-wrap: nowrap; gap: 8px; margin-bottom: 16px; overflow-x: auto; padding-bottom: 2px; scrollbar-width: none; }
          .top-chips::-webkit-scrollbar { display: none; }
          .tl-post { flex-wrap: wrap; row-gap: 2px; }
        }

        .arch-status {
          padding: 14px 0 10px; text-align: center;
          font-size: 12px; color: #6b6b6b; letter-spacing: 0.04em;
        }
        .arch-status[data-state="hidden"] { display: none; }
        .arch-status .arch-spinner {
          display: inline-block; width: 14px; height: 14px;
          margin-right: 6px; vertical-align: -2px;
          border: 2px solid rgba(255,255,255,0.12);
          border-top-color: var(--brand-primary, #eb4f38);
          border-radius: 50%;
          animation: arch-rotate 0.7s linear infinite;
        }
        @keyframes arch-rotate { to { transform: rotate(360deg); } }
      </style>

      <div class="arch-top">
        <span class="arch-home-icon"></span>
        <a href="./" class="arch-home-link">首页</a>
        <span class="arch-sep">&gt;</span>
        <span class="arch-crumb" data-part="crumb">${a(this._totalStats())}</span>
        <div class="arch-divider"></div>
      </div>

      <div class="arch-area" data-part="area">
        <div class="arch-frame">
          <div class="top-chips" data-part="topchips" role="tablist" aria-label="按年份跳转">${this._yearBtnsHTML()}</div>
          <div class="arch-main">
            <div class="tl" data-part="tl"></div>
            <nav class="year-nav" data-part="yearnav" aria-label="按年份跳转">
              <span class="year-nav-title">年份归档</span>
              <div class="year-nav-list" data-part="yearnav-list">${this._yearBtnsHTML()}</div>
            </nav>
          </div>
          <div class="arch-status" data-part="status" data-state="ready"></div>
        </div>
      </div>
    `;
	}
	mounted() {
		this._setupAreaEvents(), this._setupNavEvents(), this._scheduleRender();
	}
	_resetAndRender() {
		this._revealed = 0, this._loading = !1, this._done = !1, this._activeYear = this._years.length ? this._years[0].year : "";
		let e = this.$("[data-part=\"tl\"]");
		if (!e) return;
		if (e.innerHTML = "", this._refreshCrumb(), !this._years.length) {
			e.innerHTML = "<div class=\"arch-empty\">归档空空如也，快去发布第一篇文章吧～</div>";
			let t = this.$("[data-part=\"status\"]");
			t && (t.dataset.state = "hidden"), this._renderYearNav();
			return;
		}
		let t = this._years.slice(0, ae);
		e.innerHTML = t.map((e) => this._yearHTML(e)).join(""), this._revealed = t.length, this._done = this._revealed >= this._years.length;
		let n = this.$("[data-part=\"status\"]");
		n && (n.dataset.state = this._done ? "done" : "ready", n.textContent = this._done ? `已经到底啦 · 共 ${this._totalCount()} 篇` : ""), this._renderYearNav(), this._observeStatus(), this._scrollSpy(), this._done || requestAnimationFrame(() => this._autoFill());
	}
	_renderRaf = 0;
	_scheduleRender() {
		this._renderRaf ||= requestAnimationFrame(() => {
			this._renderRaf = 0, this._resetAndRender();
		});
	}
	_totalCount() {
		return this._years.reduce((e, t) => e + t.months.reduce((e, t) => e + t.posts.length, 0), 0);
	}
	_refreshCrumb() {
		let e = this.$("[data-part=\"crumb\"]");
		e && (e.textContent = this._totalStats());
	}
	_refreshNav() {
		let e = this._activeYear;
		this.$$("[data-part=\"yearnav\"] .year-btn, [data-part=\"topchips\"] .year-btn").forEach((t) => {
			t.classList.toggle("active", t.dataset.yearBtn === e);
		});
	}
	_renderYearNav() {
		let e = this._years.map((e) => this._yearBtnHTML(e, e.year === this._activeYear)).join(""), t = this.$("[data-part=\"yearnav-list\"]");
		t && (t.innerHTML = e);
		let n = this.$("[data-part=\"topchips\"]");
		n && (n.innerHTML = e);
	}
	_observeStatus() {
		this._observer && this._observer.disconnect();
		let e = this.$("[data-part=\"area\"]"), t = this.$("[data-part=\"status\"]");
		e && t && (this._observer = new IntersectionObserver((e) => {
			e.forEach((e) => {
				e.isIntersecting && this._loadMore();
			});
		}, {
			root: e,
			rootMargin: "120px 0px"
		}), this._observer.observe(t));
	}
	_loadMore() {
		if (this._loading || this._done) return;
		let e = this._years.slice(this._revealed);
		if (!e.length) {
			this._done = !0, this._finishStatus();
			return;
		}
		this._loading = !0;
		let t = this.$("[data-part=\"status\"]");
		t && (t.dataset.state = "loading", t.innerHTML = "<span class=\"arch-spinner\"></span>正在加载更早的归档…");
		let n = this.$("[data-part=\"tl\"]");
		setTimeout(() => {
			let r = e.slice(0, oe);
			if (n && n.insertAdjacentHTML("beforeend", r.map((e) => this._yearHTML(e)).join("")), this._revealed += r.length, this._loading = !1, this._revealed >= this._years.length) this._done = !0, this._finishStatus();
			else {
				t && (t.dataset.state = "ready", t.textContent = "");
				let e = this.$("[data-part=\"area\"]");
				e && t && t.getBoundingClientRect().bottom <= e.getBoundingClientRect().bottom && this._loadMore();
			}
		}, 200);
	}
	_finishStatus() {
		let e = this.$("[data-part=\"status\"]");
		e && (e.dataset.state = "done", e.textContent = `已经到底啦 · 共 ${this._totalCount()} 篇`);
	}
	_autoFill() {
		if (this._done || this._loading) return;
		let e = this.$("[data-part=\"area\"]"), t = this.$("[data-part=\"status\"]");
		e && t && t.getBoundingClientRect().bottom <= e.getBoundingClientRect().bottom && this._loadMore();
	}
	_setupAreaEvents() {
		let e = this.$("[data-part=\"area\"]");
		e && (e.addEventListener("click", (e) => {
			if (e.defaultPrevented) return;
			let t = e.target, n = t.closest("[data-act=\"mon-toggle\"]");
			if (n) {
				e.preventDefault();
				let t = n.getAttribute("aria-expanded") !== "false";
				n.setAttribute("aria-expanded", t ? "false" : "true");
				return;
			}
			let r = t.closest(".tl-post");
			if (r) {
				let t = r.dataset;
				e.preventDefault(), t.id && this.emit("article-select", {
					id: t.id,
					cat: t.cat || ""
				});
			}
		}), e.addEventListener("keydown", (e) => {
			let t = e.target;
			if (e.key !== "Enter" && e.key !== " ") return;
			let n = t.closest("[data-act=\"mon-toggle\"]");
			if (!n) return;
			e.preventDefault();
			let r = n.getAttribute("aria-expanded") !== "false";
			n.setAttribute("aria-expanded", r ? "false" : "true");
		}));
	}
	_setupNavEvents() {
		let e = this.$("[data-part=\"area\"]");
		e && e.addEventListener("click", (e) => {
			let t = e.target.closest("[data-year-btn]");
			if (!t) return;
			e.preventDefault();
			let n = t.dataset.yearBtn;
			n && this._jumpToYear(n);
		});
	}
	_jumpToYear(e) {
		if (this._revealed < this._years.length && !this._loading && this._years[this._revealed].year <= e) {
			this._loading = !0;
			let e = this.$("[data-part=\"tl\"]"), t = this._years.slice(this._revealed);
			e && e.insertAdjacentHTML("beforeend", t.map((e) => this._yearHTML(e)).join("")), this._revealed = this._years.length, this._loading = !1, this._done = !0, this._finishStatus();
		}
		let t = this.$("[data-part=\"area\"]"), n = this.shadow.querySelector(`.tl-year[data-year="${CSS.escape(e)}"]`);
		if (!t || !n) return;
		let r = n.getBoundingClientRect().top - t.getBoundingClientRect().top + t.scrollTop, i = window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
		t.scrollTo({
			top: r,
			behavior: i ? "auto" : "smooth"
		});
	}
	_scrollSpy() {
		let e = this.$("[data-part=\"area\"]");
		e && e.addEventListener("scroll", () => {
			this._scrollRaf ||= requestAnimationFrame(() => {
				this._scrollRaf = 0;
				let t = e.scrollTop + 24, n = e.scrollTop + e.clientHeight * .55, r = "";
				if (this.shadow.querySelectorAll(".tl-year").forEach((e) => {
					e.offsetTop <= t && (r = e.dataset.year || "");
				}), r ||= this._years.length ? this._years[0].year : "", r !== this._activeYear && (this._activeYear = r, this._refreshNav()), !this._done && !this._loading) {
					let e = this.$("[data-part=\"status\"]");
					e && e.getBoundingClientRect().bottom <= n && this._loadMore();
				}
			});
		}, { passive: !0 });
	}
};
customElements.get("archive-list") || customElements.define("archive-list", se);
var A = {
	kicker: "About · Personal Manifesto",
	title: "代码是工具，",
	titleAccent: "表达才是目的。",
	description: "我喜欢把复杂的问题，做成简单、可靠、耐看的东西。",
	cards: []
};
function j(e) {
	return a(e || "").replace(/\r?\n/g, "<br>");
}
var ce = class extends r {
	render() {
		let e = this._config, t = {
			...A,
			...e,
			cards: Array.isArray(e?.cards) ? e.cards : A.cards
		}, n = t.cards.map((e, t) => {
			let n = `manifesto-card ${e.variant === "wide" || e.variant === "accent" ? e.variant : "default"}`, r = `
        <small>${a(e.eyebrow || String(t + 1).padStart(2, "0"))}</small>
        ${e.title ? `<h2>${j(e.title)}</h2>` : ""}
        ${e.text ? `<p>${j(e.text)}</p>` : ""}
      `;
			return e.href ? `<a class="${n}" href="${a(e.href)}"
                   target="_blank" rel="noopener noreferrer">${r}</a>` : `<article class="${n}">${r}</article>`;
		}).join("");
		return `
      <style>
        :host {
          display: block;
          min-width: 0;
          flex: 1;
          height: 100%;
          color: #f4f1ed;
        }
        * {
          box-sizing: border-box;
        }
        .about-scroll {
          width: 100%;
          height: 100%;
          overflow-x: hidden;
          overflow-y: auto;
          background:
            radial-gradient(circle at 76% 14%, rgba(var(--brand-rgb), 0.065), transparent 29%),
            linear-gradient(115deg, rgba(43, 38, 34, 0.98), rgba(20, 18, 16, 0.98) 68%);
          scrollbar-color: #48423d transparent;
          scrollbar-width: thin;
        }
        .about-shell {
          width: min(100%, 1120px);
          min-height: 100%;
          margin: 0 auto;
          padding: clamp(42px, 7vh, 76px) clamp(28px, 6vw, 82px) 72px;
        }
        .kicker {
          margin: 0 0 20px;
          color: var(--brand-primary);
          font-family: var(--font-sans);
          font-size: 10px;
          font-weight: 600;
          line-height: 1.4;
          letter-spacing: 0.19em;
          text-transform: uppercase;
        }
        h1 {
          max-width: 900px;
          margin: 0;
          color: #f5f2ef;
          font-family: var(--font-sans);
          font-size: clamp(42px, 6.1vw, 78px);
          font-weight: 300;
          line-height: 0.99;
          letter-spacing: -0.048em;
        }
        h1 strong {
          display: block;
          margin-top: 5px;
          color: var(--brand-primary);
          font-weight: 650;
        }
        .intro {
          max-width: 660px;
          margin: 24px 0 0;
          color: #a39b94;
          font-family: var(--font-sans);
          font-size: 13px;
          line-height: 1.9;
        }
        .manifesto-grid {
          display: grid;
          margin-top: clamp(34px, 6vh, 58px);
          grid-template-columns: repeat(3, minmax(0, 1fr));
          border-top: 1px solid rgba(255, 255, 255, 0.13);
          border-left: 1px solid rgba(255, 255, 255, 0.13);
        }
        .manifesto-card {
          display: flex;
          min-height: 154px;
          padding: 23px;
          flex-direction: column;
          justify-content: flex-start;
          border-right: 1px solid rgba(255, 255, 255, 0.13);
          border-bottom: 1px solid rgba(255, 255, 255, 0.13);
          color: inherit;
          background: rgba(16, 14, 13, 0.52);
          font-family: var(--font-sans);
          text-decoration: none;
          transition: background 0.2s ease, transform 0.2s ease;
        }
        a.manifesto-card:hover,
        a.manifesto-card:focus-visible {
          background: rgba(255, 255, 255, 0.045);
          transform: translateY(-2px);
        }
        .manifesto-card.wide {
          grid-column: span 2;
        }
        .manifesto-card.accent {
          justify-content: flex-end;
          color: #ffffff;
          background: var(--brand-primary);
        }
        .manifesto-card small {
          display: block;
          margin-bottom: 20px;
          color: #716963;
          font-size: 9px;
          line-height: 1.35;
          letter-spacing: 0.16em;
          text-transform: uppercase;
        }
        .manifesto-card h2 {
          margin: 0;
          color: #f3efeb;
          font-family: Georgia, 'Times New Roman', serif;
          font-size: clamp(18px, 2vw, 24px);
          font-weight: 400;
          line-height: 1.4;
        }
        .manifesto-card p {
          margin: auto 0 0;
          color: #99918a;
          font-size: 11px;
          line-height: 1.75;
        }
        .manifesto-card.accent small,
        .manifesto-card.accent p {
          color: rgba(255, 255, 255, 0.78);
        }
        .manifesto-card.accent h2 {
          color: #ffffff;
          font-size: clamp(21px, 2.3vw, 29px);
          line-height: 1.18;
        }
        @media (max-width: 820px) {
          .about-shell {
            padding-right: 28px;
            padding-left: 28px;
          }
          .manifesto-grid {
            grid-template-columns: repeat(2, minmax(0, 1fr));
          }
        }
        @media (max-width: 560px) {
          .about-shell {
            padding: 34px 18px 54px;
          }
          h1 {
            font-size: clamp(38px, 13vw, 56px);
          }
          .manifesto-grid {
            grid-template-columns: 1fr;
          }
          .manifesto-card.wide {
            grid-column: auto;
          }
          .manifesto-card {
            min-height: 132px;
            padding: 19px;
          }
        }
        @media (prefers-reduced-motion: reduce) {
          .manifesto-card {
            transition-duration: 0.01ms;
          }
        }
      </style>

      <div class="about-scroll">
        <main class="about-shell">
          <p class="kicker">${a(t.kicker)}</p>
          <h1>${j(t.title)}<strong>${j(t.titleAccent)}</strong></h1>
          <p class="intro">${j(t.description)}</p>
          ${n ? `<section class="manifesto-grid" aria-label="关于我的信息">${n}</section>` : ""}
        </main>
      </div>
    `;
	}
	set config(e) {
		this._config = e || {}, this.shadow.innerHTML = this.render();
	}
};
customElements.get("about-page") || customElements.define("about-page", ce);
var le = class extends r {
	static get observedAttributes() {
		return [];
	}
	_articles = [];
	_catNames = {};
	set articles(e) {
		this._articles = e, this._render();
	}
	set catNames(e) {
		this._catNames = e;
	}
	render() {
		return "\n      <style>\n        :host {\n          display: contents;\n        }\n        .results-root {\n          display: flex;\n          min-height: 0;\n          flex: 1;\n          flex-direction: column;\n          overflow: hidden;\n        }\n        .results-head {\n          display: flex;\n          align-items: center;\n          gap: 16px;\n          padding: 40px 59px 0;\n        }\n        @media (max-width: 700px) {\n          .results-head { padding: 40px 20px 0; }\n        }\n        .results-title {\n          font-size: 22px;\n          font-weight: 500;\n          color: #ffffff;\n          margin: 0;\n          white-space: nowrap;\n        }\n        .results-form {\n          display: flex;\n          align-items: center;\n          flex: 1;\n          max-width: 400px;\n          gap: 10px;\n          padding: 8px 14px;\n          border: 1px solid #333;\n          border-radius: 6px;\n          transition: border-color 0.2s;\n        }\n        .results-form:focus-within {\n          border-color: #eb4f38;\n        }\n        .results-form svg {\n          flex-shrink: 0;\n          color: #6b6b6b;\n        }\n        .results-input {\n          flex: 1;\n          min-width: 0;\n          background: none;\n          border: none;\n          outline: none;\n          color: #f2f2f2;\n          font-size: 14px;\n          font-family: inherit;\n        }\n        .results-input::placeholder {\n          color: #6b6b6b;\n        }\n        .results-back {\n          margin-left: auto;\n          color: #9e9d99;\n          text-decoration: none;\n          font-size: 13px;\n          transition: color 0.2s;\n          white-space: nowrap;\n        }\n        .results-back:hover {\n          color: #ffffff;\n        }\n        .results-meta {\n          padding: 16px 59px 0;\n        }\n        @media (max-width: 700px) {\n          .results-meta { padding: 16px 20px 0; }\n        }\n        .results-count {\n          font-size: 14px;\n          color: #9e9d99;\n        }\n        .results-area {\n          flex: 1;\n          overflow-y: auto;\n          padding: 24px 59px 48px;\n        }\n        @media (max-width: 700px) {\n          .results-area { padding: 24px 20px 48px; }\n        }\n        .article-grid {\n          display: grid;\n          grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));\n          gap: 16px;\n        }\n        .article-card {\n          display: flex;\n          flex-direction: column;\n          background: #0f0e0d;\n          border: 1px solid #2a2a2a;\n          border-radius: 4px;\n          overflow: hidden;\n          text-decoration: none;\n          transition: border-color 0.2s, transform 0.2s;\n        }\n        .article-card:hover {\n          border-color: #eb4f38;\n          transform: translateY(-2px);\n        }\n        .card-thumb {\n          width: 100%;\n          height: 160px;\n          object-fit: cover;\n        }\n        .card-body {\n          padding: 14px 16px 16px;\n        }\n        .card-title {\n          font-size: 15px;\n          font-weight: 500;\n          line-height: 1.5;\n          color: #ffffff;\n          margin: 0 0 8px;\n          display: -webkit-box;\n          -webkit-line-clamp: 2;\n          -webkit-box-orient: vertical;\n          overflow: hidden;\n        }\n        .card-desc {\n          font-size: 13px;\n          line-height: 1.5;\n          color: #9e9d99;\n          margin: 0 0 12px;\n          display: -webkit-box;\n          -webkit-line-clamp: 2;\n          -webkit-box-orient: vertical;\n          overflow: hidden;\n        }\n        .card-meta {\n          display: flex;\n          align-items: center;\n          gap: 12px;\n          font-size: 12px;\n          color: #6b6b6b;\n        }\n        .meta-item {\n          display: flex;\n          align-items: center;\n          gap: 4px;\n        }\n        .meta-icon {\n          width: 14px;\n          height: 14px;\n          object-fit: contain;\n          opacity: 0.5;\n        }\n        .grid-empty {\n          text-align: center;\n          padding: 48px 20px;\n          color: #6b6b6b;\n          font-size: 14px;\n        }\n      </style>\n      <div class=\"results-root\">\n        <header class=\"results-head\">\n          <h1 class=\"results-title\">搜索</h1>\n          <form class=\"results-form\" data-part=\"form\" novalidate>\n            <svg width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.8\" stroke-linecap=\"round\" stroke-linejoin=\"round\">\n              <circle cx=\"11\" cy=\"11\" r=\"7\"></circle><path d=\"m21 21-4.3-4.3\"></path>\n            </svg>\n            <input class=\"results-input\" type=\"search\" data-part=\"search-input\" placeholder=\"搜索文章、目录、正文关键词…\" autocomplete=\"off\" spellcheck=\"false\">\n          </form>\n          <a class=\"results-back\" href=\"./\">返回首页</a>\n        </header>\n        <div class=\"results-meta\"><span class=\"results-count\" data-part=\"count\"></span></div>\n        <div class=\"results-area\">\n          <div class=\"article-grid\" data-part=\"grid\"></div>\n        </div>\n      </div>\n    ";
	}
	mounted() {
		let e = this.$("[data-part=\"search-input\"]"), t = this.$("[data-part=\"form\"]");
		e && (e.value = new URLSearchParams(window.location.search).get("q") || ""), t?.addEventListener("submit", (e) => {
			e.preventDefault(), this._render();
		}), e?.addEventListener("input", () => {
			this._render();
		}), this._render();
	}
	_render() {
		let e = this.$("[data-part=\"search-input\"]"), t = this.$("[data-part=\"grid\"]"), n = this.$("[data-part=\"count\"]"), r = e ? e.value.trim() : "", i = r ? c(r, this._articles, this._catNames) : [], o = window.location.pathname + (r ? "?q=" + encodeURIComponent(r) : "");
		if (window.history.replaceState(null, "", o), n && (n.textContent = r ? `与「${r}」相关的文章 · 共 ${i.length} 篇` : "输入关键词开始搜索"), t) {
			if (!i.length) {
				t.innerHTML = r ? "<div class=\"grid-empty\">没有找到相关的内容，换个关键词试试吧～</div>" : "<div class=\"grid-empty\">输入关键词，模糊搜索文章 / 目录 / 正文内容</div>";
				return;
			}
			t.innerHTML = i.map((e) => {
				let t = e.article;
				return `
        <a class="article-card" href="./#cat=${encodeURIComponent(t.cat)}&art=${encodeURIComponent(t.id)}" data-id="${a(t.id)}" data-cat="${a(t.cat)}">
          <img class="card-thumb" src="${a(t.cover)}" alt="" loading="lazy">
          <div class="card-body">
            <h3 class="card-title">${a(t.title)}</h3>
            <p class="card-desc">${a(t.summary)}</p>
            <div class="card-meta">
              <span class="meta-item">${a(t.views)} 浏览</span>
              <span class="meta-item">${a(t.commentCount)} 评论</span>
              <span class="meta-item" style="margin-left:auto;">${a(t.date)}</span>
            </div>
          </div>
        </a>
      `;
			}).join("");
		}
	}
};
customElements.get("search-results") || customElements.define("search-results", le);
function M(e) {
	e = Math.max(0, Math.floor(e));
	let t = Math.floor(e / 3600), n = Math.floor(e % 3600 / 60), r = e % 60, i = (e) => String(e).padStart(2, "0");
	return t > 0 ? `${t}:${i(n)}:${i(r)}` : `${i(n)}:${i(r)}`;
}
var N = [
	"loop",
	"single",
	"shuffle"
], ue = [
	{
		title: "SoundHelix Song 1",
		artist: "SoundHelix",
		src: "/audio/SoundHelix-Song-1.mp3"
	},
	{
		title: "SoundHelix Song 2",
		artist: "SoundHelix",
		src: "/audio/SoundHelix-Song-2.mp3"
	},
	{
		title: "SoundHelix Song 3",
		artist: "SoundHelix",
		src: "/audio/SoundHelix-Song-3.mp3"
	},
	{
		title: "SoundHelix Song 4",
		artist: "SoundHelix",
		src: "/audio/SoundHelix-Song-4.mp3"
	},
	{
		title: "SoundHelix Song 5",
		artist: "SoundHelix",
		src: "/audio/SoundHelix-Song-5.mp3"
	}
], de = class extends r {
	static get observedAttributes() {
		return [];
	}
	_tracks = ue;
	_index = 0;
	_isPlaying = !1;
	_current = 0;
	_volume = .5;
	_mode = "loop";
	_lastVolume = .5;
	_audio = null;
	_listPanel = null;
	_volumePanel = null;
	set tracks(e) {
		this._tracks = e, this._updateInfo();
	}
	get currentTrack() {
		return this._tracks[this._index] || this._tracks[0];
	}
	render() {
		return `
      <style>
        :host {
          display: flex;
          position: relative;
          z-index: 30;
          height: 50px;
          min-height: 50px;
          flex-shrink: 0;
          margin-top: auto;
          background: rgba(0,0,0,0.42);
          backdrop-filter: blur(16px) saturate(120%);
          -webkit-backdrop-filter: blur(16px) saturate(120%);
        }
        .player-inner {
          display: flex;
          flex: 1;
          align-items: center;
          height: 100%;
          border-top: 1px solid rgba(255,255,255,0.16);
          padding: 0 32px;
        }
        .player-btn {
          display: flex;
          align-items: center;
          justify-content: center;
          background: transparent;
          border: none;
          cursor: pointer;
          padding: 6px;
          border-radius: 50%;
          color: #9e9d99;
          transition: background 0.18s ease, color 0.18s ease, transform 0.15s ease;
          position: relative;
          -webkit-tap-highlight-color: transparent;
        }
        .player-btn:focus { outline: none; }
        .player-btn:focus-visible {
          outline: 2px solid rgba(var(--brand-rgb), 0.6);
          outline-offset: 2px;
        }
        .player-btn:active { transform: scale(0.94); }
        .player-btn svg { display: block; }
        .player-btn svg path,
        .player-btn svg rect,
        .player-btn svg polygon,
        .player-btn svg circle {
          fill: currentColor;
          stroke: currentColor;
          transition: fill 0.18s ease, stroke 0.18s ease;
        }
        .player-controls {
          display: flex;
          align-items: center;
          gap: 6px;
          flex-shrink: 0;
        }
        .player-btn.is-control {
          width: 32px;
          height: 32px;
          color: #b3b3b3;
        }
        .player-btn.is-control:hover {
          color: #ffffff;
          background: rgba(255,255,255,0.06);
        }
        .player-btn.is-control:active {
          color: var(--brand-primary);
        }
        .player-btn.is-primary {
          width: 32px;
          height: 32px;
          background: var(--brand-primary);
          color: #ffffff;
          border-radius: 50%;
          box-shadow: 0 3px 10px rgba(var(--brand-rgb), 0.32);
          margin: 0 4px;
          transition: background 0.18s ease, color 0.18s ease, transform 0.15s ease, box-shadow 0.18s ease;
        }
        .player-btn.is-primary:hover {
          background: #ff5a41;
          color: #ffffff;
          transform: scale(1.08);
          box-shadow: 0 5px 14px rgba(var(--brand-rgb), 0.45);
        }
        .player-btn.is-primary:active {
          transform: scale(0.95);
          background: #d9452e;
          box-shadow: 0 2px 8px rgba(var(--brand-rgb), 0.4);
        }
        .player-btn.is-primary .icon-play,
        .player-btn.is-primary .icon-pause {
          position: absolute;
          top: 50%;
          left: 50%;
          transform: translate(-50%, -50%);
          transition: opacity 0.15s ease, transform 0.2s ease;
        }
        .player-btn.is-primary .icon-pause {
          opacity: 0;
          transform: translate(-50%, -50%) scale(0.6);
        }
        .player-btn.is-primary.is-playing .icon-play {
          opacity: 0;
          transform: translate(-50%, -50%) scale(0.6);
        }
        .player-btn.is-primary.is-playing .icon-pause {
          opacity: 1;
          transform: translate(-50%, -50%) scale(1);
        }
        .player-btn.is-primary.is-playing::before {
          content: '';
          position: absolute;
          inset: -4px;
          border-radius: 50%;
          border: 1.5px solid rgba(var(--brand-rgb), 0.35);
          animation: playerPulse 1.6s ease-out infinite;
          pointer-events: none;
        }
        @keyframes playerPulse {
          0%   { transform: scale(0.95); opacity: 0.9; }
          100% { transform: scale(1.35); opacity: 0; }
        }
        .player-btn.volume-btn {
          width: 32px;
          height: 32px;
          color: #b3b3b3;
        }
        .player-btn.volume-btn:hover {
          color: #ffffff;
          background: rgba(255,255,255,0.06);
        }
        .player-btn.volume-btn:active {
          color: var(--brand-primary);
        }
        .player-btn.volume-btn .icon-mute { display: none; }
        .player-btn.volume-btn .icon-volume { display: block; }
        .player-btn.volume-btn.is-muted .icon-volume { display: none; }
        .player-btn.volume-btn.is-muted .icon-mute { display: block; color: var(--brand-primary); }
        .player-btn.mode-toggle {
          width: 34px;
          height: 32px;
        }
        .player-btn.mode-toggle svg {
          display: block;
          transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
        }
        .player-btn.mode-toggle .mode-glyph path {
          fill: none;
          stroke: currentColor;
          stroke-width: 1.8;
          stroke-linecap: round;
          stroke-linejoin: round;
        }
        .player-btn.mode-toggle.is-mode-active {
          color: var(--brand-primary);
          background: rgba(var(--brand-rgb), 0.12);
        }
        .player-btn.mode-toggle.is-mode-active:hover,
        .player-btn.mode-toggle.is-mode-active:focus-visible {
          color: #ff6a50;
          background: rgba(var(--brand-rgb), 0.2);
        }
        .mode-toggle.is-mode-active svg { transform: scale(1.05); }
        .mode-toggle.is-switch svg { animation: mode-pop 0.4s cubic-bezier(0.22, 1.4, 0.36, 1); }
        @keyframes mode-pop {
          0%   { transform: rotate(-14deg) scale(0.8); }
          60%  { transform: rotate(4deg) scale(1.14); }
          100% { transform: rotate(0) scale(1.05); }
        }
        .player-btn[data-mode="shuffle"] {
          color: var(--brand-primary);
        }
        .player-btn[data-mode="shuffle"]:hover {
          color: #ff5a41;
        }
        .player-btn.is-pill {
          background: #1d1d1d;
          border-radius: 12px;
          padding: 4px 10px;
          display: flex;
          align-items: center;
          gap: 5px;
          color: #9e9d99;
        }
        .player-btn.is-pill:hover {
          background: #2a2a2a;
          color: #ffffff;
          transform: none;
        }
        .player-btn.is-pill:active { transform: scale(0.97); }
        .player-btn.is-pill span {
          font-family: var(--font-sans);
          font-size: 12px;
          color: inherit;
          font-variant-numeric: tabular-nums;
        }
        .player-btn.is-pill.open {
          background: var(--brand-primary);
          color: #ffffff;
        }
        .player-center {
          margin: 0 32px;
          min-width: 0;
          flex: 1;
        }
        .track-info {
          margin-bottom: 6px;
          line-height: 1;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
        .track-title, .track-artist {
          font-family: var(--font-sans);
          font-size: 12px;
          color: #9e9d99;
          line-height: 1;
          letter-spacing: 0.01em;
        }
        .track-title {
          color: #f2f2f2;
          font-weight: 500;
        }
        .track-sep {
          margin: 0 6px;
          color: #5d5a59;
        }
        .progress-row {
          display: flex;
          align-items: center;
          gap: 12px;
        }
        .time-current, .time-duration {
          min-width: 32px;
          font-size: 11px;
          line-height: 1;
          color: #6b6b6b;
          font-variant-numeric: tabular-nums;
        }
        .time-duration {
          text-align: right;
        }
        .progress-track {
          position: relative;
          flex: 1;
          height: 4px;
          background: #2a2a2a;
          border-radius: 4px;
          cursor: pointer;
          transition: height 0.15s ease;
        }
        .progress-track:hover { height: 6px; }
        .progress-fill {
          position: absolute;
          left: 0;
          top: 0;
          height: 100%;
          width: 0;
          background: linear-gradient(90deg, #6b6b6b, #9e9d99);
          border-radius: 4px;
          pointer-events: none;
          transition: width 0.05s linear;
        }
        .progress-thumb {
          position: absolute;
          top: 50%;
          left: 0;
          transform: translate(-50%, -50%);
          width: 11px;
          height: 11px;
          background: #ffffff;
          border-radius: 50%;
          opacity: 0;
          transition: opacity 0.2s ease, transform 0.2s ease, left 0.05s linear;
          box-shadow: 0 0 0 3px rgba(255,255,255,0.1);
          pointer-events: none;
        }
        .progress-track:hover .progress-thumb { opacity: 1; }
        .progress-track.is-dragging .progress-thumb { opacity: 1; transform: translate(-50%, -50%) scale(1.15); }
      </style>
      <div class="player-inner">
        <div class="player-controls">
          <button class="player-btn is-control" data-act="prev" aria-label="上一首" title="上一首">
          <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
            <path d="M11 5L4 11l7 6V5z" fill="currentColor"/>
            <rect x="3.5" y="5" width="2" height="12" rx="0.6" fill="currentColor"/>
            <rect x="16.5" y="5" width="2" height="12" rx="0.6" fill="currentColor"/>
          </svg>
        </button>
        <button class="player-btn is-primary" data-act="play" aria-label="播放" title="播放/暂停">
          <svg class="icon-play" width="16" height="16" viewBox="0 0 20 20" fill="none"><path d="M5 3.5L16.5 10 5 16.5V3.5z" fill="currentColor"/></svg>
          <svg class="icon-pause" width="16" height="16" viewBox="0 0 20 20" fill="none">
            <rect x="5" y="3.5" width="3" height="13" rx="0.8" fill="currentColor"/>
            <rect x="12" y="3.5" width="3" height="13" rx="0.8" fill="currentColor"/>
          </svg>
        </button>
        <button class="player-btn is-control" data-act="next" aria-label="下一首" title="下一首">
          <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
            <path d="M11 5l7 6-7 6V5z" fill="currentColor"/>
            <rect x="3.5" y="5" width="2" height="12" rx="0.6" fill="currentColor"/>
            <rect x="16.5" y="5" width="2" height="12" rx="0.6" fill="currentColor"/>
          </svg>
        </button>
      </div>
      <div class="player-center">
        <div class="track-info">
          <span class="track-title" data-part="title">SoundHelix Song 1</span>
          <span class="track-sep">·</span>
          <span class="track-artist" data-part="artist">SoundHelix</span>
        </div>
        <div class="progress-row">
          <span class="time-current" data-part="current">00:00</span>
          <div class="progress-track" data-part="progress">
            <div class="progress-fill" data-part="fill"></div>
            <div class="progress-thumb" data-part="thumb"></div>
          </div>
          <span class="time-duration" data-part="duration">00:00</span>
        </div>
      </div>
      <button class="player-btn volume-btn" data-act="volume" aria-label="音量" title="音量">
        <svg class="icon-volume" width="20" height="20" viewBox="0 0 20 20" fill="none">
          <path d="M2.5 7v6h3.2l4.3 3.6V3.4L5.7 7H2.5z" fill="currentColor"/>
          <path class="wave wave-1" d="M12.5 6.5a4.5 4.5 0 010 7" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" fill="none"/>
          <path class="wave wave-2" d="M14.8 4.8a7 7 0 010 10.4" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" fill="none"/>
        </svg>
        <svg class="icon-mute" width="20" height="20" viewBox="0 0 20 20" fill="none">
          <path d="M2.5 7v6h3.2l4.3 3.6V3.4L5.7 7H2.5z" fill="currentColor"/>
          <path d="M13 8l4 4M17 8l-4 4" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"/>
        </svg>
      </button>
      <button class="player-btn mode-toggle" data-part="mode-btn" data-mode="loop" aria-label="列表循环">
        <svg class="mode-glyph" width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
          <path d="m17 2 4 4-4 4"/><path d="M3 11v-1a4 4 0 0 1 4-4h14"/><path d="m7 22-4-4 4-4"/><path d="M21 13v1a4 4 0 0 1-4 4H3"/>
        </svg>
      </button>
      <button class="player-btn is-pill" data-act="list" aria-label="播放列表">
        <svg width="17" height="17" viewBox="0 0 17 17" fill="none">
          <rect x="2" y="3" width="13" height="2" rx="1" fill="currentColor"/>
          <rect x="2" y="7" width="13" height="2" rx="1" fill="currentColor"/>
          <rect x="2" y="11" width="8" height="2" rx="1" fill="currentColor"/>
        </svg>
        <span data-part="list-count">${this._tracks.length}</span>
      </button>
      </div>
    `;
	}
	mounted() {
		this._initAudio(), this._bindButtons(), this._bindProgress(), this._bindVolume(), this._bindKeyboard(), this._prefillDurations(), this._updateInfo();
	}
	_initAudio() {
		let e = new Audio();
		e.preload = "metadata", this._volume = d.get("player:volume", .5), e.volume = this._volume, e.src = this._tracks[0]?.src || "", e.addEventListener("play", () => {
			this._isPlaying = !0, this._syncPlayIcon();
		}), e.addEventListener("pause", () => {
			this._isPlaying = !1, this._syncPlayIcon();
		}), e.addEventListener("timeupdate", () => {
			this._current = e.currentTime, this._updateProgress();
		}), e.addEventListener("durationchange", () => {
			let t = this._tracks[this._index];
			e.duration && isFinite(e.duration) && (t.duration = e.duration), this._updateInfo();
		}), e.addEventListener("ended", () => {
			this._mode === "single" ? (e.currentTime = 0, e.play().catch(() => {})) : this._loadTrack(this._nextIndex(), !0);
		}), e.addEventListener("error", () => {
			this._isPlaying = !1, this._syncPlayIcon();
		}), this._audio = e;
	}
	_bindButtons() {
		this.$("[data-act=\"prev\"]")?.addEventListener("click", () => this._prev()), this.$("[data-act=\"play\"]")?.addEventListener("click", () => this._toggle()), this.$("[data-act=\"next\"]")?.addEventListener("click", () => this._next()), this.$("[data-part=\"mode-btn\"]")?.addEventListener("click", () => this._cycleMode()), this.$("[data-act=\"volume\"]")?.addEventListener("click", (e) => {
			e.stopPropagation(), this._toggleVolumePanel();
		}), this.$("[data-act=\"list\"]")?.addEventListener("click", (e) => {
			e.stopPropagation(), this._toggleList();
		});
	}
	_bindProgress() {
		let e = this.$("[data-part=\"progress\"]");
		if (!e) return;
		let t = !1, n = (t) => {
			let n = e.getBoundingClientRect(), r = ("touches" in t ? t.touches[0].clientX : t.clientX) - n.left, i = Math.max(0, Math.min(1, r / n.width)) * (this._tracks[this._index].duration || 0);
			this._current = i, this._audio && (this._audio.currentTime = i), this._updateProgress();
		};
		e.addEventListener("mousedown", (r) => {
			t = !0, e.classList.add("is-dragging"), n(r), r.preventDefault();
		}), document.addEventListener("mousemove", (e) => {
			t && n(e);
		}), document.addEventListener("mouseup", () => {
			t && (t = !1, e.classList.remove("is-dragging"));
		}), e.addEventListener("touchstart", (r) => {
			t = !0, e.classList.add("is-dragging"), n(r);
		}, { passive: !0 }), document.addEventListener("touchmove", (e) => {
			t && n(e);
		}, { passive: !0 }), document.addEventListener("touchend", () => {
			t && (t = !1, e.classList.remove("is-dragging"));
		});
	}
	_bindVolume() {
		this._updateVolumeUI();
	}
	_ensureVolumePanel() {
		if (this._volumePanel) return this._updateVolumeUI(), this._volumePanel;
		let e = document.createElement("div");
		e.className = "volume-panel", e.innerHTML = "\n      <style>\n        .volume-panel {\n          position: fixed;\n          z-index: 60;\n          background: #141210;\n          border: 1px solid #2a2a2a;\n          border-radius: 8px;\n          padding: 12px 16px;\n          box-shadow: 0 14px 40px rgba(0, 0, 0, 0.65);\n          display: none;\n          animation: panelIn 0.16s ease-out;\n        }\n        .volume-panel.open { display: block; }\n        @keyframes panelIn {\n          from { opacity: 0; transform: translateY(8px); }\n          to   { opacity: 1; transform: translateY(0); }\n        }\n        .volume-panel-inner {\n          display: flex;\n          align-items: center;\n          gap: 12px;\n          color: #9e9d99;\n        }\n        .volume-slider {\n          width: 140px;\n          display: flex;\n          align-items: center;\n        }\n        .volume-slider-track {\n          position: relative;\n          flex: 1;\n          height: 4px;\n          background: #2a2a2a;\n          border-radius: 4px;\n          cursor: pointer;\n          transition: height 0.15s ease;\n        }\n        .volume-slider-track:hover { height: 6px; }\n        .volume-slider-fill {\n          position: absolute;\n          left: 0;\n          top: 0;\n          height: 100%;\n          width: 50%;\n          background: linear-gradient(90deg, #6b6b6b, var(--brand-primary));\n          border-radius: 4px;\n          pointer-events: none;\n        }\n        .volume-slider-thumb {\n          position: absolute;\n          top: 50%;\n          left: 50%;\n          transform: translate(-50%, -50%);\n          width: 12px;\n          height: 12px;\n          background: #ffffff;\n          border-radius: 50%;\n          box-shadow: 0 0 0 3px rgba(var(--brand-rgb), 0.18);\n          pointer-events: none;\n          opacity: 0;\n          transition: opacity 0.2s ease, transform 0.2s ease;\n        }\n        .volume-slider-track:hover .volume-slider-thumb,\n        .volume-panel.is-dragging .volume-slider-thumb {\n          opacity: 1;\n        }\n        .volume-panel.is-dragging .volume-slider-thumb {\n          transform: translate(-50%, -50%) scale(1.15);\n        }\n        .volume-pct {\n          font-family: var(--font-sans);\n          font-size: 12px;\n          color: #ffffff;\n          min-width: 36px;\n          text-align: right;\n          font-variant-numeric: tabular-nums;\n        }\n      </style>\n      <div class=\"volume-panel-inner\">\n        <svg width=\"14\" height=\"14\" viewBox=\"0 0 20 20\" fill=\"none\">\n          <path d=\"M2.5 7v6h3.2l4.3 3.6V3.4L5.7 7H2.5z\" fill=\"currentColor\"/>\n          <path d=\"M12.5 6.5a4.5 4.5 0 010 7\" stroke=\"currentColor\" stroke-width=\"1.4\" stroke-linecap=\"round\" fill=\"none\"/>\n        </svg>\n        <div class=\"volume-slider\">\n          <div class=\"volume-slider-track\" data-part=\"vol-track\">\n            <div class=\"volume-slider-fill\" data-part=\"vol-fill\"></div>\n            <div class=\"volume-slider-thumb\" data-part=\"vol-thumb\"></div>\n          </div>\n        </div>\n        <span class=\"volume-pct\" data-part=\"vol-pct\">50%</span>\n      </div>\n    ", document.body.appendChild(e);
		let t = e.querySelector("[data-part=\"vol-track\"]"), n = !1, r = (e) => {
			let n = t.getBoundingClientRect(), r = ("touches" in e ? e.touches[0].clientX : e.clientX) - n.left, i = Math.max(0, Math.min(1, r / n.width));
			this._setVolume(i), this._updateVolumeUI();
		};
		t.addEventListener("mousedown", (t) => {
			n = !0, e.classList.add("is-dragging"), r(t), t.preventDefault();
		}), document.addEventListener("mousemove", (e) => {
			n && r(e);
		}), document.addEventListener("mouseup", () => {
			n && (n = !1, e.classList.remove("is-dragging"));
		}), t.addEventListener("touchstart", (t) => {
			n = !0, e.classList.add("is-dragging"), r(t);
		}, { passive: !0 }), document.addEventListener("touchmove", (e) => {
			n && r(e);
		}, { passive: !0 }), document.addEventListener("touchend", () => {
			n && (n = !1, e.classList.remove("is-dragging"));
		});
		let i = this.$("[data-act=\"volume\"]");
		return document.addEventListener("click", (t) => {
			e.classList.contains("open") && (e.contains(t.target) || i && i.contains(t.target) || e.classList.remove("open"));
		}), this._volumePanel = e, this._updateVolumeUI(), e;
	}
	_setVolume(e, t = !0) {
		this._volume = Math.max(0, Math.min(1, e)), this._audio && (this._audio.volume = this._volume), this._volume > 0 && (this._lastVolume = this._volume), t && d.set("player:volume", this._volume);
	}
	_updateVolumeUI() {
		let e = Math.round(this._volume * 100), t = this.$("[data-act=\"volume\"]");
		if (t && t.classList.toggle("is-muted", this._volume === 0), this._volumePanel) {
			let t = this._volumePanel.querySelector("[data-part=\"vol-fill\"]"), n = this._volumePanel.querySelector("[data-part=\"vol-thumb\"]"), r = this._volumePanel.querySelector("[data-part=\"vol-pct\"]");
			t && (t.style.width = e + "%"), n && (n.style.left = e + "%"), r && (r.textContent = e + "%");
		}
	}
	_toggleVolumePanel() {
		let e = this._ensureVolumePanel();
		this._updateVolumeUI();
		let t = this.$("[data-act=\"volume\"]");
		if (t) {
			let n = t.getBoundingClientRect(), r = e.offsetWidth || 200;
			e.style.left = Math.max(8, Math.min(window.innerWidth - r - 8, n.left + n.width / 2 - r / 2)) + "px", e.style.bottom = window.innerHeight - n.top + 8 + "px";
		}
		e.classList.toggle("open");
	}
	_ensureListPanel() {
		if (this._listPanel) return this._listPanel;
		let e = document.createElement("div");
		e.className = "playlist-panel", e.innerHTML = `
      <style>
        .playlist-panel {
          position: fixed;
          right: 32px;
          bottom: 60px;
          width: 340px;
          max-height: 420px;
          background: #0f0e0d;
          border: 1px solid #2a2a2a;
          border-radius: 6px;
          box-shadow: 0 20px 60px rgba(0, 0, 0, 0.7);
          display: none;
          flex-direction: column;
          z-index: 50;
          overflow: hidden;
          animation: panelIn 0.18s ease-out;
        }
        .playlist-panel.open { display: flex; }
        @keyframes panelIn {
          from { opacity: 0; transform: translateY(8px); }
          to   { opacity: 1; transform: translateY(0); }
        }
        .playlist-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          padding: 13px 16px;
          font-family: var(--font-sans);
          font-size: 13px;
          font-weight: 500;
          letter-spacing: 0.06em;
          color: #ffffff;
          border-bottom: 1px solid #2a2a2a;
          background: #0a0a0a;
        }
        .playlist-count {
          font-family: var(--font-sans);
          font-size: 11px;
          color: #6b6b6b;
          font-variant-numeric: tabular-nums;
        }
        .playlist-body {
          overflow-y: auto;
          max-height: 360px;
        }
        .playlist-item {
          display: flex;
          align-items: center;
          gap: 10px;
          padding: 10px 16px;
          cursor: pointer;
          border-bottom: 1px solid #1a1a1a;
          transition: background 0.15s ease;
        }
        .playlist-item:last-child { border-bottom: none; }
        .playlist-item:hover { background: rgba(255,255,255,0.04); }
        .playlist-item.active { background: rgba(var(--brand-rgb), 0.12); }
        .playlist-item.active .playlist-title { color: var(--brand-primary); }
        .playlist-index {
          width: 18px;
          text-align: center;
          font-family: var(--font-sans);
          font-size: 11px;
          color: #6b6b6b;
          flex-shrink: 0;
          font-variant-numeric: tabular-nums;
        }
        .playlist-item.active .playlist-index { color: var(--brand-primary); }
        .playlist-info {
          flex: 1;
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 3px;
        }
        .playlist-title {
          font-family: var(--font-sans);
          font-size: 12px;
          font-weight: 500;
          letter-spacing: 0.01em;
          color: #ffffff;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
          margin: 0;
        }
        .playlist-artist {
          font-family: var(--font-sans);
          font-size: 10px;
          color: #6b6b6b;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
          margin: 0;
        }
        .playlist-duration {
          font-family: var(--font-sans);
          font-size: 10px;
          color: #6b6b6b;
          flex-shrink: 0;
          font-variant-numeric: tabular-nums;
        }
        .playlist-playing {
          display: none;
          align-items: flex-end;
          gap: 2px;
          width: 12px;
          height: 12px;
          flex-shrink: 0;
        }
        .playlist-item.active .playlist-playing { display: flex; }
        .playlist-playing span {
          display: inline-block;
          width: 2px;
          background: var(--brand-primary);
          border-radius: 1px;
          animation: eq 1s ease-in-out infinite;
        }
        .playlist-playing span:nth-child(2) { animation-delay: 0.2s; }
        .playlist-playing span:nth-child(3) { animation-delay: 0.4s; }
        @keyframes eq {
          0%, 100% { height: 4px; }
          50% { height: 12px; }
        }
      </style>
      <div class="playlist-header">
        <span>播放列表</span>
        <span class="playlist-count">${this._tracks.length} 首</span>
      </div>
      <div class="playlist-body" data-part="list-body"></div>
    `, document.body.appendChild(e);
		let t = this.$("[data-act=\"list\"]");
		return document.addEventListener("click", (n) => {
			e.classList.contains("open") && (e.contains(n.target) || t && t.contains(n.target) || e.classList.remove("open"));
		}), this._listPanel = e, e;
	}
	_renderList() {
		if (!this._listPanel) return;
		let e = this._listPanel.querySelector("[data-part=\"list-body\"]");
		e.innerHTML = this._tracks.map((e, t) => `
      <div class="playlist-item ${t === this._index ? "active" : ""}" data-index="${t}">
        <span class="playlist-index">${t + 1}</span>
        <div class="playlist-info">
          <p class="playlist-title">${a(e.title)}</p>
          <p class="playlist-artist">${a(e.artist)}</p>
        </div>
        <span class="playlist-duration">${e.duration && isFinite(e.duration) ? M(e.duration) : "--:--"}</span>
        <span class="playlist-playing" aria-hidden="true"><span></span><span></span><span></span></span>
      </div>
    `).join(""), e.querySelectorAll(".playlist-item").forEach((e) => {
			e.addEventListener("click", () => {
				let t = parseInt(e.dataset.index || "0", 10);
				this._loadTrack(t, !0);
			});
		});
	}
	_toggleList() {
		let e = this._ensureListPanel();
		this._renderList();
		let t = this.$("[data-act=\"list\"]");
		if (t) {
			let n = t.getBoundingClientRect(), r = e.offsetWidth || 320;
			e.style.left = Math.max(8, Math.min(window.innerWidth - r - 8, n.left + n.width / 2 - r / 2)) + "px", e.style.bottom = "56px";
		}
		e.classList.toggle("open");
	}
	_bindKeyboard() {
		document.addEventListener("keydown", (e) => {
			let t = e.target && e.target.tagName || "";
			t !== "INPUT" && t !== "TEXTAREA" && (e.code === "Space" ? (e.preventDefault(), this._toggle()) : e.code === "ArrowLeft" ? this._prev() : e.code === "ArrowRight" && this._next());
		});
	}
	_play() {
		if (this._audio) {
			let e = this._audio.play();
			e && e.catch && e.catch(() => {});
		}
	}
	_pause() {
		this._audio && this._audio.pause();
	}
	_toggle() {
		this._audio && (this._audio.paused ? this._play() : this._pause());
	}
	_nextIndex() {
		if (this._mode === "shuffle") {
			if (this._tracks.length <= 1) return 0;
			let e;
			do
				e = Math.floor(Math.random() * this._tracks.length);
			while (e === this._index);
			return e;
		}
		return (this._index + 1) % this._tracks.length;
	}
	_prevIndex() {
		return this._mode === "shuffle" ? this._nextIndex() : (this._index - 1 + this._tracks.length) % this._tracks.length;
	}
	_loadTrack(e, t) {
		this._index = (e + this._tracks.length) % this._tracks.length, this._current = 0, this._audio && (this._audio.src = this._tracks[this._index].src), this._updateInfo(), t && this._play(), this._renderList(), this.emit("track-change", {
			index: this._index,
			track: this._tracks[this._index]
		});
	}
	_next() {
		this._loadTrack(this._nextIndex(), !0);
	}
	_prev() {
		if (this._current > 3) {
			this._current = 0, this._audio && (this._audio.currentTime = 0), this._updateProgress();
			return;
		}
		this._loadTrack(this._prevIndex(), !0);
	}
	_cycleMode() {
		let e = N.indexOf(this._mode);
		this._mode = N[(e + 1) % N.length], d.set("player:mode", this._mode), this._renderMode();
	}
	_renderMode() {
		let e = this.$("[data-part=\"mode-btn\"]");
		if (!e) return;
		let t = this._mode, n = t === "loop" ? "列表循环" : t === "single" ? "单曲循环" : "随机播放";
		e.dataset.mode = t, e.title = n, e.setAttribute("aria-label", n), e.classList.add("is-mode-active");
		let r = e.querySelector("svg");
		if (!r) return;
		r.innerHTML = "", r.setAttribute("viewBox", "0 0 24 24"), r.setAttribute("width", "17"), r.setAttribute("height", "17");
		let i = (e) => {
			let t = document.createElementNS("http://www.w3.org/2000/svg", "path");
			t.setAttribute("d", e), r.appendChild(t);
		};
		t === "shuffle" ? [
			"M16 3h5v5",
			"M4 20L21 3",
			"M21 16v5h-5",
			"M15 15l6 6",
			"M4 4l5 5"
		].forEach(i) : t === "single" ? [
			"m17 2 4 4-4 4",
			"M3 11v-1a4 4 0 0 1 4-4h14",
			"m7 22-4-4 4-4",
			"M21 13v1a4 4 0 0 1-4 4H3",
			"M11 10h1v4"
		].forEach(i) : [
			"m17 2 4 4-4 4",
			"M3 11v-1a4 4 0 0 1 4-4h14",
			"m7 22-4-4 4-4",
			"M21 13v1a4 4 0 0 1-4 4H3"
		].forEach(i);
	}
	_syncPlayIcon() {
		let e = this.$("[data-act=\"play\"]");
		e && e.classList.toggle("is-playing", this._isPlaying);
	}
	_updateInfo() {
		let e = this._tracks[this._index], t = this.$("[data-part=\"title\"]"), n = this.$("[data-part=\"artist\"]"), r = this.$("[data-part=\"duration\"]");
		t && (t.textContent = e.title), n && (n.textContent = e.artist), r && (r.textContent = e.duration && isFinite(e.duration) ? M(e.duration) : "00:00"), this._updateProgress();
	}
	_updateProgress() {
		let e = this._tracks[this._index], t = e.duration && e.duration > 0 ? Math.min(100, this._current / e.duration * 100) : 0, n = this.$("[data-part=\"fill\"]"), r = this.$("[data-part=\"thumb\"]"), i = this.$("[data-part=\"current\"]");
		n && (n.style.width = t + "%"), r && (r.style.left = t + "%"), i && (i.textContent = M(this._current));
	}
	_prefillDurations() {
		this._tracks.forEach((e, t) => {
			let n = new Audio(e.src);
			n.preload = "metadata", n.addEventListener("durationchange", () => {
				n.duration && isFinite(n.duration) && (e.duration = n.duration, t === this._index && this._updateInfo(), this._listPanel && this._renderList());
			});
		});
	}
};
customElements.get("music-player") || customElements.define("music-player", de);
var P = 420, F = 300, fe = 240, pe = ["/images/extracted/article/20160126052324@2x.png", "/images/extracted/article/4@2x.png"], me = class e extends r {
	static get observedAttributes() {
		return [];
	}
	_article = null;
	_articles = [];
	_icons = {};
	_catNames = {};
	_giscusConfig = null;
	_viewerGen = 0;
	_swapBusy = !1;
	_swapPendingId = null;
	_cardEl = null;
	_trackedPageViewArticleId = null;
	_pageViewFallbackTimer = null;
	_giscusRenderKey = "";
	_giscusRenderGeneration = 0;
	set article(e) {
		this._article = e, this._renderArticle(), e && this._trackPageView(e);
	}
	set articles(e) {
		this._articles = e;
	}
	set icons(e) {
		this._icons = e;
	}
	set catNames(e) {
		this._catNames = e;
	}
	set giscusConfig(e) {
		this._giscusConfig = e, this._renderComments();
	}
	static commentItemTemplate(e, t) {
		return `
      <div class="comment-item">
        <div class="comment-row">
          <img class="comment-avatar" src="${a(e.avatar || t.avatarB || "")}" alt="头像">
          <div class="comment-main">
            <p class="comment-text"><span class="comment-name">${a(e.name)}</span>：<span class="comment-body">${a(e.text)}</span></p>
            <p class="comment-meta">
              <span class="comment-time">${a(e.time)}</span>
              <span class="comment-actions">
                <span class="comment-action" data-act="reply">回复</span>
                <span class="comment-action" data-act="like">
                  <img src="${a(t.zan || "")}" alt="赞">
                  <span class="like-count">${Number(e.likes) || 0}</span>
                </span>
              </span>
            </p>
          </div>
        </div>
        <div class="reply-form" hidden>
          <textarea placeholder="回复 ${a(e.name)}..."></textarea>
          <div class="reply-form-actions">
            <button class="reply-cancel" type="button">取消</button>
            <button class="reply-submit" type="button">回复</button>
          </div>
        </div>
        <div class="reply-list"></div>
      </div>
    `;
	}
	static recommendationItemTemplate(e, { href: t, isLast: n }) {
		let r = n ? "" : "border-bottom:1px solid #333;";
		return `
      <a class="recommend-item" href="${a(t)}" data-id="${a(e.id)}" data-cat="${a(e.cat)}"
         style="display:block;padding:13px 0;${r}text-decoration:none;">
        <div style="display:flex;gap:11px;">
          <img src="${a(e.cover)}" alt="" style="width:52px;height:52px;object-fit:cover;flex-shrink:0;border-radius:2px;">
          <div style="flex:1;min-width:0;">
            <p style="font-size:13px;line-height:1.6;letter-spacing:0.01em;color:#BDBAB5;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;">${a(e.title)}</p>
          </div>
        </div>
        <p style="font-size:11px;color:#6B6B6B;text-align:right;margin-top:9px;">${a(e.date)}</p>
      </a>
    `;
	}
	static viewerMetaTemplate(e, t) {
		return `
      <span><img src="${a(t.eye || "")}" alt=""><slot name="page-views">${a(e.views)}</slot></span>
      <span><img src="${a(t.comment || "")}" alt="">${a(e.commentCount)}</span>
      <span class="viewer-date"><img src="${a(t.calendar || "")}" alt="">${a(e.date)}</span>
    `;
	}
	_trackPageView(e) {
		if (this._trackedPageViewArticleId === e.id) return;
		let t = this.querySelector("#vercount_value_page_pv[slot=\"page-views\"]");
		if (!t) return;
		this._trackedPageViewArticleId = e.id, t.textContent = "•••", t.classList.add("is-loading"), t.setAttribute("aria-busy", "true");
		let n = (n) => {
			this._article?.id === e.id && (t.textContent = String(n), t.classList.remove("is-loading"), t.removeAttribute("aria-busy"), this._pageViewFallbackTimer !== null && (window.clearTimeout(this._pageViewFallbackTimer), this._pageViewFallbackTimer = null));
		};
		this._pageViewFallbackTimer !== null && window.clearTimeout(this._pageViewFallbackTimer), this._pageViewFallbackTimer = window.setTimeout(() => {
			n(Number(e.views) || 0);
		}, 5e3), te(e.id).then((t) => {
			n(t ?? (Number(e.views) || 0));
		});
	}
	static articleParagraphsTemplate(e = []) {
		let t = /<(?:address|article|aside|blockquote|details|div|dl|fieldset|figure|footer|form|h[1-6]|header|hr|main|nav|ol|p|pre|section|table|ul)\b/i;
		return e.map((e) => t.test(e) ? e : `<p>${e}</p>`).join("");
	}
	static emojiItemsTemplate(e) {
		return e.map((e) => `<span class="emoji-item">${a(e)}</span>`).join("");
	}
	render() {
		return `
      <style>
        :host {
          display: none;
          position: absolute;
          inset: 0;
          z-index: 50;
        }
        :host(.is-open) {
          display: flex;
        }
        :host *::-webkit-scrollbar {
          width: 6px;
          height: 6px;
        }
        :host *::-webkit-scrollbar-track {
          background: transparent;
        }
        :host *::-webkit-scrollbar-thumb {
          background: #444;
          border-radius: 3px;
        }
        :host *::-webkit-scrollbar-thumb:hover {
          background: #555;
        }
        .viewer-backdrop {
	          position: absolute;
	          inset: 0;
	          background: rgba(0,0,0,0.5);
	          cursor: pointer;
	        }
        .viewer-ghost {
          display: none;
          position: absolute;
          z-index: 2;
          object-fit: cover;
          pointer-events: none;
          border-radius: 4px;
        }
        .viewer-panel {
          position: relative;
          z-index: 1;
          display: flex;
          flex-direction: column;
          width: 100%;
          height: 100%;
          background:
            radial-gradient(circle at 18% 0%, rgba(var(--brand-rgb), 0.055), transparent 30%),
            rgba(15, 14, 13, 0.975);
          overflow: hidden;
        }
        .viewer-panel.viewer-open {
          display: flex;
        }
        .viewer-panel.is-anim {
          will-change: transform, opacity;
        }
        .viewer-head {
          display: flex;
          align-items: center;
          justify-content: space-between;
          min-height: 64px;
          padding: 14px clamp(20px, 3vw, 40px);
          border-bottom: 1px solid #2a2a2a;
          background: rgba(15, 14, 13, 0.82);
          backdrop-filter: blur(16px);
          flex-shrink: 0;
        }
        .viewer-headline {
          display: flex;
          align-items: center;
          gap: 8px;
          font-size: 12px;
          color: #6b6b6b;
        }
        .viewer-breadcrumb {
          color: #9e9d99;
          text-decoration: none;
          transition: color 0.2s;
        }
        .viewer-breadcrumb:hover {
          color: #ffffff;
        }
        .viewer-sep {
          color: #5d5a59;
        }
        .viewer-cat-name {
          color: #c9c6c2;
          text-decoration: none;
          transition: color 0.2s;
        }
        .viewer-cat-name:hover {
          color: #ffffff;
        }
        .viewer-close {
          display: flex;
          align-items: center;
          justify-content: center;
          width: 32px;
          height: 32px;
          border: none;
          background: none;
          cursor: pointer;
          color: #9e9d99;
          border-radius: 50%;
          transition: background 0.2s, color 0.2s;
        }
        .viewer-close:hover {
          background: rgba(255,255,255,0.06);
          color: #ffffff;
        }
        .viewer-body {
          display: flex;
          flex: 1;
          min-height: 0;
        }
        .viewer-body.viewer-mask {
          opacity: 0.3;
        }
        .viewer-body.viewer-in {
          animation: fadeInUp ${fe}ms ease-out both;
        }
        @keyframes fadeInUp {
          from { opacity: 0.3; transform: translateY(12px); }
          to { opacity: 1; transform: translateY(0); }
        }
        .viewer-main {
          flex: 1;
          overflow-y: auto;
          overscroll-behavior: contain;
          scrollbar-width: thin;
          scrollbar-color: #444 transparent;
          padding: clamp(32px, 5vw, 64px) clamp(24px, 5vw, 72px) 96px;
          scroll-behavior: smooth;
        }
        .viewer-main > * {
          width: 100%;
          max-width: 860px;
          margin-inline: auto;
        }
        .viewer-main.viewer-swap-out {
          opacity: 0;
          transition: opacity 0.12s ease-out;
        }
        .viewer-main.viewer-swap-in {
          animation: fadeIn 0.2s ease-out both;
        }
        @keyframes fadeIn {
          from { opacity: 0; }
          to { opacity: 1; }
        }
        .viewer-title {
          font-size: clamp(28px, 3vw, 38px);
          font-weight: 650;
          line-height: 1.25;
          letter-spacing: -0.025em;
          color: #ffffff;
          margin: 0 auto 18px;
          text-wrap: balance;
          overflow-wrap: anywhere;
        }
        .viewer-meta {
          display: flex;
          flex-wrap: wrap;
          align-items: center;
          gap: 6px 16px;
          font-size: 12px;
          color: #8f8b86;
          margin: 0 auto 16px;
        }
        .viewer-meta span {
          display: inline-flex;
          align-items: center;
          gap: 4px;
        }
        .viewer-meta img {
          width: 14px;
          height: 14px;
          object-fit: contain;
          filter: brightness(0) invert(0.62);
          transition: filter 0.2s ease;
        }
        ::slotted([slot="page-views"].is-loading) {
          min-width: 1.55em;
          color: var(--brand-primary, #eb4f38);
          letter-spacing: 0.08em;
          animation: viewCountPulse 0.8s ease-in-out infinite;
        }
        @keyframes viewCountPulse {
          0%, 100% { opacity: 0.3; }
          50% { opacity: 1; }
        }
        @media (prefers-reduced-motion: reduce) {
          ::slotted([slot="page-views"].is-loading) { animation: none; opacity: 0.7; }
        }
        .viewer-tags {
          display: flex;
          flex-wrap: wrap;
          gap: 8px;
          margin: 0 auto 28px;
        }
        .viewer-tag {
          display: inline-block;
          padding: 4px 11px;
          font-size: 12px;
          color: #9e9d99;
          background: rgba(255,255,255,0.05);
          border: 1px solid #2a2a2a;
          border-radius: 999px;
          cursor: pointer;
          text-decoration: none;
          transition: color 0.2s, border-color 0.2s, background 0.2s;
        }
        .viewer-tag:hover {
          color: #eb4f38;
          border-color: #eb4f38;
          background: rgba(235,79,56,0.08);
        }
        .viewer-cover {
          display: block;
          width: 100%;
          aspect-ratio: 16 / 9;
          object-fit: cover;
          border: 1px solid rgba(255,255,255,0.07);
          border-radius: 12px;
          margin: 0 auto clamp(32px, 5vw, 52px);
          box-shadow: 0 24px 70px rgba(0,0,0,0.28);
        }
        .article-content {
          font-size: 16px;
          line-height: 1.9;
          color: #bbb7b1;
          letter-spacing: 0.012em;
          overflow-wrap: break-word;
        }
        .article-content p {
          margin: 0 0 22px;
        }
        .article-content > :first-child {
          margin-top: 0;
        }
        .article-content h2,
        .article-content h3,
        .article-content h4 {
          color: #f5f3f0;
          font-weight: 650;
          line-height: 1.4;
          letter-spacing: -0.015em;
          text-wrap: balance;
        }
        .article-content h2 {
          margin: 52px 0 20px;
          padding-top: 8px;
          font-size: 25px;
        }
        .article-content h3 {
          margin: 38px 0 16px;
          font-size: 20px;
        }
        .article-content h4 {
          margin: 30px 0 14px;
          font-size: 17px;
        }
        .article-content .headerlink {
          color: inherit;
          text-decoration: none;
        }
        .article-content .headerlink::before {
          content: '#';
          margin-right: 10px;
          color: var(--brand-primary, #eb4f38);
          font-weight: 500;
          opacity: 0.72;
        }
        .article-content a:not(.headerlink) {
          color: #f08070;
          text-decoration-color: rgba(var(--brand-rgb), 0.45);
          text-decoration-thickness: 1px;
          text-underline-offset: 4px;
          transition: color 0.18s ease, text-decoration-color 0.18s ease;
        }
        .article-content a:not(.headerlink):hover {
          color: #ff9a8c;
          text-decoration-color: currentColor;
        }
        .article-content strong {
          color: #e7e3de;
          font-weight: 650;
        }
        .article-content ul,
        .article-content ol {
          margin: 0 0 24px;
          padding-left: 1.45em;
        }
        .article-content li {
          padding-left: 0.35em;
        }
        .article-content li + li {
          margin-top: 8px;
        }
        .article-content li::marker {
          color: var(--brand-primary, #eb4f38);
        }
        .article-content blockquote {
          margin: 28px 0;
          padding: 18px 22px;
          border-left: 3px solid var(--brand-primary, #eb4f38);
          border-radius: 0 9px 9px 0;
          background: rgba(var(--brand-rgb), 0.075);
          color: #c9c4bd;
        }
        .article-content blockquote > :last-child {
          margin-bottom: 0;
        }
        .article-content code {
          padding: 0.16em 0.42em;
          border: 1px solid rgba(255,255,255,0.08);
          border-radius: 5px;
          background: rgba(255,255,255,0.055);
          color: #e8b4aa;
          font-size: 0.9em;
        }
        .article-content pre code,
        .article-content .highlight code {
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          color: inherit;
          font-size: inherit;
        }
        .article-content pre,
        .article-content figure.highlight {
          margin: 28px 0;
          border: 1px solid #292725;
          border-radius: 10px;
          background: #0a0909;
          box-shadow: inset 0 1px rgba(255,255,255,0.025);
          overflow-x: auto;
        }
        .article-content > pre {
          padding: 18px 20px;
        }
        .article-content figure.highlight table {
          width: 100%;
          min-width: max-content;
          border-collapse: collapse;
        }
        .article-content figure.highlight td {
          padding: 18px 0;
          vertical-align: top;
        }
        .article-content figure.highlight .gutter {
          width: 1%;
          border-right: 1px solid #242220;
          color: #514e4a;
          text-align: right;
          user-select: none;
        }
        .article-content figure.highlight .gutter pre {
          padding: 0 13px;
        }
        .article-content figure.highlight .code pre {
          padding: 0 18px;
        }
        .article-content pre,
        .article-content figure.highlight pre {
          font-size: 13px;
          line-height: 1.75;
          tab-size: 2;
        }
        .article-content img {
          display: block;
          max-width: 100%;
          height: auto;
          margin: 30px auto;
          border-radius: 10px;
        }
        .article-content hr {
          height: 1px;
          margin: 44px 0;
          border: 0;
          background: linear-gradient(90deg, transparent, #34312f 15%, #34312f 85%, transparent);
        }
        .article-content > table:not(.highlight) {
          display: block;
          width: 100%;
          margin: 28px 0;
          border-collapse: collapse;
          overflow-x: auto;
        }
        .article-content > table:not(.highlight) th,
        .article-content > table:not(.highlight) td {
          padding: 10px 14px;
          border: 1px solid #302e2b;
          text-align: left;
        }
        .article-content > table:not(.highlight) th {
          color: #e7e3de;
          background: rgba(255,255,255,0.04);
        }
        .viewer-divider {
          height: 1px;
          background: #333;
          margin: 56px auto 36px;
        }
        .viewer-comments-head {
          display: flex;
          align-items: center;
          gap: 8px;
          margin-bottom: 16px;
        }
        .viewer-comments-head h2 {
          font-size: 16px;
          font-weight: 500;
          color: #f2f2f2;
          margin: 0;
        }
        .viewer-dot {
          width: 3px;
          height: 3px;
          border-radius: 50%;
          background: #5d5a59;
        }
        .comment-total {
          font-size: 12px;
          color: #6b6b6b;
        }
        .viewer-comments-line {
          height: 1px;
          background: #333;
          margin-bottom: 20px;
        }
        .legacy-comments[hidden],
        .giscus-container[hidden] {
          display: none !important;
        }
        .giscus-container {
          min-height: 160px;
          padding-top: 20px;
        }
        .giscus-container ::slotted(.giscus) {
          display: block;
          width: 100%;
        }
        .giscus-status {
          margin: 0;
          padding: 28px 18px;
          border: 1px dashed #3b3937;
          border-radius: 6px;
          color: #8d8984;
          font-size: 13px;
          line-height: 1.7;
          text-align: center;
        }
        .giscus-status strong {
          display: block;
          margin-bottom: 8px;
          color: #d8d4cf;
          font-size: 15px;
        }
        .giscus-status p {
          margin: 0;
        }
        .giscus-status ol {
          max-width: 520px;
          margin: 16px auto 0;
          padding-left: 24px;
          text-align: left;
        }
        .giscus-status li + li {
          margin-top: 7px;
        }
        .giscus-status a {
          color: var(--brand-primary, #eb4f38);
          text-underline-offset: 3px;
        }
        .viewer-comment-form {
          margin-bottom: 24px;
        }
        .comment-input-box {
          margin-bottom: 8px;
        }
        .comment-input-box textarea {
          width: 100%;
          padding: 10px 14px;
          background: #252828;
          border: 1px solid #333;
          border-radius: 4px;
          color: #f2f2f2;
          font-size: 13px;
          font-family: inherit;
          line-height: 1.5;
          resize: none;
          outline: none;
          transition: border-color 0.2s;
        }
        .comment-input-box textarea:focus {
          border-color: #eb4f38;
        }
        .comment-form-actions {
          display: flex;
          justify-content: space-between;
          align-items: center;
        }
        .comment-form-actions button[data-act="emoji"] {
          width: 30px;
          height: 30px;
          display: flex;
          align-items: center;
          justify-content: center;
          background: transparent;
          border: 1px solid #3C3936;
          border-radius: 4px;
          cursor: pointer;
          transition: border-color 0.2s ease, background 0.2s ease;
        }
        .comment-form-actions button[data-act="emoji"]:hover {
          border-color: var(--brand-primary);
          background: rgba(var(--brand-rgb), 0.08);
        }
        .comment-form-actions button[data-act="emoji"] img {
          width: 17px;
          height: 17px;
          object-fit: contain;
          filter: brightness(0) invert(0.62);
          opacity: 1;
          transition: filter 0.2s ease, transform 0.2s ease;
        }
        .comment-form-actions button[data-act="emoji"]:hover img {
          filter: brightness(0) invert(1);
          transform: scale(1.1);
        }
        .comment-form-actions button[data-act="emoji"]:active {
          transform: scale(0.94);
        }
        .comment-form-actions button[type="submit"] {
          padding: 6px 20px;
          background: #eb4f38;
          color: #ffffff;
          border: none;
          border-radius: 3px;
          font-size: 13px;
          cursor: pointer;
          font-family: inherit;
          transition: background 0.2s;
        }
        .comment-form-actions button[type="submit"]:disabled {
          background: #333;
          color: #6b6b6b;
          cursor: not-allowed;
        }
        .comment-form-actions button[type="submit"].is-active {
          background: #ee5b44;
        }
        .comment-item {
          padding: 12px 0;
        }
        .comment-row {
          display: flex;
          gap: 10px;
        }
        .comment-avatar {
          width: 32px;
          height: 32px;
          border-radius: 50%;
          object-fit: cover;
          flex-shrink: 0;
        }
        .comment-main {
          flex: 1;
          min-width: 0;
        }
        .comment-text {
          font-size: 13px;
          line-height: 1.6;
          color: #b3b3b3;
          margin: 0 0 4px;
        }
        .comment-name {
          color: #c9c6c2;
          font-weight: 500;
        }
        .comment-body {
          color: #b3b3b3;
        }
        .comment-meta {
          display: flex;
          align-items: center;
          gap: 12px;
          font-size: 12px;
          color: #6b6b6b;
          margin: 0;
        }
        .comment-actions {
          display: flex;
          align-items: center;
          gap: 8px;
        }
        .comment-action {
          cursor: pointer;
          transition: color 0.2s;
          display: flex;
          align-items: center;
          gap: 3px;
          color: #9e9d99;
        }
        .comment-action:hover {
          color: var(--brand-primary, #eb4f38);
        }
        .comment-action img {
          width: 14px;
          height: 14px;
          object-fit: contain;
          filter: brightness(0) invert(0.62);
          opacity: 1;
          transition: filter 0.2s ease;
        }
        .comment-action:hover img {
          filter: brightness(0) invert(1);
        }
        .comment-action.liked {
          color: #eb4f38;
        }
        .comment-action.liked img {
          filter: brightness(0) invert(1) drop-shadow(0 0 4px rgba(var(--brand-rgb), 0.65));
        }
        .comment-empty {
          text-align: center;
          padding: 24px;
          color: #6b6b6b;
          font-size: 13px;
        }
        .reply-form {
          margin: 8px 0 8px 42px;
        }
        .reply-form textarea {
          width: 100%;
          padding: 8px 12px;
          background: #252828;
          border: 1px solid #333;
          border-radius: 3px;
          color: #f2f2f2;
          font-size: 12px;
          font-family: inherit;
          resize: none;
          outline: none;
        }
        .reply-form-actions {
          display: flex;
          gap: 8px;
          margin-top: 6px;
        }
        .reply-cancel, .reply-submit {
          padding: 4px 12px;
          border: none;
          border-radius: 3px;
          font-size: 12px;
          cursor: pointer;
          font-family: inherit;
        }
        .reply-cancel {
          background: #1d1d1d;
          color: #9e9d99;
        }
        .reply-submit {
          background: #eb4f38;
          color: #ffffff;
        }
        .reply-list {
          margin-left: 42px;
        }
        .reply-list .comment-item {
          padding: 8px 0;
        }
        .reply-list .comment-item .comment-text {
          font-size: 12px;
        }
        .viewer-aside {
          width: 300px;
          flex-shrink: 0;
          overflow-y: auto;
          scrollbar-width: thin;
          scrollbar-color: #444 transparent;
          padding: 40px 28px 96px;
          border-left: 1px solid #2a2a2a;
          background: rgba(0,0,0,0.08);
          min-height: 100%;
        }
        .viewer-aside[hidden] {
          display: none !important;
        }
        .viewer-aside.viewer-swap-out {
          opacity: 0;
          transition: opacity 0.12s ease-out;
        }
        .viewer-aside.viewer-swap-in {
          animation: fadeIn 0.2s ease-out both;
        }
        .viewer-aside-head {
          display: flex;
          align-items: center;
          gap: 8px;
          margin-bottom: 16px;
        }
        .viewer-aside-head h2 {
          font-size: 14px;
          font-weight: 500;
          color: #f2f2f2;
          margin: 0;
        }
        .viewer-aside-head p {
          font-size: 11px;
          color: #6b6b6b;
          margin: 0;
        }
        .viewer-rec-empty {
          text-align: center;
          padding: 24px;
          color: #6b6b6b;
          font-size: 13px;
        }
        .emoji-panel {
          display: none;
          position: fixed;
          z-index: 2000;
          background: #1d1d1d;
          border: 1px solid #333;
          border-radius: 6px;
          padding: 8px;
          gap: 4px;
          flex-wrap: wrap;
          width: 192px;
        }
        .emoji-panel.open {
          display: flex;
        }
        .emoji-item {
          width: 32px;
          height: 32px;
          display: flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          border-radius: 4px;
          font-size: 18px;
          transition: background 0.15s;
        }
        .emoji-item:hover {
          background: rgba(255,255,255,0.06);
        }
        @media (max-width: 1000px) {
          .viewer-main {
            padding-inline: 32px;
          }
          .viewer-aside {
            width: 250px;
            padding-inline: 22px;
          }
        }
        @media (max-width: 760px) {
          :host {
            position: fixed;
          }
          .viewer-backdrop {
            display: none;
          }
          .viewer-panel {
            background: #0f0e0d;
          }
          .viewer-head {
            min-height: 58px;
            padding: 10px 16px;
          }
          .viewer-headline {
            min-width: 0;
            overflow: hidden;
            white-space: nowrap;
          }
          .viewer-cat-name {
            overflow: hidden;
            text-overflow: ellipsis;
          }
          .viewer-close {
            flex: 0 0 36px;
            width: 36px;
            height: 36px;
          }
          .viewer-body {
            display: block;
            overflow-x: hidden;
            overflow-y: auto;
            overscroll-behavior: contain;
            scrollbar-width: thin;
            scrollbar-color: #444 transparent;
          }
          .viewer-main {
            overflow: visible;
            padding: 28px 20px 96px;
          }
          .viewer-title {
            font-size: clamp(25px, 8vw, 32px);
            line-height: 1.3;
          }
          .viewer-cover {
            margin-bottom: 34px;
            border-radius: 9px;
          }
          .article-content {
            font-size: 16px;
            line-height: 1.85;
          }
          .article-content h2 {
            margin-top: 42px;
            font-size: 22px;
          }
          .article-content h3 {
            margin-top: 32px;
            font-size: 19px;
          }
          .article-content pre,
          .article-content figure.highlight {
            border-radius: 8px;
          }
          .viewer-aside {
            width: auto;
            min-height: 0;
            padding: 30px 20px 104px;
            border-top: 1px solid #2a2a2a;
            border-left: 0;
            overflow: visible;
          }
        }
        @media (max-width: 420px) {
          .viewer-main {
            padding-inline: 16px;
          }
          .viewer-meta {
            gap: 7px 12px;
          }
          .viewer-tags {
            gap: 7px;
          }
          .viewer-cover {
            aspect-ratio: 4 / 3;
          }
          .article-content h2 {
            font-size: 21px;
          }
          .article-content blockquote {
            padding: 16px 18px;
          }
        }
      </style>
      <div class="viewer-backdrop" data-part="backdrop"></div>
      <img class="viewer-ghost" data-part="ghost" alt="" draggable="false">
      <section class="viewer-panel" data-part="panel">
        <header class="viewer-head">
          <div class="viewer-headline">
            <a href="./" class="viewer-breadcrumb">首页</a>
            <span class="viewer-sep">&gt;</span>
            <a href="./" class="viewer-cat-name" data-part="cat-name"></a>
          </div>
          <button type="button" class="viewer-close" data-part="close-btn" aria-label="关闭阅读（Esc）" title="关闭（Esc）">
            <svg width="15" height="15" viewBox="0 0 16 16" fill="none">
              <path d="M3 3l10 10M13 3L3 13" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
            </svg>
          </button>
        </header>
        <div class="viewer-body" data-part="body">
          <main class="viewer-main" data-part="main">
            <h1 class="viewer-title" data-part="title"></h1>
            <div class="viewer-meta" data-part="meta"></div>
            <div class="viewer-tags" data-part="tags"></div>
            <img class="viewer-cover" data-part="cover" alt="文章封面">
            <div class="article-content" data-part="content"></div>
            <div class="viewer-divider"></div>
            <section class="viewer-comments">
              <div class="viewer-comments-head">
                <h2>评论</h2>
                <span class="viewer-dot"></span>
                <span class="comment-total" data-part="comment-total">共 0 条评论</span>
              </div>
              <div class="viewer-comments-line"></div>
              <div class="legacy-comments" data-part="legacy-comments">
                <form class="viewer-comment-form" data-part="comment-form" novalidate>
                  <div class="comment-input-box"><textarea rows="2" placeholder="说点什么吧…" data-part="comment-input"></textarea></div>
                  <div class="comment-form-actions">
                    <button type="button" data-act="emoji" aria-label="表情" title="表情">
                      <img src="/images/extracted/article/iconfont-biaoqing1@2x.png" alt="表情">
                    </button>
                    <button type="submit" disabled>评论</button>
                  </div>
                </form>
                <div class="comment-list" data-part="comment-list"></div>
              </div>
              <div class="giscus-container" data-part="giscus" hidden>
                <div data-part="giscus-status"></div>
                <slot name="giscus"></slot>
              </div>
            </section>
          </main>
          <aside class="viewer-aside" data-part="aside">
            <div class="viewer-aside-head">
              <h2>推荐文章</h2>
              <p>Recommend</p>
              <span class="viewer-dot"></span>
            </div>
            <div class="recommend-list" data-part="recommend-list"></div>
          </aside>
        </div>
      </section>
    `;
	}
	mounted() {
		this._setupBackdrop(), this._setupCloseButton(), this._setupCommentEvents(), this._setupEmoji(), this._setupKeyboard();
	}
	_setupBackdrop() {
		this.$("[data-part=\"backdrop\"]")?.addEventListener("click", () => this.closeWithFlip());
	}
	_setupCloseButton() {
		this.$("[data-part=\"close-btn\"]")?.addEventListener("click", () => this.closeWithFlip());
	}
	_setupKeyboard() {
		document.addEventListener("keydown", (e) => {
			if (!this.classList.contains("is-open")) return;
			let t = e.target && e.target.tagName || "";
			t !== "INPUT" && t !== "TEXTAREA" && e.key === "Escape" && this.closeWithFlip();
		});
	}
	_setupCommentEvents() {
		let e = this.shadow;
		e.addEventListener("click", (e) => {
			let t = e.target, n = t.closest("[data-act=\"like\"]");
			if (n && this.shadow.contains(n)) {
				let e = n.querySelector(".like-count");
				if (e) {
					let t = (+(e.textContent || "0") || 0) + 1;
					e.textContent = String(t);
				}
				n.classList.add("liked");
				return;
			}
			let r = t.closest("[data-act=\"reply\"]");
			if (r && this.shadow.contains(r)) {
				let e = r.closest(".comment-item"), t = e && e.querySelector(".reply-form");
				if (t && (t.hidden = !t.hidden, !t.hidden)) {
					let e = t.querySelector("textarea");
					e && e.focus();
				}
				return;
			}
			if (t.closest(".reply-cancel")) {
				let e = t.closest(".reply-form");
				e && (e.hidden = !0);
				return;
			}
			if (t.closest(".reply-submit")) {
				let e = t.closest(".reply-form"), n = t.closest(".comment-item");
				if (!e || !n) return;
				let r = e.querySelector("textarea"), i = (r?.value || "").trim();
				if (!i) {
					r?.focus();
					return;
				}
				let a = this._createCommentEl(i, "我");
				a.classList.add("is-reply");
				let o = n.querySelector(".reply-list");
				o && o.appendChild(a), e.hidden = !0, r && (r.value = "");
				return;
			}
			if (t.closest("[data-part=\"comment-form\"] button[type=\"submit\"]")) {
				e.preventDefault();
				let n = t.closest("[data-part=\"comment-form\"]");
				if (!n) return;
				let r = n.querySelector("textarea"), i = (r?.value || "").trim();
				if (!i) {
					r?.focus();
					return;
				}
				let a = n.parentElement?.querySelector(".comment-list"), o = a?.querySelector(".comment-empty");
				o && o.remove(), a && a.appendChild(this._createCommentEl(i, "我")), r && (r.value = ""), this._syncCommentCount();
				return;
			}
			let i = t.closest(".viewer-tag");
			if (i && this.shadow.contains(i)) {
				let t = i.dataset.tag;
				t && (e.preventDefault(), this.emit("tag-select", { tag: t }, { cancelable: !0 }).defaultPrevented || (this.emit("viewer-close"), setTimeout(() => {
					window.location.hash = "#tag=" + encodeURIComponent(t);
				}, 50)));
				return;
			}
			let a = t.closest(".recommend-item");
			if (a && this.shadow.contains(a)) {
				let t = a.dataset.id, n = a.dataset.cat;
				t && (e.preventDefault(), this.emit("article-select", {
					id: t,
					cat: n
				}));
			}
		}), e.addEventListener("input", (e) => {
			let t = e.target;
			if (t.matches("[data-part=\"comment-form\"] textarea") || t.matches("[data-part=\"comment-input\"]")) {
				let e = this.shadow.querySelector("[data-part=\"comment-form\"] button[type=\"submit\"]"), n = !!t.value.trim();
				e && (e.disabled = !n, e.classList.toggle("is-active", n));
			}
		});
	}
	_setupEmoji() {
		let t = this.$("[data-act=\"emoji\"]");
		if (!t) return;
		let n = document.createElement("div");
		n.className = "emoji-panel", n.style.cssText = "display:none !important;position:fixed !important;z-index:2000;background:#1d1d1d;border:1px solid #333;border-radius:6px;padding:8px;gap:4px;flex-wrap:wrap;width:192px;", n.innerHTML = e.emojiItemsTemplate([
			"😀",
			"😂",
			"😉",
			"😍",
			"👍",
			"🎉",
			"🤔",
			"😢",
			"🔥",
			"✨",
			"❤️",
			"🙌"
		]), document.body.appendChild(n), n.addEventListener("click", (e) => {
			if (!e.target.classList.contains("emoji-item")) return;
			let t = this.shadow.querySelector("[data-part=\"comment-input\"]");
			t && (t.value += e.target.textContent, t.focus(), t.dispatchEvent(new Event("input", { bubbles: !0 })));
		}), document.addEventListener("click", (e) => {
			n.classList.contains("open") && (n.contains(e.target) || t && t.contains(e.target) || n.classList.remove("open"));
		}), t.addEventListener("click", (e) => {
			e.stopPropagation();
			let r = t.getBoundingClientRect();
			n.style.left = r.left + "px", n.style.bottom = window.innerHeight - r.top + 6 + "px", n.classList.toggle("open");
		});
	}
	_createCommentEl(t, n) {
		let r = document.createElement("template");
		return r.innerHTML = e.commentItemTemplate({
			avatar: pe[0],
			likes: 0,
			name: n,
			text: t,
			time: this._formatNow()
		}, this._icons).trim(), r.content.firstElementChild;
	}
	_formatNow() {
		let e = /* @__PURE__ */ new Date(), t = (e) => String(e).padStart(2, "0");
		return `${e.getFullYear()}/${t(e.getMonth() + 1)}/${t(e.getDate())} ${t(e.getHours())}:${t(e.getMinutes())}`;
	}
	_syncCommentCount() {
		let e = this.$("[data-part=\"comment-list\"]"), t = this.$("[data-part=\"comment-total\"]");
		e && t && (t.textContent = `共 ${e.querySelectorAll(":scope > .comment-item").length} 条评论`);
	}
	_getGiscusMount() {
		let e = Array.from(this.children).find((e) => e instanceof HTMLElement && e.classList.contains("giscus") && e.slot === "giscus");
		if (e) return e;
		let t = document.createElement("div");
		return t.className = "giscus", t.slot = "giscus", this.appendChild(t), t;
	}
	_renderComments() {
		let e = this._article;
		if (!e) return;
		let t = this._giscusConfig, n = t?.enabled === !0, r = this.$("[data-part=\"legacy-comments\"]"), i = this.$("[data-part=\"giscus\"]"), a = this.$("[data-part=\"giscus-status\"]"), o = this.$("[data-part=\"comment-total\"]");
		if (!r || !i || !a) return;
		let s = this._getGiscusMount();
		if (r.hidden = n, i.hidden = !n, !n) {
			this._giscusRenderKey = "", this._giscusRenderGeneration++, this.querySelectorAll("script[data-giscus-loader]").forEach((e) => e.remove()), s.replaceChildren(), a.replaceChildren(), this._syncCommentCount();
			return;
		}
		if (o && (o.textContent = "由 GitHub Discussions 提供支持"), [
			t.repo,
			t.repoId,
			t.category,
			t.categoryId
		].some((e) => !String(e || "").trim())) {
			this._giscusRenderKey = "", this._giscusRenderGeneration++, this.querySelectorAll("script[data-giscus-loader]").forEach((e) => e.remove()), s.replaceChildren();
			let e = document.createElement("div");
			e.className = "giscus-status";
			let n = document.createElement("strong");
			n.textContent = "完成 Giscus 配置后即可开始评论";
			let r = document.createElement("p");
			r.textContent = "评论功能已默认开启，目前还缺少 GitHub Discussions 的必要配置。";
			let i = document.createElement("ol"), o = /^[\w.-]+\/[\w.-]+$/.test(t.repo) ? t.repo : "";
			[
				{
					text: "在目标仓库开启 Discussions",
					href: o ? `https://github.com/${o}/settings` : "https://docs.github.com/repositories/managing-your-repositorys-settings-and-features/enabling-features-for-your-repository"
				},
				{
					text: "为仓库安装 Giscus App",
					href: "https://github.com/apps/giscus"
				},
				{
					text: "在 Giscus 配置页生成并填写 repoId 与 categoryId",
					href: "https://giscus.app/zh-CN"
				}
			].forEach(({ text: e, href: t }) => {
				let n = document.createElement("li"), r = document.createElement("a");
				r.textContent = e, r.href = t, r.target = "_blank", r.rel = "noopener noreferrer", n.appendChild(r), i.appendChild(n);
			}), e.append(n, r, i), a.replaceChildren(e);
			return;
		}
		a.replaceChildren();
		let c = `${t.termPrefix ?? "article:"}${e.commentKey || e.id}`, l = JSON.stringify({
			repo: t.repo,
			repoId: t.repoId,
			category: t.category,
			categoryId: t.categoryId,
			term: c,
			strict: t.strict !== !1,
			reactionsEnabled: t.reactionsEnabled !== !1,
			emitMetadata: t.emitMetadata === !0,
			inputPosition: t.inputPosition ?? "top",
			theme: t.theme ?? "dark_dimmed",
			lang: t.lang ?? "zh-CN",
			loading: t.loading ?? "lazy"
		}), u = this.querySelector("script[data-giscus-loader]"), d = s.querySelector(".giscus-frame");
		if (this._giscusRenderKey === l && (u || d)) return;
		this._giscusRenderKey = l;
		let f = ++this._giscusRenderGeneration;
		this.querySelectorAll("script[data-giscus-loader]").forEach((e) => e.remove()), s.replaceChildren();
		let p = document.createElement("script");
		p.src = "https://giscus.app/client.js", p.async = !0, p.crossOrigin = "anonymous", p.dataset.giscusLoader = "", p.setAttribute("data-repo", t.repo), p.setAttribute("data-repo-id", t.repoId), p.setAttribute("data-category", t.category), p.setAttribute("data-category-id", t.categoryId), p.setAttribute("data-mapping", "specific"), p.setAttribute("data-term", c), p.setAttribute("data-strict", t.strict === !1 ? "0" : "1"), p.setAttribute("data-reactions-enabled", t.reactionsEnabled === !1 ? "0" : "1"), p.setAttribute("data-emit-metadata", t.emitMetadata === !0 ? "1" : "0"), p.setAttribute("data-input-position", t.inputPosition ?? "top"), p.setAttribute("data-theme", t.theme ?? "dark_dimmed"), p.setAttribute("data-lang", t.lang ?? "zh-CN"), p.setAttribute("data-loading", t.loading ?? "lazy"), p.addEventListener("load", () => {
			p.remove(), f !== this._giscusRenderGeneration && (this._giscusRenderKey = "", this._renderComments());
		}), p.addEventListener("error", () => {
			if (p.remove(), f !== this._giscusRenderGeneration) return;
			this._giscusRenderKey = "";
			let e = document.createElement("p");
			e.className = "giscus-status", e.textContent = "评论加载失败，请检查网络连接或 Giscus 配置。", s.replaceChildren(), a.replaceChildren(e);
		}), this.appendChild(p);
	}
	_renderArticle() {
		let t = this._article;
		if (!t) return;
		let n = this._icons, r = this.$("[data-part=\"title\"]");
		r && (r.textContent = t.title);
		let i = this.$("[data-part=\"meta\"]");
		i && (i.innerHTML = e.viewerMetaTemplate(t, n));
		let o = this.$("[data-part=\"tags\"]");
		if (o) {
			let e = (t.tags || []).map((e) => `<a class="viewer-tag" href="./#tag=${encodeURIComponent(e)}" data-tag="${a(e)}">${a(e)}</a>`).join("");
			o.innerHTML = e, o.style.display = e ? "" : "none";
		}
		let s = this.$("[data-part=\"cover\"]");
		s && (s.src = t.cover, s.alt = t.title);
		let c = this.$("[data-part=\"content\"]");
		c && (c.innerHTML = e.articleParagraphsTemplate(t.paragraphs));
		let l = t.comments || [], u = this.$("[data-part=\"comment-list\"]");
		u && (u.innerHTML = l.length ? l.map((t) => e.commentItemTemplate(t, n)).join("") : "<p class=\"comment-empty\">还没有评论，来抢沙发吧～</p>"), this._renderComments();
		let d = this.$("[data-part=\"recommend-list\"]");
		if (d) {
			let n = this._articles.filter((e) => e.id !== t.id), r = n.filter((e) => e.cat === t.cat), i = n.filter((e) => e.cat !== t.cat), a = [...r, ...i].slice(0, 6), o = this.$("[data-part=\"aside\"]");
			o && (o.hidden = a.length === 0), d.innerHTML = a.length ? a.map((t, n) => e.recommendationItemTemplate(t, {
				href: "#",
				isLast: n === a.length - 1
			})).join("") : "<p class=\"viewer-rec-empty\">暂无推荐</p>";
		}
		let f = this.$("[data-part=\"cat-name\"]");
		f && (f.textContent = this._catNames[t.cat] || t.cat, f.href = "./#cat=" + encodeURIComponent(t.cat)), this.$("[data-part=\"main\"]")?.scrollTo(0, 0), this.$("[data-part=\"body\"]")?.scrollTo(0, 0), this.$("[data-part=\"aside\"]")?.scrollTo(0, 0);
	}
	_open() {
		this.classList.add("is-open"), this.hidden = !1, this._viewerGen++, this._renderArticle();
	}
	_close() {
		this.classList.remove("is-open"), this.hidden = !0, this._viewerGen++, this._trackedPageViewArticleId = null, this._pageViewFallbackTimer !== null && (window.clearTimeout(this._pageViewFallbackTimer), this._pageViewFallbackTimer = null), this.emit("viewer-close");
	}
	openWithFlip(e, t) {
		this._cardEl = e, this._viewerGen++, this.classList.add("is-open"), this.hidden = !1, this._renderArticle();
		let n = this._article;
		if (!n) return;
		let r = this._viewerGen, i = window.matchMedia?.("(prefers-reduced-motion: reduce)").matches;
		document.title = n.title + " - " + t, requestAnimationFrame(() => {
			if (r !== this._viewerGen) return;
			let t = this.getBoundingClientRect(), n = e ? e.getBoundingClientRect() : null, a = n ? {
				left: n.left - t.left,
				top: n.top - t.top,
				width: n.width,
				height: n.height
			} : null, o = this.shadow.querySelector("[data-part=\"cover\"]"), s = o?.getBoundingClientRect(), c = s ? {
				left: s.left - t.left,
				top: s.top - t.top,
				width: s.width,
				height: s.height
			} : null, l = this.$("[data-part=\"panel\"]"), u = this.$("[data-part=\"body\"]"), d = this.$("[data-part=\"backdrop\"]"), f = this.$("[data-part=\"ghost\"]");
			if (l && l.classList.add("viewer-open"), u && u.classList.add("viewer-mask"), d && d.classList.add("viewer-in"), i || !a || !c) {
				u && (u.classList.remove("viewer-mask"), u.classList.add("viewer-in"));
				return;
			}
			if (l) {
				let e = {
					left: 0,
					top: 0,
					width: t.width,
					height: t.height
				};
				l.classList.add("is-anim"), l.style.transformOrigin = "top left", l.style.transition = "none", l.style.transform = `translate(${a.left}px, ${a.top}px) scale(${a.width / e.width}, ${a.height / e.height})`, l.offsetWidth, l.style.transition = `transform ${P}ms cubic-bezier(0.22, 1, 0.36, 1)`, l.style.transform = "";
			}
			if (f && o) {
				let n = (e?.querySelector(".card-thumb"))?.getBoundingClientRect(), i = n ? {
					left: n.left - t.left,
					top: n.top - t.top,
					width: n.width,
					height: n.height
				} : a;
				f.style.display = "block", f.style.left = "0", f.style.top = "0", f.style.transformOrigin = "top left", f.style.width = (i?.width || 0) + "px", f.style.height = (i?.height || 0) + "px";
				let s = f;
				s.src = o.src;
				let d = `translate(${i?.left || 0}px, ${i?.top || 0}px) scale(1, 1)`, p = `translate(${c.left}px, ${c.top}px) scale(${c.width / (i?.width || 1)}, ${c.height / (i?.height || 1)})`;
				f.style.transform = d;
				let m = f.animate([{ transform: d }, { transform: p }], {
					duration: 500,
					easing: "cubic-bezier(0.22, 1, 0.36, 1)",
					fill: "both"
				});
				setTimeout(() => {
					r === this._viewerGen && u && (u.classList.remove("viewer-mask"), u.classList.add("viewer-in"));
				}, Math.round(P * .8)), setTimeout(() => {
					r === this._viewerGen && (m && m.cancel && m.cancel(), f && (f.style.display = "none", f.style.transform = ""), l && (l.classList.remove("is-anim"), l.style.transition = "", l.style.transform = ""));
				}, 580);
			}
		});
	}
	closeWithFlip() {
		let e = ++this._viewerGen;
		this._swapPendingId = null;
		let t = this._article?.id, n = window.matchMedia?.("(prefers-reduced-motion: reduce)").matches, r = this._cardEl, i = this._rectOf(r), a = () => {
			if (e !== this._viewerGen) return;
			this._close();
			let t = this.$("[data-part=\"panel\"]");
			t && (t.style.transition = "", t.style.transform = "", t.style.transformOrigin = "", t.style.opacity = "", t.classList.remove("is-anim"));
			let n = this.$("[data-part=\"body\"]");
			n && (n.classList.add("viewer-mask"), n.classList.remove("viewer-in"));
			let i = this.$("[data-part=\"backdrop\"]");
			i && i.classList.remove("viewer-in"), this._ghostReset(), r && r.focus && r.focus({ preventScroll: !0 });
		};
		if (n || !t || !this._inViewportRect(i)) {
			let e = this.$("[data-part=\"backdrop\"]");
			e && e.classList.remove("viewer-in"), a();
			return;
		}
		let o = this.$("[data-part=\"body\"]");
		o && (o.classList.add("viewer-mask"), o.classList.remove("viewer-in")), setTimeout(() => {
			if (e !== this._viewerGen) return;
			let t = this.getBoundingClientRect(), n = this._toLocalRect(i, t), o = this.shadow.querySelector("[data-part=\"cover\"]"), s = o ? this._toLocalRect(this._rectOf(o), t) : null;
			if (!n) {
				a();
				return;
			}
			let c = {
				left: 0,
				top: 0,
				width: t.width,
				height: t.height
			}, l = this.$("[data-part=\"panel\"]");
			l && (l.classList.add("is-anim"), l.style.transformOrigin = "top left", l.style.transition = `transform ${F}ms cubic-bezier(0.45, 0, 0.55, 1), opacity 110ms ease ${F}ms`, l.style.transform = `translate(${n.left}px, ${n.top}px) scale(${n.width / c.width}, ${n.height / c.height})`, l.style.opacity = "0");
			let u = r ? this._rectOf(r.querySelector(".card-thumb")) : null, d = this._toLocalRect(u, t) || n, f = this.$("[data-part=\"ghost\"]");
			if (s && o && f && o.src) {
				this._ghostShow(), f.style.left = "0", f.style.top = "0", f.style.transformOrigin = "top left", f.style.width = s.width + "px", f.style.height = s.height + "px", f.src = o.src;
				let t = `translate(${s.left}px, ${s.top}px) scale(1, 1)`, n = `translate(${d.left}px, ${d.top}px) scale(${d.width / s.width}, ${d.height / s.height})`;
				f.style.transform = t;
				let r = f.animate([{ transform: t }, { transform: n }], {
					duration: F,
					easing: "cubic-bezier(0.45, 0, 0.55, 1)",
					fill: "both"
				});
				setTimeout(() => {
					e === this._viewerGen && (r && r.cancel && r.cancel(), f.style.width = d.width + "px", f.style.height = d.height + "px", f.style.transform = `translate(${d.left}px, ${d.top}px)`);
				}, F);
			}
			let p = this.$("[data-part=\"backdrop\"]");
			p && p.classList.remove("viewer-in"), setTimeout(a, 410);
		}, 130);
	}
	_rectOf(e) {
		if (!e || !e.getBoundingClientRect) return null;
		let t = e.getBoundingClientRect();
		return t.width > 0 && t.height > 0 ? {
			left: t.left,
			top: t.top,
			width: t.width,
			height: t.height
		} : null;
	}
	_toLocalRect(e, t) {
		return e ? {
			left: e.left - t.left,
			top: e.top - t.top,
			width: e.width,
			height: e.height
		} : null;
	}
	_inViewportRect(e) {
		if (!e) return !1;
		let t = e.left + e.width, n = e.top + e.height;
		return t > 0 && e.left < window.innerWidth && n > 0 && e.top < window.innerHeight;
	}
	_ghostShow() {
		let e = this.$("[data-part=\"ghost\"]");
		e && (e.style.display = "block");
	}
	_ghostReset() {
		let e = this.$("[data-part=\"ghost\"]");
		e && (e.style.display = "none", e.style.transform = "", e.style.width = "", e.style.height = "", e.style.left = "", e.style.top = ""), e && e.getAnimations && e.getAnimations().forEach((e) => e.cancel());
	}
};
customElements.get("article-viewer") || customElements.define("article-viewer", me), re();
//#endregion
//#region src/main.js
function he() {
	let e = document.getElementById("site-data");
	if (!e) return null;
	try {
		return JSON.parse(e.textContent);
	} catch {
		return null;
	}
}
var I = he(), L = I?.articles || [], R = I?.categories || [], z = I?.icons || {}, B = I?.siteName || "CallMeSoul", ge = I?.icp || "", _e = I?.social || [], ve = I?.archiveUrl || "", ye = I?.aboutUrl || "", be = I?.homeUrl || "/", xe = I?.giscus || { enabled: !0 };
function Se(e) {
	return R.find((t) => t.id === e) || null;
}
function V(e) {
	return L.find((t) => t.id === e) || null;
}
function H() {
	let e = location.hash.match(/[#&]cat=([\w-]+)/);
	return e ? decodeURIComponent(e[1]) : "all";
}
function U() {
	let e = location.hash.match(/[#&]art=([\w-]+)/);
	return e ? decodeURIComponent(e[1]) : null;
}
function W() {
	let e = location.hash.match(/[#&]tag=([^&]+)/);
	return e ? decodeURIComponent(e[1]) : "";
}
function Ce() {
	let e = {};
	return L.forEach((t) => {
		t.tags && t.tags.forEach((t) => {
			e[t] = (e[t] || 0) + 1;
		});
	}), Object.keys(e).sort().map((t) => ({
		name: t,
		count: e[t]
	}));
}
function G(e) {
	return Se(e) ? e : "all";
}
function K(e, t) {
	let n = t ? `cat=${t}` : "";
	return "#" + (n ? `${n}&` : "") + `art=${e}`;
}
var q = document.querySelector("site-sidebar"), J = document.querySelector("article-list"), Y = document.querySelector("article-viewer"), X = document.querySelector("search-panel"), Z = document.querySelector("search-results"), Q = document.querySelector("about-page");
if (q && (q.categories = R.map((e) => ({
	...e,
	count: e.count == null ? (I?.articles || []).filter((t) => String(t.cat ?? "").toLowerCase() === String(e.id ?? "").toLowerCase()).length : e.count
})), q.social = _e, q.siteName = B, q.icp = ge, q.tags = I?.tags && I.tags.length ? I.tags : Ce(), q.archiveUrl = ve, q.aboutUrl = ye, q.addEventListener("navigate", (e) => {
	let t = e.detail.tag ? "#tag=" + encodeURIComponent(e.detail.tag) : "#cat=" + encodeURIComponent(e.detail.cat);
	J ? location.hash = t : location.href = be + t;
})), Q && (Q.config = I?.about || {}), J && (J.articles = L, J.icons = z, J.addEventListener("tag-select", (e) => {
	let t = e.detail.tag;
	location.hash = "#tag=" + encodeURIComponent(t);
}), J.addEventListener("article-select", (e) => {
	let { id: t, cat: n } = e.detail;
	if (n) location.hash = "#cat=" + encodeURIComponent(n);
	else {
		let e = V(t);
		e && (location.hash = K(e.id, G(H())));
	}
})), Y && (Y.articles = L, Y.icons = z, Y.giscusConfig = xe, Y.addEventListener("article-select", (e) => {
	let { id: t, cat: n } = e.detail;
	location.replace(K(t, n || G(H()))), $();
}), Y.addEventListener("viewer-close", () => {
	U() && (location.hash = "#cat=" + encodeURIComponent(G(H())));
})), X && (X.articles = L, X.addEventListener("search-select", (e) => {
	let { id: t, cat: n } = e.detail;
	location.hash = K(t, n || G(H()));
})), Z) {
	let e = {};
	R.forEach((t) => {
		e[t.id] = t.name;
	}), Z.articles = L, Z.catNames = e, Z.addEventListener("result-select", (e) => {
		let { id: t, cat: n } = e.detail;
		location.href = "/" + K(t, n);
	});
}
function $() {
	if (!J) return;
	let e = G(H()), t = W(), n = U(), r = n ? V(n) : null;
	if (q && J && (q.setAttribute("active-cat", e), q.setAttribute("active-tag", t)), J && (J.setAttribute("active-cat", e), J.setAttribute("active-tag", t)), r && Y) {
		if (Y.article = r, !Y.classList.contains("is-open")) {
			let e = (J?.shadowRoot || document).querySelector(`.article-card[data-id="${r.id}"]`);
			Y.openWithFlip(e, B);
		}
		document.title = r.title + " - " + B;
	} else Y && Y.classList.contains("is-open") && (Y.closeWithFlip(), document.title = B + " - 首页");
}
window.addEventListener("hashchange", $), document.addEventListener("DOMContentLoaded", $);
//#endregion
